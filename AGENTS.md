# 给实现者的项目约定

> **本仓库约定的正本。** Codex 直接读这一份；Claude Code 读根目录的 `CLAUDE.md`，
> 那份只有几行，转发到这里。**两个工具看到的是同一份约定。**
>
> **这份文件只做路由和索引，不装规则。** 规则在 `process/`（通用）和 `docs/`（业务）。
> 一旦开始在这里写具体规则，两层就又混回去了。

---

## 这个仓库是什么

一个 AI Agent Skill：运营说自己卖什么，Agent 完成从产品理解到可发信名单的全过程，
产出带个性化英文开发信草稿的分层名单。TikTok + Instagram 双平台。

**注意这个仓库有两种代码**：

| | 位置 | 谁执行 |
|---|---|---|
| **Skill 定义** | `skill/` | Agent 读了之后照着做 —— 判断、编排、写草稿 |
| **采集脚本** | `scripts/` | Node 执行 —— API 调用、分页、去重、评分、产出 |

判断力在 `skill/`，机械劳动在 `scripts/`。**加功能前先想清楚它属于哪边** ——
把判断写进脚本会得到一个僵硬的规则引擎，把机械劳动交给 Agent 会又慢又贵。

---

## 两层文档

| 层 | 位置 | 内容 | 能不能改 |
|---|---|---|---|
| 通用 | `process/` | 流程、纪律、评定办法、验证手段。**与本产品无关** | 跨项目复用，改动要慎重 |
| 业务 | `docs/` | 本产品要什么、红线是哪五条、验收怎么算 | 按 `process/2-CHANGE.md` 的办法改 |

**业务层引用通用层，不复述它。** 在 `docs/` 里写下的每一条规则，
如果换个产品也成立，那它写错地方了 —— 移到 `process/` 去。

---

## 开工前按顺序读

1. `process/README.md` —— 流程全貌与总纲
2. `docs/SPEC.md` —— 需求（**唯一来源**）
3. 按你这次要做的事，读对应的一份：

| 你要做的事 | 读 |
|---|---|
| 写/改需求 | `process/1-REQUIREMENTS.md` |
| 想改需求、或想说"这个太麻烦" | `process/2-CHANGE.md` |
| 写代码 | `process/3-BUILD.md` + **`docs/CONVENTIONS.md`** |
| 写测试 | `process/4-VERIFY.md` —— **写测试前必读** |
| 改模块边界、调用顺序或对外契约 | `process/5-DESIGN.md` + **`docs/ARCHITECTURE.md`** |
| 改 Skill 的行为 | `skill/SKILL.md` + 对应的 `skill/references/*` |
| 改完了要同步 | `docs/SYNC.md` |

---

## 本项目的具体信息

- **红线**：`docs/SPEC.md` 的 **P1–P5**，共 5 条。不参与任何取舍
- **需求编号的唯一真相来源**：`docs/requirements.json`。
  `SPEC.md` 里的表格是它的渲染 —— 由 `npm run spec` 生成并校验，**不要手改**
- **架构文档**：`docs/ARCHITECTURE.md` —— 模块边界、**顺序契约**、Agent↔scripts 的缝隙契约。
  改这三样之前先读它；新增 `scripts/` 下的模块必须在锚点表里登记，否则 `npm run arch` 会红
- **决策记录**：`DECISIONS.md`，编号 `ADR-NN`（数据需求用 `D` 前缀，两套不撞号）
- **检查命令**：`npm run check`
  = 纪律 lint → SPEC 一致性 → **架构锚点** → 类型检查 → 需求测试 → 变异测试 → 脚本自检 → 链路审计
- **审计命令**：`npm run audit`
- **文档同步表**：`docs/SYNC.md`
- **CI**：`.github/workflows/check.yml`，每次 push 跑同一条链

---

## 本项目里反着的通用做法

`docs/CONVENTIONS.md` 列出了在这里**会违反红线的通用最佳实践** ——
防御性默认值、数据层面的优雅降级、catch 后返回空数组、DRY 到底、
封装内部细节、空值合并成布尔、给预算设默认值。

**你的默认做法在这里大概率是错的。写代码前扫一眼。**

---

## 目前已知的缺口

不要试图消灭它们 —— 它们是**显式**的，消灭掉只会让审计报告好看，不会让产品变好。

| 缺口 | 记在哪 |
|---|---|
| P2 的判断那一半机器查不了 | ADR-01 · `npm run audit` 会打印 |
| 测试与实现同上下文写成（同源污染） | ADR-04 |
| 语义筛选与开发信草稿的实际效果未经真实发信验证 | `docs/SPEC.md` 尚未确定一节 |

端点与响应结构已于 2026-08-26 用真实调用核实，双平台跑通。
但**产出物的质量**（语义判断准不准、开发信回复率）只有真发信才知道 ——
`docs/SPEC.md` 的「尚未确定的」一节列了哪些还没答案。

响应结构仍走探测 cascade（`scripts/providers/tikhub.ts` 的 `pickList`）：
TikHub 的 schema 随版本变，识别不出时打印顶层 key 而不是硬猜。

---

## 汇报纪律

1. **不要用流程术语**（环节名、分期、编号）。说"还差什么、卡在哪、下一步谁做"
2. **把"你让我做的事"和"我顺手做的事"分开说。** 修好工具不等于推进任务
3. **说"做不到"没关系，说"应该可以"不行**
