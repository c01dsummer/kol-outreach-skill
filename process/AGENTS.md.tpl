# 给实现者的项目约定

> 复制到项目根目录改名为 `AGENTS.md`,把 `<...>` 处换成本项目的内容。
> **同时在根目录放一个三行的 `CLAUDE.md` 转发过来**(或做符号链接),
> 这样 Codex 和 Claude Code 读到的是同一份。
>
> **这份文件只做路由和索引,不装规则。**规则在 `process/`(通用)和 `docs/`(业务)。
> 一旦开始在这里写具体规则,两层就又混回去了。

---

## 两层文档

| 层 | 位置 | 内容 | 能不能改 |
|---|---|---|---|
| 通用 | `process/` | 流程、纪律、评定办法、验证手段。**与本产品无关** | 跨项目复用,改动要慎重 |
| 业务 | `docs/` | 本产品要什么、红线是哪几条、验收怎么算 | 按 `process/2-CHANGE.md` 的办法改 |

**业务层引用通用层,不复述它。**在 `docs/` 里写下的每一条规则,
如果换个产品也成立,那它写错地方了 —— 移到 `process/` 去。

---

## 开工前按顺序读

1. `process/README.md` —— 流程全貌与总纲
2. `docs/SPEC.md` —— 本产品的需求(**唯一来源**)
3. 按你这次要做的事,读对应的一份:

| 你要做的事 | 读 |
|---|---|
| 写/改需求 | `process/1-REQUIREMENTS.md` |
| 想改需求、或想说"这个太麻烦" | `process/2-CHANGE.md` |
| 写代码 / 做 UI | `process/3-BUILD.md` + `docs/CONVENTIONS.md` + `<本项目架构文档>` |
| 写测试 | `process/4-VERIFY.md` —— **写测试前必读** |
| 改模块边界、调用顺序或对外契约 | `process/5-DESIGN.md` + `<本项目架构文档>` |
| 开分支、切改动、往主干合 | `process/6-INTEGRATE.md` |

---

## 本项目的具体信息

- **红线**:`docs/SPEC.md` 的 `<P 类>`,共 `<N>` 条。不参与任何取舍
- **需求编号的唯一真相来源**:`docs/requirements.json`(机器可读)。
  文档里的表格是它的渲染,**两者不一致以 json 为准,并当场修文档**
- **架构文档**:`<路径>` —— 模块边界、顺序语义、对外契约。**改这三样之前先读它**
- **决策记录**:`DECISIONS.md`,编号 `ADR-NN`
- **检查命令**:`npm run check` = `<列出这条链包含什么>`
- **审计命令**:`npm run audit`
- **变异清单**:`<命令>` —— 只列每条变异"违反了什么",不含实现原文;
  给写测试的上下文用(`process/4-VERIFY.md`)
- **文档同步表**:`docs/SYNC.md`

---

## 本项目里反着的通用做法

`docs/CONVENTIONS.md` 列出了在本项目中**会违反红线的通用最佳实践**。
写代码前扫一眼 —— 你的默认做法在这里可能是错的。

---

## 汇报纪律

向决策者汇报时:

1. **不要用流程术语**(环节名、分期、编号)。说"还差什么、卡在哪、下一步谁做"
2. **把"你让我做的事"和"我顺手做的事"分开说。**
   修好工具不等于推进任务 —— 摆在一起汇报会让人误以为进度前进了
3. **说"做不到"没关系,说"应该可以"不行**
