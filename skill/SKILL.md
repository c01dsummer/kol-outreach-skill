---
name: kol-outreach
description: 海外 KOL 建联助手 —— 从产品理解到可发信名单。用户说自己卖什么，Agent 完成关键词策略推导、TikTok + Instagram 双平台创作者发现、语义契合度筛选、分层评分，产出带个性化英文开发信草稿的 CSV 名单。当用户提到"找红人/博主合作""红人建联""KOL 采集""influencer outreach""帮我搜一批达人"等意图时使用。
---

# 海外 KOL 建联助手

把"我卖什么"变成"给谁发信、为什么是他、第一句话说什么"。

## 何时触发

- "帮我找一批 TikTok/IG 博主合作"
- "我要做红人建联"
- "帮我搜一批 skincare 类的达人"
- "influencer outreach / KOL sourcing"
- 用户给了一个商品链接并问怎么推广

## 核心原则

**你的价值在判断，不在采集。**

采集谁都会做，SaaS 工具在筛选导出上做得比你好。你不可替代的是四件事：读懂产品、推导搜索策略、判断内容调性是否匹配、写出有针对性的第一封信。把力气花在这四件事上。

具体到执行：

1. **便宜的步骤先跑，额外请求只对幸存者跑** —— 先小样试探再放量；主页公开指标只对语义判断为 ✅/⚠️ 的候选执行
2. **输出理由，不只输出分数** —— 运营看得懂"为什么这人排在前面"才会信任名单
3. **花费全程可见** —— 用户看不到请求在烧钱，你必须主动报告
4. **不确定就说不确定** —— 召回差、平台不匹配、识别不确定，如实讲，不要凑数

## 前置检查

需要新增 API 请求时确认 `TIKHUB_API_KEY` 可用；缺 key 时按脚本提示配置。已有数据导出、全部缓存重算及其他零请求路径不要求 key；账户余额与本地预算是两件事，供应商返回 402 时再按实际提示处理。

当前仓库不接入外部增强供应商，也不读取任何第二方 key。不要声称邮箱已经验证或受众画像已经补全。主页公开指标仍使用 `TIKHUB_API_KEY`；用户不需要这批指标时可以跳过，主流程照常完成。

## 六个阶段

### Phase 01 — 产品理解

目标：用最少的提问拿到足够信息。

**能推的绝不问。** 用户给了商品链接（亚马逊/独立站/店铺页），先抓下来自己读：品类、卖点、价位带、目标人群、竞品。没给链接就问一句"有商品链接吗，有的话我直接看"。

**只问这三个**，因为都无法从链接推导，且都直接影响后续每一步：

| 问题 | 为什么必须问 |
|------|--------------|
| 目标市场 | 决定搜索 `region` 参数和受众降权规则 |
| **本次预算上限（美元）** | 成本闸门，见下方 |
| 要找多少人 | 决定采集深度和停止条件 |

**反模式**：把七个维度做成问卷逐个问。运营会在第三个问题时放弃。三个问题一次性问完，能合并成一句就一句。

详见 `references/product-intake.md`。

### Phase 02 — 关键词策略 + 小样试探 ★

这是整个流程最关键的一步。搜错词，后面每一步都在浪费。

1. 按品类词 / 场景词 / 竞品词 / 人群词四个维度生成候选词
2. **两个平台分别出词** —— IG 可先试短词；早期 `smoothie` 样本提示词长会影响返回，但不证明所有词组都无结果，按本次小样调整
3. **每词每平台只抓 1 页**，把真实抓到的人摆给用户看
4. 问一句"这批对吗"，不对就调词重来

试探用于减少整轮返工；请求数按本次词与补全动作计算，费用按对应端点声明另核。**不要跳过这一步直接放量（F3）。**

若本次某平台的关键词与发现路径召回都差，如实说明本次没找到足够候选，再建议调整方向；不能据此宣称该平台没人做这类内容，也不要硬凑数量。

详见 `references/keyword-strategy.md`。

传给 probe 与 collect 的 `tasks` 至少有一个任务；每项明确写出非空白 `keyword`、`category`/`scene`/`competitor`/`audience` 之一的 `dimension`、`tiktok`/`instagram` 之一的 `platform`。脚本原样校验，不补值；坏任务与 IG 路线问题一起报告，退出 2，不发请求、不写任务文件。旧任务续跑及改预算也查整张列表，包括已完成项。按报错指出的文件、任务和字段修配置，不跳过坏项来继续（D16）。

市场 `market` 写非空白字符串，collect 目标人数 `target_count` 写有限数，保留用户给的合规原值；不自行修剪市场、改大小写或转换人数。新 collect / probe 仅在市场缺席时用 US，并明确转述 stderr 的默认说明；新 collect 仅在人数缺席时用 50，probe 不校验人数。显式 null 或其他坏值不能套缺省；旧任务缺任一字段（含改额续跑）也不能补造。字段错误与任务/路线问题合并报告（collect 预算输入合规时），退出 2，先于建目录、任务写入、预留和请求，旧 task.json 不变。按原始意图修输入，不增加国家或人数限制；细节见 `references/product-intake.md`（D17）。

### Phase 03 — 规模采集

策略确认后才放量。自动分页、跨关键词全局去重、跨平台同人识别。

- **轮转采集**：先给每个关键词各抓一页，再回头抓第二页。**不能顺序跑完一个词再跑下一个** —— 实测竞品词一个词就够 50 人，一个词就能把整轮配额翻完
- ✅ **任务列表里每个关键词×平台都至少被问过一次。** 达标之后**不是一律停** —— 还没抓过第一页的任务照抓不误，第一页之后才按达标停（F9）。所以**达标收尾时**（`stopped` 为 `target`）**task.json 的顺序不再决定谁被砍掉**，IG 写在前面还是后面都一样。⚠️ **预算先用尽时（`stopped` 为 `budget`）顺序照样决定谁被砍掉** —— 实测同一批任务、同样预算，IG 排最后时 IG 一次都没被问过，IG 排最前时被砍的换成 TikTok。所以**预算可能不够时，把最在意的平台排在前面**。⚠️ **它保证的是「问过」，不保证问到多少人** —— IG 词第一页之后按续页令牌翻、达标即停（D6.u），实际条目数与去重作者数分开看，补完 profile 还可能被粉丝闸门全筛掉。**当前可通过增加 IG 关键词探索更多候选**，但跨词重叠只在两个词的首页上量过一次（搜到的作者没有交集，ADR-101 第十五节），不能外推到别的词，不保证每加一个词都增加新人；脚本不设任何默认的平台配额。⚠️ **预算比这条保证优先**：不够抓齐第一页时照旧存断点、退出码 3（stdout 的 `stopped` 为 `budget`），没问到的那些仍在 `pending_keywords` 里，不会为了凑齐第一页超支。达标停下（`stopped` 为 `target`）时仍要把 `pending_keywords` 告诉用户 —— ⚠️ 它列的是**没跑完**的词，**不是没跑过的**：搜过一页、还没到页数上限的 TikTok 词也在里面（IG 词在一次运行结束时就记进完成 —— 令牌不跨运行，续跑不会再为它请求，D6.v），而 stdout 今天仍然分不出这两者 —— **P5.i 补的是 `meta.json` 与 HTML 报告那一半，没碰 stdout**（ADR-94 第十六节丁）。所以转述时说「这些词还没跑完」，**别说「这些方向没查过」**。**续跑会不会再发关键词搜索，照 stderr 那句话转述，别自己推** —— 脚本每次都按当时的数重算了才打那句话（详见下面「续跑」那一段）
- **单关键词页数上限 4 页，跨运行累计**（D6.h）—— 达上限的词记进 `done`，追加预算续跑也不会再翻它。⚠️ 在这条之前，这句话**只在一次运行内成立**：被预算卡停、或出错中止的那一跑不留页数记录，续跑于是又给一份满配额（实测跨四次续跑累计 12 页）。⚠️ **上一版留下的目录**（本条之前建的）里没有页数记录，那种目录里已经抓过页的词**此后一页都不再翻**，只补还欠着的第一页（D6.m）—— 要接着往下翻就新建一个任务目录
- 目标人数达标即停（数的是**能进名单的人**，不是采集总数）—— **但还没抓过第一页的任务不在「停」的范围里**，见上一条
- 请求间隔 150ms（限速 10 RPS）

TikTok 走视频搜索，**IG 缺省走 Reels 搜索**，取得作品及作者线索（只有用户明确要求时，才给某个 IG 任务写 `ig_route: "hashtag"` 改走话题搜索：只取首页、不翻页、不走兜底，Agent 不自行添加 —— D15.j/k、D6.w，ADR-112）；两者都可能命中商家与机构，仍须读内容判断。**IG 侧顺着续页令牌翻页，页数随达标走**（D6.u、ADR-111）：响应里的 `data.pagination_token` 就是官方声明的分页参数，采集器带着它请求下一页；没达标就继续翻、达标即停，单个任务仍受 4 页上限约束。⚠️ **令牌只在同一次运行内有效**（不写进 `task.json`）：一次运行结束时还握着令牌的 IG 任务记进 `done`，续跑不会再为已经抓过页的 IG 任务花钱（D6.v）—— 预算中断后续跑，IG 那一侧不会接着翻，要向用户说清楚。2026-09-22 一次实测中，带令牌的后续请求获正常响应。历史 `smoothie` 记录中，链式请求观察到的累计去重作者多于等次数原样重发，重发又多于单请求；差异不能归因给令牌，也不证明统计显著性，证据范围见 ADR-101 第十三节。每页 $0.002，与 TikTok 续页在同一轮转、同一预算里竞争；跨词作者重叠只在 selfcare／journaling 两个词的首页上量过一次：搜到的作者没有交集（按用户名认，没有粉丝数，不是按「能进名单」的口径数的），说明不了多加一个词能多出多少能进名单的人，不能外推到别的词或更深的页（ADR-101 第十五节）；页数随达标走是需求所有者的取舍，不是证据证明的最优（ADR-111 第三节）。复跑 `npm run probe:ig-paging -- --keyword smoothie --chain 10`（自带等次数原样重发曲线；只比较本次累计量，差值不能归因给游标或端点波动，缺续页令牌也不等于结果耗尽；ADR-101 第十三节）。⚠️ 历史样本（两个词的 Reels 首页全是视频）不足以判定 Reels 搜索对纯图文、轮播及 PhotoMode 的覆盖，不能由接口名称判定哪些创作者必然找不到，匹配与排序也不由样本证明（ADR-101 第十五节）。工具是 `npm run probe:ig-paging`（要能出网的机器＋充过值的 key），字段命名两边不一致、以及怎么读那个工具的结论，都在 `references/providers/tikhub.md`。

### Phase 04 — 语义筛选 ★

**硬指标按规则算分**：粉丝区间、内容积累、有无邮箱、商务合作信号。这部分是客观的。

**内容相关性靠你判断**：读 bio + 实际取得的搜索命中作品文案，回答"这个人适合推这个产品吗"，并给出**理由**。`recent_posts` 是关键词筛过的样本，不是主页最近若干条作品；理由只描述见到的证据。

账号的 `discovery_sources` 由脚本记录实际返回它的搜索路径；可据此区分 Reels、账号名兜底与话题搜索。缺席或空数组表示来源未知，不补写；已观察集合可能不含完整历史，也不对应某条作品或请求次数。关键词任务标签与 `as_hashtag` 不能代替实际来源。

理由比分数重要。形如：

- ✅ 做 5 分钟早餐内容，出镜频繁，厨房场景多 —— 强相关
- ⚠️ 标榜健身，但本次搜索命中的作品都在带减脂代餐 —— 与本产品品类可能冲突，需确认合作情况
- ❌ 账号名含品类词但内容是搬运合集，无真人出镜 —— 疑似机构号

评分细则与分层规则见 `references/semantic-fit.md`。

### Phase 05 — 公开指标评估（可选）

语义筛选完成后，对 `fit=✅/⚠️` 的候选抓取主页最近作品：

```bash
npm run enrich -- --dir output/{task}
```

分平台计算粉丝互动率、播放互动率、中位播放量、播粉比、触达稳定度、发帖间隔与
最后发布距采样时间。活跃标签为 `active`（≤45 天）、`cooling`（46–90 天）、
`dormant`（>90 天），只做提示，不改变 score 或分层。
`high` 受众质量风险只降一级进入人工复核，永不自动排除。

**这不是假粉率，也不是带货预测。** 公开信号只能发现相对同行的异常；实际带货需要订单、
点击和转化数据，本 Skill 不做归因。没有运行本步骤时保持“未查询”，不要补默认值。

合作报价只有用户提供 creator quote 或公开 rate card 时才记录；不根据粉丝数自动估价。
详见 `references/public-metrics.md`。第三方供应商的准入条件见
`references/providers/external-enrichment.md`，当前没有任何一家接入。

### Phase 06 — 交付

- 分层名单 CSV（UTF-8 BOM，Excel/Numbers 直接打开中文不乱码）
- **每个 A 级候选一封英文开发信草稿**，基于 Phase 04 实际读到的内容
- 可读的 HTML 报告
- 分平台公开指标、风险依据与数据边界（运行 Phase 05 时）
- 成本与召回统计

开发信只写英语，见 `references/outreach-draft.md`。输出格式见 `references/output-format.md`。

导出要求 `task.json` 可读且 `tasks` 至少有一项合规任务；缺列表、空列表或坏任务也不能带着已有名单继续导出。render 在写名单、表格、报告或联系记忆之前退出 2，stderr 给出文件路径与输入问题，已有文件保持原样。按原搜索配置恢复真实任务记录；不能为通过校验编造关键词、维度或平台。费用未知仍可离线导出，但不豁免任务输入校验（D16）。

## 成本闸门与续跑

询问本次**总预算**；新任务未提供时按 $2 并明确告知。probe 是独立进程预算，缺席默认 $0.5；不能把试探费用当成任务累计费用。显式非法金额不得套默认值。

脚本按实际端点的固定公开基础价预检每次请求，保留精确小额（如 $0.005）。转述输出的预算占用与分项，不由请求数乘单价：占用可含无 HTTP 状态的保守留存及未结额，**不是实际账单，也不保证未来价格**。详细费用字段见 `references/output-format.md`，来源见 `references/providers/tikhub.md`。

50% 与 80% 每个运行实例各提醒一次；退款不重置。余额买不起下一请求就暂停，不必已用到 100%。`collect/enrich` 只有保存成功才退出 3；报告进度后由用户选择提高总上限、用已有数据收尾或缩小目标，不猜还需多少钱。

每个阶段结束报告当前进度与已知费用；费用不可用时说明原因，不造占比。80% 提醒时给出继续、缩小目标、用已有数据收尾三个选项；提醒保持简短，已确认的预算不重复询问。

用户已确认新总上限时继续原任务：

```bash
npm run collect -- --resume output/{task} --budget <新总上限>
npm run enrich -- --dir output/{task} --budget <新总上限>
```

`--budget 3` 表示总上限 $3，不是再加 $3；改额不重算历史费用。旧账缺失、损坏、价目无法核实或有未结项，不能靠加钱恢复。退出 2 报具体输入/费用问题，退出 1 报运行或保存失败，不能统称预算用尽。

费用不可用仍可导出已有数据，零请求 collect 和缓存/私密 enrich 可继续本地处理。不要重建账目、把 requests=0 当免费历史，或重新 `--config` 覆盖任务；新建另一个任务可能重复请求，需按用户意图决定。

续跑是否新增请求，按 stderr 的待查关键词、待补 profile 和费用状态转述。达标不表示所有词已查询；第一页保证、跨运行页数上限和既有调度仍适用。collect/enrich 每次请求前保存费用预留，强制中断留下未结项时保留占用并阻止付费与改额，不能自行退款或重发。费用保存失败退出 1，即使部分结果随后保存成功也不能说本轮成功。终态已保存而业务响应未保存时仍可能重查；不承诺断电或多个写者安全。probe 仍只有本进程预算。

## 跨任务记忆

每次任务结束后写入 `memory/creators.json`。开始新任务时先读，**已推荐过的人默认不再进新名单**。
但**同一个任务自己上一轮的推荐不算**，否则续跑会把自己的产出判成「已推荐过」而滤成空名单。

`collect.ts` 以**退出码 2** 结束且 stderr 提到 `memory/creators.json` 时，表示那个文件读不出来 ——
**停下来。不要重跑，不要拿旧名单往下走，也不要自己找路绕过它。**
采集结果和已花的预算都完好，把 stderr 的原话报给用户（它会说是哪个人的哪个字段不对），
让用户修好文件后按 stderr 给出的那条命令 `--resume`（预算用尽时那条命令会带上 `--budget`）。

stderr 还会给出另一条路：加 `--ignore-memory` 强出名单。**不要自己加。** 强出名单是用户的取舍，
不是你的 —— 他明确要求时才加，并按 `references/memory.md` 把代价说清。

**续跑要不要花钱，照 stderr 转述**：剩余关键词和待补 profile 都为零才是零请求；有待查项还要看费用是否允许付费。费用未知时报告待查量与原因，不能说任务已完成，也不能承诺只需加钱。

详见 `references/memory.md`。

## 参考文档

| 文件 | 内容 |
|------|------|
| `references/product-intake.md` | 产品诊断模板、从链接提取信息、七大品类的关键词倾向 |
| `references/keyword-strategy.md` | 四维关键词生成、两平台分别出词、试探判读标准 |
| `references/providers/_interface.md` | 数据源适配接口契约 |
| `references/providers/tikhub.md` | TikHub 端点、参数、字段路径、双平台差异 |
| `references/providers/external-enrichment.md` | 第三方增强供应商准入；当前无接入 |
| `references/semantic-fit.md` | 语义契合判断标准、评分细则、A/B/C 分层 |
| `references/public-metrics.md` | 公开指标、受众质量风险与合作报价口径 |
| `references/outreach-draft.md` | 英文开发信写法与模板 |
| `references/memory.md` | 记忆文件结构、跨平台同人识别 |
| `references/output-format.md` | CSV 列定义、HTML 报告 |

## 不做什么

| 不做 | 原因 |
|------|------|
| **实际发送邮件**（S1） | 送达率、SPF/DKIM、退信处理是另一个量级的复杂度，且必须用用户自己的邮箱服务。产出草稿，发信交给用户现有工具 |
| 多语言开发信（S2） | 首版仅英语 |
| 主动索要效果回填（S3） | 用户回填成功率太低。`contacted` 由用户手动标记 |
| 中国大陆平台（S4） | 本 Skill 定位出海 |
| CRM / 付款 / 寄样 / 归因（S5） | 超出 Skill 形态 |
