# 不带斜杠 —— 带斜杠只匹配目录，挡不住 worktree 里那条同名的符号链接，
# 而 `git add -A` 会把它收进仓库：别的检出（含 CI）拿到的是一条指向
# 作者机器绝对路径的死链。实例与代价记在 ADR-94 第十六节丁、ADR-97 第七节
node_modules
.env
.env.local
output/
.check-cache/
memory/
.DS_Store
# 运行证据只在本地保存，不提交到仓库。
evidence/
