# 架构 · 零件之间

> **这份文档只写代码说不出的事。**规则在 `process/5-DESIGN.md`。
>
> 零件本身去看代码和类型；**零件之间**的顺序、方向、契约、字段归属在这里。
> 一句话如果读完代码之后不再是新信息，它就不该在这一页上。
>
> **下面两张表没有机器核。** ADR-78 撤掉了 `npm run arch`：它查得了覆盖与绑定，
> 查不了说明列写的还对不对 —— 而真正漂的一直是说明列。撤它那天实测：这一页上
> 「闭包实测 14 个」实际是 13、「闭包从 14 个缩到 3 个」实际是 5，**而 arch 一路全绿**。
> 改了模块、顺序或依赖方向，靠 `docs/SYNC.md` 提醒你回来改；
> **忘了改的那一次，没有东西会拦下来。**

---

## 一件新工作放哪边

仓库有两种代码，**加功能之前先决定它属于哪边**：

| | 位置 | 谁执行 | 装什么 |
|---|---|---|---|
| **判断** | `skill/` | Agent 读了照做 | 读懂产品、推关键词、判断内容匹配、写草稿 |
| **机械** | `scripts/` | Node 执行 | API 调用、分页、去重、评分、落盘 |

判据一句话：

> **同样的输入，两次跑出来必须一模一样吗？**
> 必须 → `scripts/`。允许不一样、且不一样才有价值 → `skill/`。

- 分层规则（`fit === '❌'` 一律 C；有邮箱且 `fit === '✅'` 才是 A）**必须**每次一样
  → `scripts/lib/score.ts`
- 「这个做露营装备测评的博主适不适合卖充电宝」，也就是那个 `fit` 从哪来，
  **不该**每次一样 → `skill/references/semantic-fit.md`

反过来用最容易出事：把判断写进脚本会得到一个僵硬的规则引擎（关键词表越写越长，
永远差一个 case）；把机械劳动交给 Agent 会又慢又贵，而且**不可复现**——
同一份数据两次跑出两份名单，用户无法知道哪一份是对的。

---

## 模块锚点表

层的含义：**入口**只做读参数、落盘、打印、退出码；**逻辑**装有语义的决策；
**适配**把外部响应翻译成内部类型；**检查**是检查链自身。

> 入口层不许放决策逻辑 —— 判据是「这段代码的顺序错了会不会出错」。
> 会，它就有语义，就该搬进 `lib/`（`docs/CONVENTIONS.md` 第 10 条 —— 那条自己数着踩过几次，这儿不抄）。

> ⚠️ **这张表是人维护的，没有任何检查核它。** 表里可能留着已经删掉的模块，
> 磁盘上可能有没登记的模块，说明列写的可能早就不成立 —— 最后一项从来就没人核过
> （ADR-12 记着四次漂移），前两项从 ADR-78 起也没人核了。**当索引读，别当证据读。**

<!-- BEGIN:ANCHORS 人维护，无机器校验（ADR-78） -->

| 模块 | 层 | 服务的需求 | 它保证什么 |
|---|---|---|---|
| `scripts/probe.ts` | 入口 | F3 P1 P3 | 每词每平台只抓一页，供 Agent 判读方向；拿不到粉丝数报「未知」而不是 0 |
| `scripts/lib/discovery.ts` | 逻辑 | D15 | 实际观察来源的稳定并集与人读投影；不从任务配置或 profile 推断历史，不控制请求或评分 |
| `scripts/lib/ig-route.ts` | 逻辑 | D15 | IG 路线配置的校验与话题请求词；collect 与 probe 共用这一份，在任何请求之前调用 |
| `scripts/probe-ig-paging.ts` | 入口 | — | 核实工具，不在任何管线上：拿真 key 问 `instagram/v2/search_reels` 到底能不能翻页。三个模式 —— 默认只打一次报响应形状；`--chain N` 把上一次响应里的 `pagination_token`（**官方 spec 声明的参数名，不是猜的**）接进下一次；`--repeat N` 原样重发只量漂移。**`--chain` 一定同时跑 `--repeat` 那条对照曲线**（所以发 2N 次请求）—— 这个端点会漂，少了对照，链上多出来的人分不清是翻页给的还是漂给的，而两种读法的结论正好相反。量的是**人**不是条目：同一个人的多条视频不算多个人，「条目在涨、人没涨」单独报出来。末尾不涨只说得出「这 N 次之内没再涨」；**唯一不靠推断的终止条件**是服务端不再给下一个游标 —— 那是它自己说的 |
| `scripts/collect.ts` | 入口 | D6 P3 F7 D4 F9 | 轮转采集不让第一个关键词吃掉全部配额，**且任务列表里每个关键词×平台都至少被问过一次** —— 达标之后仍然给「一页都没抓过」的任务补第一页，第一页之后才按达标停（F9）。⚠️ 它保证的是**问过**，不保证问到多少人；预算比它优先，不够抓齐第一页时照旧存断点退 3；记忆读不出来退 2 且不产出名单 |
| `scripts/enrich.ts` | 入口 | D8 D10 F8 P3 | 只对语义筛选后的候选抓主页样本；已查过的账号默认不重复付费，但每次都按当前口径就地重算（零请求） |
| `scripts/render.ts` | 入口 | P5 U1 U2 U5 U7 D4 | 交付物生成的唯一出口，且是唯一往跨任务记忆写回的地方；写回在报告之前，报告才能声明它的结果；四个交付物都走整体替换 |
| `scripts/lib/pipeline.ts` | 逻辑 | D6 D11 P1 P4 F5 F8 U1 U3 | 入口脚本原先裸露的两段管线；**顺序契约全在这里**，见下表。「这个人还要不要补 profile」**只此一份**（`needsProfile`）—— 补全循环拿它挑人，「续跑要不要花钱」拿它算钱；各写一份的话，先改的那边不会报错，于是循环去补的人和账单上说的人对不上（ADR-25）。**「这个任务一页都没抓过」同样只此一份**（`firstPagePending`）—— 采集调度拿它决定达标之后还补谁，「续跑要不要花钱」拿它算钱，同一个先例（F9.c）。**「这个任务抓了几页、还能不能再翻」也只此一份**（`pagesFetched`／`underPageCap`，常量 `MAX_PAGES` 与判定放在一起，不从入口传进来）—— 采集调度拿它决定还翻不翻，而「续跑要不要花钱」靠 `done` 与它对上；传参或各写一份的话，两边可以各拿一个数，而先改的那一边不会报错（D6.h／D6.m）|
| `scripts/lib/task-label.ts` | 逻辑 | U8 P5 | 接收原任务下标生成展示身份；只依赖类型，入口和管线共用，不推断搜索路径 |
| `scripts/lib/posts.ts` | 逻辑 | D11 P1 | 搜索作品键的归一化与稳定并集共用一份契约；适配器给真实标识加平台前缀，采集累加和同人合并都在这里去重，缺标识不丢作品 |
| `scripts/lib/score.ts` | 逻辑 | P1 F6 F8 | 打分、分层、粉丝闸门、两种降级判定；语义否决对分层有一票否决权 |
| `scripts/lib/identity.ts` | 逻辑 | D1 D2 D3 D11 P1 | 跨平台同人识别与合并；不确定不合并，未知粉丝数相加仍是未知 |
| `scripts/lib/memory.ts` | 逻辑 | P4 D4 D6 | 跨任务记忆的读写与过滤；「文件不存在」与「读不出来」是两个状态，后者抛而不是退化，且绝不拿它去覆盖原文件；解析成功还要过结构校验，键按 D1 规范化，形状不对或撞键当读不出来；写回前生成的键过同一道校验；写回走临时文件加改名，写不进去报为未写回而不中断交付 |
| `scripts/lib/atomic.ts` | 逻辑 | D4 | **唯一一份**整体替换写入：临时文件按最严权限建、目标原有的权限位带过去、失败清半成品、写之前清掉已死进程留下的残留（活着的与刚写下的不动，清不掉不算失败）、目标只读时不替换（改名会绕过文件自己的权限）、目标是软链时写它指向的那份；改名前刷文件、改名后与新建目录逐层尽力刷目录 —— 尽力而为，不作保证（ADR-50）。记忆、任务目录的状态文件、四个交付物、以及两份覆盖记录（`test.ts` 的单元认领、`selfcheck.ts` 的入口认领）都经它落盘 —— 各写一份时同一个权限位的 bug 修好一次又原样重现 |
| `scripts/lib/cost-ledger.ts` | 逻辑 | D12 D13 P1 P3 P5 | 精确金额、费用账校验与预留结算；只接收固定价目，不依赖文件或网络；总上限修改不重算历史项 |
| `scripts/providers/tikhub-pricing.ts` | 适配 | D12 P5 | 固定公开价目的版本与完整端点报价，供费用账核对历史依据；不远程查价、不推断账户账单 |
| `scripts/lib/budget.ts` | 逻辑 | D13 D14 P3 F7 | 连接价目与费用账，延迟检查付费资格；同一 state 同步账目/次数，由任务入口注入费用保存回调，预留和终态保存失败锁定该实例；process 预算不落盘 |
| `scripts/lib/cost-json.ts` | 逻辑 | D13 P3 | 根预算、次数与费用账原数值的精确边界；非法费用数字保持不可用并保留原件，任务文件、meta 与 stdout 共用，HTML 只读金额文本 |
| `scripts/lib/email.ts` | 逻辑 | D7 | 反爬写法的邮箱提取；宁可返回 null 也不误判正常语句 |
| `scripts/lib/assessment.ts` | 逻辑 | D8 D9 D10 F8 U7 | 公开样本 → 指标 / 风险 / 活跃度 / 报价效率，每项带测量状态与溯源；**样本记录的窗口也截在这里**，入口脚本只负责调用与落盘 |
| `scripts/lib/rows.ts` | 逻辑 | P2 U1 U5 U7 | 交付物的行与排序；草稿里的占位符必须原样穿透到产出物 |
| `scripts/lib/csv.ts` | 逻辑 | D5 D4 | UTF-8 with BOM 与转义；落盘走整体替换 |
| `scripts/lib/xlsx.ts` | 逻辑 | U5 D4 | 零依赖 ZIP 与 CRC32；空分层也建 sheet；落盘走整体替换 |
| `scripts/lib/report.ts` | 逻辑 | P5 U2 U3 U4 U6 | 单文件内联 HTML；数据边界声明与分层 tab 的初始可见性 |
| `scripts/lib/task.ts` | 逻辑 | D6 D4 D14 | 任务目录的读写；费用检查点只替换盘上任务的账目与次数，保留根预算和业务字段；**累加器与交付物是两个文件**，名单与去重状态分三步落盘，肯定的声明最后写 |
| `scripts/lib/types.ts` | 逻辑 | P1 D1 D8 D11 | 三态模型的定义处 —— 它把「没查到」和「查了没有」在类型层面分开；**D1 的身份键与支持的平台列表只此一份**，采集去重、记忆读写、账号评估都调它 —— 各写一份表达式时的「一致」只是巧合 |
| `scripts/providers/tikhub.ts` | 适配 | D2 D3 D8 D11 P1 | 唯一的数据源实现；响应结构走探测 cascade，识别不出时暴露顶层 key |
| `scripts/check/size.ts` | 检查 | — | 把「一个改动 = 一个能一次读完的 diff」变成能报错的检查；量的是分支相对主干的新增行，基线算不出来时说「无从判断」而不是放行；**量两遍**（一遍照实定文件清单、一遍忽略行内空白定计数），折掉多少逐类打出来 |
| `scripts/check/size-rule.ts` | 检查 | — | 上面那条闸门的**判定**本身。四类分开算、豁免必须指名类别；**「量新增行带哪些 git 参数」也在这里**（`GIT_CONFIG` 与两个 `NUMSTAT`）—— 它决定这道闸门看得见多少改动，所以是判定，由一个真 git 仓库的夹具钉住（ADR-98）；抽出来是为了它能被测 |
| `scripts/check/trailer.ts` | 检查 | — | 提交信息的 trailer 块。当初两个闸门都要在提交信息里读指令，分支寿命那道 2026-09-18 撤了（ADR-76），现在只剩体量一个调用方；而提交信息里也会举例说明这些指令 —— 判据借的是 git 自己的 trailer 定义，**只此一份** |
| `scripts/check/spec-sync.ts` | 检查 | — | SPEC.md 的表格由 requirements.json 生成，两者不可能漂移 |
| `scripts/check/spec-rule.ts` | 检查 | — | 需求登记表的**判定**：形状（根对象与每条需求：渲染要读的字段必须在，缺了会把空值写进生成的文档）、完整性（编号唯一、交点与决策记录编号真实存在、作废须有理由）、表格渲染与内容指纹（凡影响渲染的都进指纹，包括分类表及其顺序）、审计对一条需求的裁定（红线判据逐条认领，**单元断言与自检端到端跑过的两种认领同权**，但两份记录分开交进来：**只由自检认领的判据（单元断言一条都没认），必须有一条 `by: "selfcheck"` 的负片，否则硬失败** —— 认领与负片要来自同一头，缺省那个验证者够不到入口，指着它的变异不写 `by` 就只会「存活」；这一条与红线无关，凡是这种认领都要，⚠️ 但它不替判据拆分把关（一条负片盖住四半里的一半也算数）；一条都没认领同样是缺口；**只有没人认领的那条豁免**才把 `✓` 压成 `⊘` ——认领过的判据即便名下还挂着一条豁免，也算完整，不再是缺口）、报告里那两格的排版（「变异」与「判据 N(负片 K)+⊘M/T」——**判据级的负片此前在报告里一个字都没有**：变异的 `req` 两种编号混着写，而审计只拿需求号去查。两格都曾拼在入口的模板串里，谁也够不着，删掉一档照样全绿）、审计对一个交点的裁定（认领编号两侧顺序无关；**有红线的交点没有测试认领就是硬失败**）；不碰文件系统，因而可被测 |
| `scripts/check/quoted.ts` | 检查 | — | 引文遮罩:一段 Markdown 里哪些行是在**演示**语法而不是使用它(围栏块、HTML 注释)。**凡是要按结构解析 Markdown 的地方都先过它** —— 同一个坑在两处各栽过一次 |
| `scripts/check/adr-sync.ts` | 检查 | — | 决策记录一条一个文件；编号唯一、文件名与正文一致、索引不漂移，并守住 `DECISIONS.md` 不再装回整册 |
| `scripts/check/adr-rule.ts` | 检查 | — | 上面那条检查的**判定**本身。文件名与编号的对应、撞号、索引渲染 |
| `scripts/check/mutate.ts` | 检查 | — | 给测试做的测试：变异存活即那条测试是假的；验证者崩了不算抓到，**夹具没造对同样不算**（两者各有自己的记号，自检打、判定认，见了整次判跑不起来 —— 那都不是任何一条断言的功劳）。缺省的验证者是 `scripts/test.ts`；写了 `by` 的那些改跑别的（今天只有自检），为的是接线那一层 —— 入口脚本里的接线 `test.ts` 够不到。⚠️ **对只由自检认领的判据，`by` 不是可选的**：没有一条这样的负片，审计硬失败（原先这类缺口只能在 `exemptions` 里顶着，落地 4 起改成闸门）。`by` 与 `kills` 同进同出 —— `kills` 点明该红的是哪几条夹具（一组，每一条都要红；写成单个名字当场拦下）；**点名的那些一旦都红过，入口就把验证者整组停掉**，不必等它跑完（判据「见没见齐」在 `mutate-rule.ts`，这里只管什么时候杀），而不写 `by` 只写 `kills` 虽然在机制上生效也照样拦下：那是 ADR-70 明写「另外评定」的那条延伸。**自己验自己的当场拦下**：从种子读文件建图、递归收出验证基础设施闭包（多大随仓库动，命令见 ADR-82），改在闭包里又指名了验证者的变异一律不算数 —— 建图这一半（读文件）在这里，判据在 `verifier-rule.ts`。**开跑之前的三道体检有固定顺序**：接线成不成立 → 自己验自己 → `kills` 点的夹具立不立得住。由粗到细 —— 名字不认得连判定都拿不到验证者；自己验自己是**这份证据整份不算数**；点不着夹具是**证据算数、功劳记错了人**。反过来排的话，一条自己验自己的变异会先因为名字问题被打回，人改完名字再撞第二堵墙。第三道读验证者的源码建清册（同一个验证者只读一次），判据在 `mutate-rule.ts` |
| `scripts/check/mutate-rule.ts` | 检查 | — | 上面那条的**判定**：一次运行算「抓到」「红错了地方」「跑不起来」还是「存活」—— 只有断言红了才算抓到（崩溃不是断言的功劳），而点了名的还要**名单里每一条都红**（有一条没红，这条变异对那一条就什么也没证明）。**见齐就停的那一次另走一条路**：主动停下的运行没有退出码、也没打出汇总，两道闸都会判它跑不起来 —— 而看见那几行不带记号的 `✗ <名字>` 本身就是「断言真的跑了并且红了」的直接证据，比「打出了汇总」这个代理更硬；汇总那道闸是给不点名的那一大批用的。记号照旧一票否决。**认得哪些验证者也在这里**：跑哪个脚本、它的失败汇总长什么样，两样同一处声明 —— 分开放的话，换一边不换另一边的症状是「它真的红了，却被判成跑不起来」。验证者还有一条硬约束由这里守着：**打完汇总不许立刻退出** —— 汇总是最后打的，输出接的是管道，硬退出会把它截掉（实测 stderr 积压 400 行时 40 次丢 18 次），而没有提前退出也就没有绕过汇总的路，「每条失败路径都经过同一句汇总」从此不必靠读代码相信。**`kills` 点的那些夹具在不在、各自是不是只有一条叫那个名字，判据也在这里 —— 名单里每一项各查一次**：按语法树扫验证者的源码建清册 —— 按字面扫分不出「真的在调用」与「串里、注释里写着一句长得像调用的话」，而那个方向是放行。登记谁算「起名」的门槛不是「有没有起过名」，而是**「这个名字有没有可能满足 `kills`」**：自检里跑一个脚本的那一族失败时只打带记号的那一句，点了也永远得不到「抓到」，所以不登记。**报告里那条豁免旁边说什么也是这里的判定**：只按编号**逐字相等**问「有没有一条变异记在它名下」，不判「豁免该不该撤」（后者要看 `scope`，豁免有、变异没有）；排版也留在判定里，与 `criteriaCell` 同一个理由 —— 原先写死在 `mutate.ts` 与 `audit.ts` **三处**模板串里，谁也够不着，而三份各写各的，一条判据有了负片之后改了两处忘了第三处，症状是「同一件事，两份报告说两样」 |
| `scripts/check/jobs-rule.ts` | 检查 | — | 变异跑的**派工**判定：派几个、一条结论怎么写成一行又怎么读回来、以及派出去却没回话的怎么算。并行的单位是**目录**不是进程 —— 一条变异是真的改写一份源文件，两条同时改同一棵树，验证者跑的就是「两处叠在一起」那棵树，而报告仍按条归因，每一条的绿或红都不算数。派法是**一条一派**不是先切好再发：指名了验证者的那几条跑的是另一个验证者，代价与缺省那个不可比（原型上量过，静态切成 6 份时两份比另外四份多跑 32 秒），而要靠静态切法摊平就得先知道每条要跑多久 —— 那个数没人量过，猜一个写进代码就是把「不知道」伪装成「知道」。**派出去却没有结论回来的算硬失败**：worker 崩了、被杀了、汇报行被截断，在输出上长得都一样，而把「没查过」当成「跑过了、没事」正是 `process/README.md` 总纲里那个头号事故。**起 worker 那两步的判定也在这里**（原先内联在入口，#110 挪出来）：一是**哪些文件进得了 worker 那份副本** —— `CONVENTIONS.md` 第 10 条把「跳过哪些目录、哪些文件算数」明写在判定那一层，因为它决定 worker 看得见什么，而错一格的后果不是跑得慢，是一份装着密钥的 `.env.local` 被复制进每个隔离目录（那两张表是 `.gitignore` 的手抄本，手抄本会漂，实测漂过一次，所以 `.env` 那一族按前缀认）；二是**`spawn` 交回来的对象起没起来** —— 起进程时资源不够时 Node 在装管道之前就返回，`stdin` 压根不存在，不先问这一句的话紧跟着那句同步的装监听器会先抛「读不到 undefined 上的属性」，闸门照样非零退出、目录照样收干净，错的是那句话把人指向派工代码，而该做的是调高允许打开的文件数。入口那一半（问得够不够早、硬来之后收没收干净）变异够不到，由自检那条压低 fd 的夹具守着 |
| `scripts/check/mutate-restore.ts` | 检查 | — | 变异跑到一半被打断时，把动过的那份源文件还回去：信号杀进来时 `finally` 和挂在退出上的清理都不跑，留在工作区里的是一处**故意违反某条需求的改动**，而下一次检查报的是「锚点失效」，指着变异集说话。挡不住硬杀与断电，差额明写在模块头上 |
| `scripts/check/tsx-cmd.ts` | 检查 | — | 起一个跑 `.ts` 的子进程用**哪条命令**，全仓只此一份：当前这个 node 加 `tsx/cli` 那个公开导出，不经 `npx`、不经 shell、也不指 `node_modules/.bin`。自检、需求测试、变异三处的**等法**各不相同（同步等两股 / 同步等一股 / 异步等还要自成一组），而「起哪个可执行文件、前面垫哪些参数」是**同一个决定** —— 三处原先各写各的、而且各错各的，只修其中一处就是又一次「只修实例」（ADR-69 第一块欠条，它自己写明修法是三处一起改） |
| `scripts/check/verifier-rule.ts` | 检查 | — | 一条变异改的是不是**验证者自己要用的东西**（`by` 指名了验证者时才管）。验证者够得到一个文件有三条路：`import`、当工具起、以及靠 `--import` 预加载 —— 只扫第一条会少收一大截，而偏小的那一头是放行。划开「工具」与「被测对象」的判据是：起 `scripts/check/` 底下的算工具，起别处的算被测对象 —— 后者正是本条记录的头号用例（自检真跑被变异的入口），收进闭包就把它自己挡在门外了。**抽边故意写得宽**（注释里提到的路径也收）：两头不对称，多收只是把闭包撑大、多拦几条，少收就是放行一条自己验自己的变异。自检那句失败汇总的文案也收在这里 —— 自检打、判定认，只此一份 |
| `scripts/check/why-rule.ts` | 检查 | — | 变异集 why 的判定：夹带实现原文当场拦下，让 `--brief` 名副其实 |
| `scripts/check/group-rule.ts` | 检查 | — | 自检**这一跑该跑哪几组**的判定：`--only` 怎么解析、依赖闭包怎么收。它决定这一跑看得见多少东西 —— 缩小一格的后果不是跑得慢，是一整节被静默跳过（那节被 `if (dir && rendered !== undefined)` 守着）而指着它的负片「跑完、没红」，所以是判定不是工具（`docs/CONVENTIONS.md` 第 10 条）。点了不存在的组**当场抛并说出是哪个名字**（只问「抛没抛」的断言拦不住它，M-H42-b 因此存活过一轮）。**空的 `--only=`（一组都不跑）与没写 `--only`（全跑）分成两种返回值**，判定只管把两者分开、不替调用方定性：入口对前者按红报（零组跑完打绿、退出 0，形状与一次合法子集全绿一模一样），后者才是整跑。⚠️ **归组那一半不在这里** —— 把夹具标签翻成组 id 的 `groupOfLabel` 在 `mutate-rule.ts`，它与清册那一侧各扫一遍源码、判定重复了一份，欠条记在 ADR-99 第九节 |
| `scripts/check/attribution-rule.ts` | 检查 | — | 变异与豁免**记在谁名下**、以及变异编号**唯一不唯一**的判定。记在一个不存在的编号名下时当场拦下 —— 那条变异照样跑、照样被抓到，却对任何一条需求都不算数；编号自己重复也当场拦下 —— 两条一样照跑照被抓到，而报告里两行顶着同一个名字，指不回表里哪一行。两种毛病同时在时**先报重复**，这个先后写在判定里不在入口（记错名下那份报告印的也是编号）。当初是 `mutate.ts` 与 `arch-sync.ts` 共用的，后者 2026-09-18 撤了（ADR-78），现在只剩 `mutate.ts` 一个调用方 |
| `scripts/check/selfcheck.ts` | 检查 | F5 | 每个可执行文件都要有出处：**采集管线那几个**在这里用假 fetch 从头跑到尾，**检查脚本**在 `npm run check` 里各自成一步（那条路不保证跑到尾 —— 检查可以合法提前退出），其余写进 `EXEMPT` 说明理由。跑完还写一份**入口认领**记录：这一次端到端真跑过的验收判据 —— 形状与判定都用 `claims.ts` 那一套，全绿且源码一路没动过才写得下 |
| `scripts/check/fake-fetch.ts` | 检查 | — | 自检用的假响应；它决定了自检能走到多深 |
| `scripts/check/claims.ts` | 检查 | — | 测试运行时留下的覆盖记录（跑过哪些需求、认领了哪些验收判据与交点）的路径与形状，以及关于它的五个判定：新鲜度（带整棵 `scripts/` 树的指纹，对不上就是过期记录）、形状（认不出就说认不出，不兜底成空数组；该验哪几栏列成一张名单，加字段时只改那一处）、归属（谁拥有这份记录，谁负责开跑前先清掉它）、写盘资格（干净、断言全过、而且源码一路没被动过的运行才写得下）、读不出来时是哪一种读不出来（还没跑过／记录坏了／这个路径读不了，说法不一样）。两份记录各有一个写方：单元认领由 `test.ts` 写、入口认领由 `selfcheck.ts` 写，`audit.ts` 两份都读，三方由此指向同一处。**不合成一份多一栏**：拥有的一方开跑前先清、跑完整份重写，两个写方共用一份会互相抹掉，而审计读到的是一份形状合法、内容少了一半的记录 —— 理由记在那个常量的注释里 |
| `scripts/check/audit.ts` | 检查 | — | 链路审计，回答完成度而不是「功能做完了没有」；计量单位是**验收判据**不是需求，红线的判据**两种认领一个都没有**（单元断言没认、自检也没端到端跑过）就硬失败；**只由自检认领的判据**没有一条 `by: "selfcheck"` 的负片也硬失败（与红线无关）；**检查链自己的判定模块**也在它的名单上，没有变异守着就硬失败 |
| `scripts/check/audit-rule.ts` | 检查 | — | 审计里三半**判定**。① 检查链自己算不算数：哪些文件是判定模块（scripts/check/ 下不带 shebang 的，不按文件名认）、哪些没有变异守着。② 计量输入怎么分：现行的参与计量、作废的只参与展示，两半一起给（只过滤不展示，作废的需求就从审计里彻底消失）。③ 报告上那几个数怎么数、哪些变异算数：哪些变异是判据级的、其中哪些是**改跑自检**的（后者是落地 4 那条硬失败问的集合，与前者分开两个函数 —— 一个回答「报告数几条」，一个回答「硬失败该不该响」）、验收判据那一行里测试认领／入口认领／显式豁免**三栏互不重叠**（两边都认领的算在测试那一栏、只有自检认领的才进入口那一栏、豁免只数两边都没认领的 ——各数各的会把同一条数两遍，红线那三栏加出比总数还大的值，而逐条那一头早把它算成认领了）。抽出来都是为了能被测 —— 它自己也在①那张名单上 |
| `scripts/check/debt-rule.ts` | 检查 | — | 欠条台账的**判定**：把 `docs/adr/` 里三套「什么时候回来」的写法（`重启条件` 加冒号、`## 什么条件下重开这个讨论` 那一节、`## 死亡条件` 那一节与 `**什么条件下……**` 加冒号）抽成一份清单，以及**一道硬失败**——⚠️ 欠条块里没写重启条件当场红。**只抽取，不判断**：不判哪条已还，也不判这次改动踩到了谁 —— 读懂一条条件是会随模型变强的那一侧，一张手写的正则表不是，撤掉匹配那一半的账记在 ADR-86。两处偏向都朝同一边：第一套写法的后缀是一张**闭集**，块内与块头都先过 `quoted.ts` 的引文遮罩 —— 一条叙述或一段围栏示例顶上去，那个块就被判成「写了条件」，于是真孤儿报不出来，而漏报正是这道闸门放行的意思 |
| `scripts/test.ts` | 检查 | — | 需求测试的实现。**覆盖了哪些编号以 `npm run audit` 为准**，不在这里复述 |

<!-- END:ANCHORS -->

---

## 顺序契约

**这张表是这份文档存在的理由。** 下面每一条的共同点：每个函数单独都对，
错的是它们被组合起来的方式 —— 这类 bug 不在任何一个单元里，所以任何单元测试都看不到。

每行必须写出**守它的变异**。变异要真实存在于 `scripts/check/mutations.json`、
且指向该行的位置 —— **从 ADR-78 起没有检查验这一条了**，靠写的人自己核。
没有变异守着的顺序声明，只是散文；而现在「有没有变异守着」这句话本身也只是散文。

<!-- BEGIN:ORDER 每行写出守它的变异，人维护，无机器校验（ADR-78） -->

| 顺序契约 | 位置 | 错了会怎样 | 守它的变异 |
|---|---|---|---|
| 同人识别 → 合并 → 粉丝闸门 → 记忆过滤 | `scripts/lib/pipeline.ts` | 闸门跑在合并之前，「TikTok 3000 + IG 3000、合起来够线」的人被提前丢掉；记忆过滤跑在闸门之前，`filtered_contacted` 把连闸门都过不了的人也算进去，向用户虚报打扰规模 | M-P1-g M-P4-b |
| 保留原任务下标 → 再筛选可展示的标签 | `scripts/lib/pipeline.ts` | 剩余任务按新位置重新编号，用户不能把提示指回原任务 | M-U8-a M-U8-b M-U8-o |
| 实际返回账号时记录来源 → 合页/同人合并保留 → probe 与交付展示 | `scripts/providers/tikhub.ts`、`scripts/lib/pipeline.ts`、`scripts/lib/identity.ts`、`scripts/probe.ts`、`scripts/lib/rows.ts` | 配置替代真实来源、首次或后页观察被吞、关联账号来源丢失，或保存后到交付时消失 | M-D15-a M-D15-b M-D15-d M-D15-e M-D15-f M-D15-g M-D15-i |
| `finalize` 不得就地修改传入的累加器 | `scripts/lib/pipeline.ts` | 「累加器只增不减」退回成依赖调用方记得先落盘 —— ADR-08 那个数据丢失 bug 的形状 | M-D6-c |
| 算分 → 分层 → 地域降级 → 风险降级 → 排序 | `scripts/lib/pipeline.ts` | 降级跑在 `tierOf` 之前会被重新计算的 tier 覆盖，地域不达标或高风险的人照样留在 A 级被直接发信；排序跑在降级之前，A 区里混着已经掉到 B 的人 | M-F5-a M-F8-a M-U1-b |
| 记忆过滤只排除**别的任务**推荐过的人 | `scripts/lib/memory.ts` | render 写回记忆之后再 `--resume`，本任务已付费采集的人全被判成「已推荐过」，产出一份空名单 | M-D6-b |
| 补没抓过的第一页 → 才轮到达标判断停下 | `scripts/collect.ts`（「哪些还欠着第一页」的判定在 `scripts/lib/pipeline.ts`，两边共用） | 达标判断跑在每一次请求之前时，前面几个词各抓一页就填满目标，排在后面的关键词×平台一个请求都不发 —— 而报告里「查了没人」和「一次都没查」长得一模一样；反过来把它放宽成无视达标一直翻，多翻出来的页花的是用户没打算花的钱 | M-F9-a M-F9-d |
| 「这个任务问过第一页了」这条记录，只在响应真的到手之后写 | `scripts/collect.ts` | 预算恰好在这个词上用尽时，断点里写着它问过、而它一条数据都没拿到：续跑不会再碰它，它的第一页就永远丢了，中止时那句话还把它从「还没问到的」里少报一个 | M-F9-b |
| 保存任务预留 → 发请求；保存真实终态 → 读正文或重试 | `scripts/lib/budget.ts`、`scripts/providers/tikhub.ts` | 请求先发会留下费用丢失窗口；保存失败后继续读正文或请求，会把本地错误当成供应商重试条件 | M-D14-a M-D14-b M-D14-c M-D14-d M-D14-e |
| 恢复未结项先拒绝付费与改额；保存失败先锁定本运行 | `scripts/lib/budget.ts`、`scripts/collect.ts`、`scripts/enrich.ts` | 丢掉未结项会重新释放可花额度；费用失败若进入常规收尾，会被成功输出或后续记忆错误盖掉 | M-D14-f M-D14-h M-D14-i M-D14-j |
| 费用检查点保留盘上业务状态；完成页或账号后再保存业务结果 | `scripts/lib/task.ts`、`scripts/collect.ts`、`scripts/enrich.ts` | 提前保存去重声明会替尚未写入的名单背书；只保存费用会丢掉已完成页与账号的业务进度 | M-D14-g M-P3-f M-D6-o |
| 作者累加器落盘 → 才推进同一页的分页进度 | `scripts/collect.ts`（`persist()`） | 反过来的话，两次写之间被打断或累加器写失败时，盘上的分页进度已经前进而这一页的作者没存下：TikTok 续跑跳过这一页，IG 丢掉这一页、续跑也不再为这个任务请求（令牌不跨运行，D6.v）（D6.t，ADR-113） | M-D6-ac |
| 硬来收尾：**收掉各 worker 底下的验证者那一组 → 才删隔离目录** | `scripts/check/jobs-rule.ts` | 反过来的话，刀落下之前那几组还能往一棵正在被删的树里写，盘上重新长出半成品，而「收干净了」那句已经说出口 —— 静默；丢掉按组那一半就回到「硬杀必漏一批跑着被改过源码的进程」 | M-H40-g M-H40-h |

<!-- END:ORDER -->

---

## 缝隙契约：Agent ↔ scripts

四个入口都是**一次性进程**，彼此之间只通过磁盘上的文件交接。
Agent 是编排者，它读 stdout 做决策。

| 入口 | 读 | 写 | 退出码 |
|---|---|---|---|
| `probe` | `--config probe.json` | **不落盘** | 0 · 1 · 2 |
| `collect` | `--config task.json` 或 `--resume <dir>` `[--ignore-memory]` | `task.json` `creators.raw.json` `creators.json` | 0 · 1 · 2 · **3** |
| `enrich` | `--dir <dir>`（`task.json` `creators.json`） | `enrichment.json` `task.json` | 0 · 1 · 2 · **3** |
| `render` | `--dir <dir>`（`task.json` `creators.json` `enrichment.json`） | `creators.json` `kol.csv` `kol.xlsx` `meta.json` `report.html` · **`memory/creators.json`** | 0 · 2 |

**stdout 是结构化 JSON，stderr 是进度。** 这个分工是硬约束 —— Agent 解析 stdout，
往里混进度信息会让解析在最需要它的时候（长任务、预算告急）失败。

`probe` 的 `sample[].top_post` 供 Agent 判断方向：作品字段缺席或空数组写 `（未查询）`，
有作品则给第一条文案的前 120 字符，空串照留（P1.i）。它不是最高播放量，也不是主页近期样本；
空串不能证明作者没写文案，适配层目前也会把未取到的文案归为空串（ADR-103）。

搜索任务身份（U8、ADR-106）：`meta.keywords[]` 与 `probe.results[]` 的成功/普通错误行
由脚本写入原 `tasks` 零起始 `task_index`，Agent 只读。人读序号为下标加一；筛选不得重编号。
`pending_keywords`、续跑判定用的关键词列表、probe 进度和 collect 完成/达上限提示共用任务标签，
包含原序号、维度、平台、原词；续跑成本提示仍只使用列表长度报告数量。HTML 使用行的下标；旧行缺失或非法下标显示「无从确认」。
`as_hashtag` 保留为配置元数据，当前不切换 TikHub 搜索端点，不能作已执行路径声明。
IG 路线只由任务的 `ig_route` 决定：缺席走 Reels；`"hashtag"` 走话题搜索，在 `TikHub.search()` 最前面分派，probe 与 collect 共用。
probe 的成功与普通错误行照配置带出 `ig_route`：写了才有这个键，不补成 Reels，也不由 `as_hashtag` 推断（D15.l、D15.m）。
`ig_route` 不合规时 collect（新建与续跑）与 probe 在建目录、预留与请求之前以退出码 2 结束（D15.j）。
查询状态、计数及任务列表重排的既有边界不变。

退出码按可行动的原因区分：

| | 含义 | Agent 该做什么 |
|---|---|---|
| `0` | 正常；probe 预算不足也保留部分输出 | 读 stdout 与停止提示 |
| `2` | 输入/初始费用状态、实际请求缺 key，或记忆读不出来 | 报具体原因，不能仅建议加钱 |
| `3` | collect/enrich 已知额度不足且断点保存成功 | 用户确认新总上限后续跑，或用已有数据收尾 |
| `1` | 运行中费用内部错误、保存失败或其他失败 | 读 stderr；不得声称断点已保存 |

恢复 pending 拒绝付费/改额为 2，运行中意外 pending/receipt 错为 1。直接分页探针非 200 仍中止，不输出成功结果 JSON。
零请求 collect、缓存/私密 enrich 与 render 不要求 key 或可付费账；坏的整份 JSON 仍无法读取。

collect/enrich 的任务费用先保存预留才发请求，结算保存成功后才读正文或进入下一尝试（D14、ADR-109）。费用检查点只更新盘上 task 的账目与次数，不能提前保存内存业务进度或去重声明。检查点失败锁定本运行并退出 1；尽力保存成功也不产出常规成功提示或被记忆错误改成 2。process 探针不接此回调。强杀后 pending 仍占额度；终态已存但业务响应未存时仍可能重查，不承诺断电或多个写者安全。

续跑提示分别计算待查关键词与待补 profile；两者皆零才声明无新请求。有待查项但费用不可用时同时报告阻止原因，不能承诺续跑即花钱（D6.q/r）。记忆读不出来仍按 P4 停止产出，只有用户显式 `--ignore-memory` 才跳过，并保留未去重声明。

### 字段所有权

`creators.json` 由两边轮流写，**谁写哪些字段是固定的**：

| 写者 | 字段 |
|---|---|
| `collect`（脚本） | 采集与 profile 的全部原始字段、`source_keyword`、`source_dimension`、`discovery_sources`（仅搜索适配写入） |
| **Agent** | `fit` · `fit_reason` · `outreach_draft` |
| `enrich`（脚本） | 只写 `enrichment.json`，**不碰 `creators.json`** |
| `render`（脚本） | `score` · `tier` · `tier_adjustments` · `account_assessment` |

越界写别人的字段，下一次跑会把它覆盖掉，而且不会有任何报错。


**搜索作品的身份与合并归脚本管理（D11、ADR-105）。** 适配器只从对应作品字段生成
可选 `RecentPost.id`，使用 `平台:原始id`；外部标识缺失就不生成键，Agent 不补写它。
采集首次收页与后续合页都经 `scripts/lib/posts.ts`，同人合并也复用这一契约：
已有记录在先、后页在后；同人合并则主记录在先、关联记录在后，主记录选择规则不变。
同平台同 id 保留第一次记录及其字段，后到的新作品按原顺序追加，缺 id 的作品逐条保留，
因此旧任务的条目数不保证等于不同作品数。合并不修改输入；两侧都无非空作品时保持未查询。
这只汇集关键词命中的证据，不替代 `enrich` 的独立主页样本，也不改变评分或同人识别规则。

### 两个 creators 文件

实际发现来源归脚本管理（D15、ADR-110）：适配器将返回账号的实际端点与原任务词/维度写入 `discovery_sources`，合页及同人合并稳定取并集；旧缺席不妨碍追加新观察。它不同于完整归属用的 `source_tasks`，不从后者倒推，也不改变后者的未知规则。来源在现有业务保存点落盘；profile、费用检查点及 Agent 不补写它。probe 透传集合；表格与 HTML 使用纯格式化投影，HTML 在输出处转义。集合只代表已记录的账号发现来源，不保证完整历史，不对应某条作品或请求费用次数。

| 文件 | 是什么 | 规则 |
|---|---|---|
| `creators.raw.json` | 采集**累加器** | **只增不减**（D6）。`--resume` 的输入是它 |
| `creators.json` | 交付物 | 累加器的过滤视图。过滤只作用于它 |

早先两者共用一个文件：collect 结尾把过滤后的名单写回 `creators.json`，
而 `--resume` 又拿它当输入 —— render 写回记忆之后再续跑，全员被判成「已推荐过」，
已付费采集的数据被清成空数组。见 ADR-08。

---

## 三态在类型层的落点

总纲那句「任何会产出结论的东西都必须能说『我没有资格回答』」，在代码里长这样：

| 形态 | 含义 |
|---|---|
| `undefined` | **没查过** |
| `null` | 查过，确实没有 |
| 有值 | 查过，就是它 |
| `recent_posts` 的空数组 | **例外：读作没查过**，与缺席同一态。今天没有生产者，全是盘上旧数据里 IG 那条兜底路径凭空写的（ADR-102） |
| `Measurement<T>` | `measured`（带 source / observed_at / sample_size / basis）· `unavailable`（带 reason）· **字段整个缺席 = 未查询** |
| `meta.json` 的 `capabilities` | 每种能力分别统计 `measured` / `unavailable` / `unqueried`，三个数不合并 |

这三档为什么不能压成两档、以及压掉之后会发生什么，在 `docs/CONVENTIONS.md`
第 1、6 条 —— **不在这里复述**。这一页只说它落在哪：`scripts/lib/types.ts`。

---

## 结构上尚未确定的

按 `process/5-DESIGN.md`，边界目前是猜的地方必须显式写出来，不许留给下一个人自己发现。

| 问题 | 现在是什么状态 |
|---|---|
| **数据源契约只被一家实现过** | `skill/references/providers/_interface.md` 定义了适配契约，但只有 TikHub 一个实现。抽象对不对，要等第二家接进来才知道 —— 现在说「换个供应商只要写个适配器」是**「应该可以」，不是结论** |
| **`enrich` 与 `render` 的关联键** | 靠 `platform:handle` 字符串关联（`accountKey`）。跨平台合并后的主账号与关联账号各自查一次，这个关系没有类型保证，只有约定 |
| **`skill/` 与 `scripts/` 的边界在语义筛选那一段是模糊的** | 粉丝闸门、评分权重在脚本里，语义判断在 Agent 那边，但「竞品词 +15 是否给高了」这类问题横跨两边（见 `docs/SPEC.md` 尚未确定的一节）。调整它需要同时动两边，目前没有任何检查会提醒这件事 |

### 费用账与付费接线的边界（D12–D14、ADR-107/108/109）

金额模型不依赖供应商、文件或网络；报价提供固定版本表，budget 包装持有与 task 保存共享的同一 state。新建与恢复分开，旧费用缺失不能产生零账；原 ledger 可独立核验，根预算冲突的整体输出诊断不阻止合规的显式改额。

原预算 token → 精确金额 → 本端点预留 → 保存任务 pending → fetch → 按 HTTP 状态结算 → 保存任务终态 → 正文解析，先后不可交换：解析失败不能改变已知 HTTP 的费用类别。retry/fallback 各走一遍，费用错误不触发后继请求。费用检查点只保存 ledger/requests，业务仍由原保存点处理；显式改额先保存两处一致上限，再请求。process 探针不保存任务。接线由独立进程假网络与 M-D14-a…j 验收，业务保存另由 M-P3-f、M-D6-o 守住。

费用投影的唯一字段契约见 ADR-108；各入口及 render 共用，金额文本只在 JSON 出口转精确 number。render 不创建或修复费用账；保留原账的本地写回不把未知金额变零。

请求前持久预留按 ADR-109 的目标契约实现，必须另用强制中断与保存故障验证，不能由纯模型测试推定通过。保留边界是断电、多写者以及未保存的业务响应；费用终态已存不证明业务进度已存。
