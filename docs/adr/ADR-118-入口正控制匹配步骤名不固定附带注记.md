# ADR-118 入口正控制匹配步骤名不固定附带注记

- 日期：2026-09-26 · 独立自检入口补证的首次正常运行之后发现
- 类型：实现方便（测试表达纠正，按 CONVENTIONS 第八节举证）
- 触发事实：合法入口退出 0、固定完成行完整，但初稿把已声明步骤名写成整行相等，附带状态注记使正常对照假红
- 冲击的需求：harness 的既有自检选择入口契约；产品需求登记表不变
- 结论：采纳
- 理由：事前依据是公开步骤名与固定完成行，没有要求步骤名之后不得有注记；初稿额外编码了这一限制
- 连带改动：`scripts/test.ts` 的测试表达；生产入口、参数规则和运行判定不改

修正保留步骤名严格边界：整行步骤名，或步骤名后接完整括号注记；注记内容不作为 expected，不接受任意后缀。退出 0、完整完成标记、无进程故障、认领字节不变和临时资源回收的约束均不放宽。

独立作者只接收声明边界，没有接收运行日志、入口函数体或变异 find/replace。修订后的未提交草稿已在正常入口上验证：合法对照与未知参数拒绝两条断言通过。初次假红不作为抓到需求违反的证据，正常通过也不代表接线退化已经被证明会遭拒绝。

本记录写于首次失败及修订正常运行之后，不是此前已经完成的决策记录。

> ⚠️ 欠条：入口补证草稿尚未完成全套验收 · 重启条件：入口补证代码 PR 提交前，在完整变异集合中以独立需求验证路由确认正常基线通过、接线退化由事先指定断言拒绝；崩溃、超时及夹具故障不计功，并保持认领、资源清理和自我验证隔离。

> ⚠️ 草稿全套验收已完成（代码提交 `3528913cc42d0fdbdbb7026ed460caeee9959985`）：修订后的步骤名边界在正常入口和完整变异集合中通过；M-H42-j 由事先指定的拒绝断言抓到，正常正控制保持通过。实际故障注入证据及其当前代码一致性已核对，崩溃、超时、夹具故障不能算该断言功劳。认领恢复、独占资源清理及自我验证隔离保持。此前首次假红与事后修订的先后仍按本记录原文读取。
