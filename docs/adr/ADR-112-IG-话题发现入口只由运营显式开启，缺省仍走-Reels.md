# ADR-112 IG 话题发现入口只由运营显式开启，缺省仍走 Reels

- 日期 · 2026-09-24 · 交接清单「IG 可选话题发现入口」
- 类型：**产品取舍**（需求所有者裁决）；实现与需求改动另开 PR
- 触发它的事实：`/api/v1/instagram/v2/fetch_hashtag_posts` 只做过实验采样，未接入生产（`docs/data-source-strategy.md`）；
  历史样本摘要里话题页给出了另两路没有的候选，但样本原件不在仓库、跨词与长期交集都没量过
- 冲击的需求：D15.a/b/h（来源要能记话题端点）、D11（话题作品 id 的取值字段）、D12.c（价目要登记这个端点）、D6（话题任务一页一次请求、没有兜底）
- 结论：**采纳，按下面的裁决实现；上线有两个前提（第三节），未满足前不接入口**

---

## 一、需求所有者的裁决（2026-09-24）

- **只由运营显式开启**：任务配置里给某个 Instagram 任务写 `ig_route: "hashtag"`。Agent 不自行添加；
  缺省（字段缺席）仍走 Reels，不改变任何品类的默认发现路径。

## 二、已由现有需求或裁决定死的

- **不复用 `as_hashtag`**：它是配置元数据，不切换端点、不改标签（D15.a、U8.k、ADR-106）。
- **请求**：`keyword` 取任务关键词去掉一个开头的 `#`，`feed_type=top`，首页；不走账号名兜底。
  翻页等 ADR-111 的实现合入、且话题端点的令牌位置有样本后另议。
- **校验**：`ig_route` 只认 `"hashtag"`、只能写在 Instagram 任务上；去掉开头 `#` 后为空或含空白的关键词拒绝。
  这些都在任何预留与请求之前以退出码 2 结束（零请求）。
- **原词保留**：`discovery_sources.keyword`、`source_keyword`、任务标签和关键词行都用任务关键词原样，只有请求参数去掉 `#`。
- **不进任务标签**：同词任务靠任务序号区分（U8.a）；每个账号的实际路线由 `discovery_sources` 的端点表达。
  要在标签或关键词表上标路线，须另改 U8。
- **分派位置**：在 `TikHub.search()` 最前面按 `ig_route` 分派，probe 与 collect 共用 —— 否则会试探用 Reels、采集用话题（F3）。
- **图文**：作为内容匹配证据进入搜索命中作品（文案）。**播放数只给通过现有视频判定的条目**（与主页样本共用同一个判定函数，
  不另写一份）；其余条目不写播放数 —— 该媒体类型的播放字段没有确认过语义，不能当成真实的 0。
  主页绩效样本口径不变（D8）。
- **评分**：搜索命中的播放数会参与现有的 +5 与展示作品排序；评分规则本身不变（D11.p）。
  两条路线命中同一作品 id 时按 D11 先到者保留，播放数可能取决于任务顺序 —— 实现时补跨路线同 id 的测试。

## 三、上线前提（未满足前不接入口）

1. **价目**：固定价目版本 `tikhub-public-20260720-5d52fe8fb109` 只登记了八条生产端点，D12.c 对未登记端点报错。
   用户本地读取定价资产，**SHA256 等于 `5d52fe8fb109a569e4e16b39b611ee9d5233c9131d264f1e856a987f23cc8cbf` 时**，
   才把 `fetch_hashtag_posts` 那一行原样转录进同一版本，并在 ADR-107 末尾追加观察时刻。
   哈希不等或资产取不到：停下，交需求所有者决定是否另开价目版本（另开会让所有端点的新请求改用新版本）。
   不用固定规范 description 里的 $0.002 代替价目。
2. **字段**：用户在本地样本上核对话题响应的列表路径、作品 `id`、`user.username`、`caption_text`、视频标记字段。
   核完才写解析器；没有样本证据的字段不写成判据。

## 四、施工顺序（每步一条 PR）

1. 价目：按第三节第 1 条转录一行，配价目解析的测试；ADR-107 追加。
2. 解析器与来源类型：话题解析、来源端点与显示名扩一项、共用的视频判定；不分派、没有入口。
3. 入口（最后一块砖）：配置校验与 `search()` 分派；同一条 PR 改 D15.a/b/h、D11、D6，按 `docs/SYNC.md` 同步
   `skill/references/providers/tikhub.md`、`_interface.md`、`skill/SKILL.md`、`skill/references/keyword-strategy.md`、
   `docs/ARCHITECTURE.md` 缝隙契约，以及 `tikhub.md` 里「实验 hashtag 不成为生产路径」那句。

## 五、2026-09-24 两个上线前提的核对结果

用户在本机运行只读命令（不发数据请求、不用 key），输出贴回会话：

1. **价目**：本地读取的定价资产 SHA256 为 `5d52fe8fb109a569e4e16b39b611ee9d5233c9131d264f1e856a987f23cc8cbf`，
   与价目版本 `tikhub-public-20260720-5d52fe8fb109` 记录的一致。该资产中的原始元组为
   `["/api/v1/instagram/v2/fetch_hashtag_posts",0.002,0,0,"10/second"]` —— 单价 0.002 USD（2000 微美元），
   阶梯标记 0、免费额度标记 0。第三节第 1 条满足，可在同一版本内转录这一行。
2. **字段**：在用户本地重采样本（`sample-lInRuK`，另一组观测，只用于字段存在性，不用于任何计数结论）的
   4 份话题响应上：列表在 `data.data.items`（各 23–24 条）；首条 `id` 为字符串、有 `user.username`；
   文案字段是 `caption_text`（Reels 与 general 是 `caption.text`）；全部条目的 `media_type` 取值为 1、2、8，
   `is_video` 真假都有，`product_type` 为 `feed`、`clips`、`carousel_container` —— 图文与轮播确实混在话题页里；
   三条路线的响应都有与 `data.data` 同级的 `data.pagination_token`。
   只核了每份的首条的作者与文案字段，逐条覆盖率未核：解析器对缺席字段保持缺席，不推算。

## 六、实现追记（第四节三步落地）

- 第 1 步价目随 #167、第 2 步解析器随 #169、第 3 步入口随本条 PR 合入。第三节两个上线前提都已满足（第五节）。
- 需求：新增 D15.j（配置校验）、D15.k（分派与原词保留）、D6.w（只取首页、不兜底），改写 D15.a 与 D11.h。
  第四节第 3 步点名的 D15.h 没改：它要求展示「路线」而没有列出路线名，话题页的显示名已随 #169 加上。
- 实现：校验与请求词在 `scripts/lib/ig-route.ts`，collect（新建与续跑）与 probe 都在任何请求之前调用；
  `TikHub.search()` 最前面分派。话题页不交回续页令牌，入口按 D6.u 在拿回首页后把任务记进 `done` ——
  翻页仍按第二节「另议」，本条没有开。
- 独立审阅指出两处，同一条 PR 里改了：话题任务收尾原先说「本次没有可继续的续页令牌」，而话题页的响应其实带着令牌，
  改为「话题路线只取首页」并写进 D6.w；`tikhub.md`、`data-source-strategy.md` 几处「八条生产端点」改称「八条缺省生产端点」，
  话题端点另列。
- 同一次审阅另有三条当时没修，登记如下；第三条在 PR 评审第二轮被再次提出，随本条 PR 还上了：

  > ⚠️ 欠条：probe 的配置报错（含 `ig_route` 不合规）在 stderr 带着 `BudgetInputError` 的类名与堆栈，和预算无关 ·
  > 重启条件：下一条改动碰到 probe 的报错出口

  > ⚠️ 欠条：probe 的结果行输出 `as_hashtag`、不输出 `ig_route`，话题任务 0 人时只看 stdout 分不出走的哪条路线 ·
  > 重启条件：下一条改动碰到 probe 结果行的字段（U8、D15.g）

  > ⚠️ 这张欠条已还：原文是「续跑带 `--budget` 的那条路径会在调度前落盘，没有专门的夹具证明路线校验排在它前面」。
  > 自检 `hashtag-route` 组补了续跑带 `--budget 3` 的夹具：退出码 2、零请求、不留预留、盘上 task.json 原样不动。
  > 变异 M-D15-w 只在这条路径上跳过路线校验，补夹具之前它存活，补上之后只红这两条新断言。

## 七、2026-09-24 追记：第六节第一张欠条还上（F3.c、F3.d、F3.e）

需求所有者要求把第六节登记的欠条逐条还掉，这是第一张。

- 需求：probe 因输入问题以退出码 2 结束时的 stderr，分三条（三者会被不同的代码路径各自弄坏，process/1-REQUIREMENTS.md「会 → 拆成两条」）：
  - F3.c：配置里的问题（配置读不出或不合规、初始费用不可用、路线不合规）不带内部异常的类名与调用堆栈；
  - F3.d：同样这些问题写出配置路径与问题本身；
  - F3.e：请求时缺 API key 只写缺 key 这件事，不带类名与堆栈。
  退出码划分沿用 ADR-108，没有改。
- 实现：`scripts/probe.ts` 末尾的 catch 对 `BudgetInputError` 只打消息。配置里的问题在 `main()` 开头那个 try 里抛出，
  包装时带上配置路径；缺 API key 是请求时由 TikHub 抛的，消息里没有配置路径，也用不着。
  运行中的内部错误（退出码 1）照旧整条打出来，保留排查用的堆栈。
- 验证：独立上下文先于实现写了三类自检断言（配置读不出、`budget_usd` 不合法、`ig_route` 不合规），对着旧实现全红。
  开 PR 前的独立审阅又补了两类（初始费用不可用、缺 API key）、收紧了「问题本身」那半条的判据；
  这几处写在实现之后，红由变异重演：M-F3-a 退回整条打异常（F3.c 那几条红），M-F3-b 去掉配置路径、M-F3-c 只剩配置路径
  （F3.d 那几条红），M-F3-d 只收拾带配置路径前缀的报错（只有 F3.e 那一条红）。
  PR 评审（CodeRabbit）指出原先一条 F3.c 里三件能各自坏的事共用一份认领，按上面那条规矩拆成了三条。

> ⚠️ 这张欠条已还（第六节第一张，probe 的配置报错带类名与堆栈）：见上。

同一次审阅查出一处既有缺陷，不在本条修：

> ⚠️ 欠条：probe 与 collect 都不校验 `tasks` 的形状 —— 缺 `tasks`、`tasks` 不是数组、元素为 `null` 时 probe 以退出码 1 结束并带
> `TypeError` 与堆栈（ADR-108 说配置问题退出 2）；元素是空对象时 probe 退出 0，还按 Reels 发出一次不带关键词的付费请求 ·
> 重启条件：本条合入后另开一条 PR，先补需求（校验 `SearchTask` 已有的必填字段，不补默认值），collect 与 probe 共用一处校验

## 八、2026-09-24 追记：第六节第二张欠条还上（D15.l、D15.m）

需求所有者要求把第六节登记的欠条逐条还掉，这是第二张：probe 的结果行输出 `as_hashtag`、不输出 `ig_route`，
话题任务 0 人时只看 stdout 分不出走的哪条路线。

- 需求：成功结果与错误结果是两处各自拼出来的，会被各自弄坏，按 process/1-REQUIREMENTS.md「会 → 拆成两条」分开，
  与 U8.f／U8.g 同一个拆法：
  - D15.l：成功结果带出该任务配置里的 `ig_route`；写了就原样带出，没写就不带这个字段，
    不由 `as_hashtag`、关键词写法、平台或返回结果推断补写；
  - D15.m：错误结果同样带出。
  「没写就不带」不补成 `"reels"`：那是配置里没有的值（P1），缺席走 Reels 已由 D15.k 写明。
- 实现：`scripts/probe.ts` 的 `routeOf`，两处结果行共用。
- 验证：
  - 独立上下文先于实现写了自检 `hashtag-route` 组成功与错误两侧的断言：对着旧实现红的是写了 hashtag 的那几条；
    「没有这个键」那几条在旧实现上本来就成立，红由变异重演。
  - 开 PR 前的审阅指出三处缺口，补的断言与改的夹具都写在实现之后，红由变异重演：
    错误结果那一侧加一跑 TikTok 任务出错；成功那一跑加一个 as_hashtag 与 # 两样都占的诱饵；
    两跑错误夹具里垫着的成功任务改成与出错任务路线相反，否则错误行拿别的任务的路线也会假绿。
    断言与认领留在夹具里，不抄进这里（`npm run selfcheck -- --only=hashtag-route`）。
  - 变异 `M-D15-x`…`z`、`M-D15-aa`…`ae` 逐条打进代码，点名的都红（`npm run mutate` 跑的是全部变异）。
  - D15.l 列的推断来源里，成功行上有专门负片的只有 `as_hashtag`（`M-D15-aa`，与 # 两样合起来是 `M-D15-ae`）；
    关键词以 # 开头、TikTok 任务由 `M-D15-z`（没写路线也写出一个空值）守着，返回结果（0 人那一行）由 `M-D15-x`
    （成功行整个不带路线）守着，这两条都不是专门模拟那种推断的写法。错误行上，按平台补写有专门的 `M-D15-ac`，
    拿别的任务的路线有 `M-D15-ad`。每条断言都已由至少一条变异证明会红。
  - 实现给错误行加字段时弄失效了既有变异 `M-U8-g` 的锚点（审阅查出），锚点改成那一行开头的前缀，含义不变。

> ⚠️ 这张欠条已还（第六节第二张，probe 的结果行不输出 `ig_route`）：见上。
