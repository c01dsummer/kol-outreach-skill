# 决策记录

> 一条记录，一个不可回收的编号。**追加，不删改。**
>
> 编号 `ADR-NN`。数据需求用 `D` 前缀（`D1`、`D2`…），两套前缀不撞号。
>
> 驳回尤其要记 —— 同一个提议会反复出现，尤其当实现者是每次从零开始的 AI。
> **一条驳回记录的价值，和它被引用的次数成正比。**

---

## 一条记录 = 一个文件

文件名 `ADR-<编号>-<标题>.md`，第一行是 `# ADR-<编号> <标题>`。
两处编号必须一致，`npm run adr` 会验；下面的索引由它生成，**勿手改**。

装在一个文件里的时候，ADR 是这个仓库最大的冲突面 —— 它在语义上追加、
彼此独立，本该是最不该冲突的东西。三条在途分支各自把整册从 537 行写到
1978~2539 行，而且编号互相交错：第一条合进主干之后，后两条不是「追加到末尾」，
是往文件中间十几个位置插入。

换成文件之后，**并发抢号变成文件名撞车**，git 当场报冲突，而不是静默交错。
还有一个更要紧的后果：ADR 不依赖任何代码，于是它**可以先于实现单独合进主干** ——
一个改动里最大的那块因此不必再挤在代码的 PR 里。

**编号允许有空号。** 号一旦用过就不复用，包括那些活在别的分支上、还没合进来的。

写什么、怎么分类，见 `process/2-CHANGE.md`。

---

## 索引

<!-- BEGIN:GENERATED 由 npm run adr 生成，勿手改 -->

| 编号 | 结论 |
|---|---|
| **ADR-01** | [P2 拆成可执行的一半和只能靠自觉的一半](ADR-01-P2-拆成可执行的一半和只能靠自觉的一半.md) |
| **ADR-02** | [变异存活时先分清是测试假，还是变异等价](ADR-02-变异存活时先分清是测试假，还是变异等价.md) |
| **ADR-03** | [成本闸门不预置默认值，超限后允许追加预算续跑](ADR-03-成本闸门不预置默认值，超限后允许追加预算续跑.md) |
| **ADR-04** | [测试与实现由同一上下文写成，登记为已知缺口](ADR-04-测试与实现由同一上下文写成，登记为已知缺口.md) |
| **ADR-05** | [429 退避重试：文档承诺了但实现漏了](ADR-05-429-退避重试：文档承诺了但实现漏了.md) |
| **ADR-06** | [首次完整跑通 Skill 暴露的四个采集缺陷](ADR-06-首次完整跑通-Skill-暴露的四个采集缺陷.md) |
| **ADR-07** | [旧项目的代码值不值得参考：三处借鉴，四处反面教材](ADR-07-旧项目的代码值不值得参考：三处借鉴，四处反面教材.md) |
| **ADR-08** | [采集累加器与交付物拆成两个文件；记忆过滤按任务作用域](ADR-08-采集累加器与交付物拆成两个文件；记忆过滤按任务作用域.md) |
| **ADR-09** | [入口脚本的管线逻辑抽到 lib/pipeline.ts](ADR-09-入口脚本的管线逻辑抽到-libpipelinets.md) |
| **ADR-10** | [不接 DecodeCreator；公开信号只做风险复核](ADR-10-不接-DecodeCreator；公开信号只做风险复核.md) |
| **ADR-11** | [当前活跃度只做标签，不改 KOL 分层](ADR-11-当前活跃度只做标签，不改-KOL-分层.md) |
| **ADR-12** | [架构文档只锚定骨架，不由代码生成](ADR-12-架构文档只锚定骨架，不由代码生成.md) |
| **ADR-13** | [缓存命中永远按当前口径重算，不引版本号](ADR-13-缓存命中永远按当前口径重算，不引版本号.md) |
| **ADR-14** | [旧缓存的样本记录也收进窗口，与新抓样本同一形状](ADR-14-旧缓存的样本记录也收进窗口，与新抓样本同一形状.md) |
| **ADR-15** | [记忆读不出来时不产出名单；那一份记忆绝不被覆盖](ADR-15-记忆读不出来时不产出名单；那一份记忆绝不被覆盖.md) |
| **ADR-16** | [变更分类补上「需求冲突」与「产品取舍」，并写明哪些必须找人](ADR-16-变更分类补上需求冲突与产品取舍，并写明哪些必须找人.md) |
| **ADR-17** | [需求登记表的元数据要么派生要么删；交点在计量里有了位置](ADR-17-需求登记表的元数据要么派生要么删；交点在计量里有了位置.md) |
| **ADR-18** | [指纹要看得见嵌套字段；查不到的去重状态记为不知道](ADR-18-指纹要看得见嵌套字段；查不到的去重状态记为不知道.md) |
| **ADR-19** | [解析成功不等于形状对；写不进去不等于交付失败](ADR-19-解析成功不等于形状对；写不进去不等于交付失败.md) |
| **ADR-20** | [交点认领改成运行时收集；写回失败不再被说成文件坏了](ADR-20-交点认领改成运行时收集；写回失败不再被说成文件坏了.md) |
| **ADR-21** | [容器对了不等于里面对；生成物与源一致不等于两边装着同一批东西](ADR-21-容器对了不等于里面对；生成物与源一致不等于两边装着同一批东西.md) |
| **ADR-22** | [身份规范化两侧必须一致；错误信息不许替下一步打包票](ADR-22-身份规范化两侧必须一致；错误信息不许替下一步打包票.md) |
| **ADR-23** | [承诺改了代码没改文档；分类填成另一个合法分类，红线会静静少一条](ADR-23-承诺改了代码没改文档；分类填成另一个合法分类，红线会静静少一条.md) |
| **ADR-24** | [红线改动必须过独立复核；验收标准拆成可独立计量的判据](ADR-24-红线改动必须过独立复核；验收标准拆成可独立计量的判据.md) |
| **ADR-25** | [「有个冒号」不等于键能用；「关键词跑完了」不等于续跑不花钱](ADR-25-有个冒号不等于键能用；关键词跑完了不等于续跑不花钱.md) |
| **ADR-26** | [存在性判断吞掉了权限错误；审计自己的判定抽出来交给变异守](ADR-26-存在性判断吞掉了权限错误；审计自己的判定抽出来交给变异守.md) |
| **ADR-28** | [承诺的「条件」变了要重走全表；影响渲染的东西都得进指纹，包括顺序](ADR-28-承诺的条件变了要重走全表；影响渲染的东西都得进指纹，包括顺序.md) |
| **ADR-30** | [红线不许作废；\`updated_at\` 删掉；硬杀留下的临时文件下次清掉](ADR-30-红线不许作废；`updated_at`-删掉；硬杀留下的临时文件.md) |
| **ADR-31** | [代码注释也是副本；删字段和改承诺一样要走同步表](ADR-31-代码注释也是副本；删字段和改承诺一样要走同步表.md) |
| **ADR-32** | [记忆的键带空白 —— 同一间屋子的第四扇门](ADR-32-记忆的键带空白-——-同一间屋子的第四扇门.md) |
| **ADR-33** | [登记表只查了关系，没查形状](ADR-33-登记表只查了关系，没查形状.md) |
| **ADR-34** | [变异记在一个不存在的编号名下，全绿](ADR-34-变异记在一个不存在的编号名下，全绿.md) |
| **ADR-35** | [一个 PR 装了四件事 —— 拆成两个，并记下为什么会这样](ADR-35-一个-PR-装了四件事-——-拆成两个，并记下为什么会这样.md) |
| **ADR-36** | [同一个键出现两次 —— 解析已经把证据吃掉了](ADR-36-同一个键出现两次-——-解析已经把证据吃掉了.md) |
| **ADR-37** | [只堵了空白，剩下的字符还开着 —— 第六扇门](ADR-37-只堵了空白，剩下的字符还开着-——-第六扇门.md) |
| **ADR-38** | [名单和它的状态分两次落盘 —— 顺序反了就是静默破红线](ADR-38-名单和它的状态分两次落盘-——-顺序反了就是静默破红线.md) |
| **ADR-39** | [进程号还活着，不等于文件有主](ADR-39-进程号还活着，不等于文件有主.md) |
| **ADR-40** | [原子写换掉了权限位；空白该规范化还是该判损坏](ADR-40-原子写换掉了权限位；空白该规范化还是该判损坏.md) |
| **ADR-41** | [哪个顺序都不安全 —— 断言要最后写](ADR-41-哪个顺序都不安全-——-断言要最后写.md) |
| **ADR-42** | [权限收在写完之后，那段窗口里数据是敞开的](ADR-42-权限收在写完之后，那段窗口里数据是敞开的.md) |
| **ADR-43** | [给一个值加了第二个来源，措辞还在描述第一个](ADR-43-给一个值加了第二个来源，措辞还在描述第一个.md) |
| **ADR-44** | [第三次把「哪一步坏的」编给用户；以及年龄兜底的代价要写出来](ADR-44-第三次把哪一步坏的编给用户；以及年龄兜底的代价要写出来.md) |
| **ADR-45** | [三步协议建立在一个没被保证的前提上](ADR-45-三步协议建立在一个没被保证的前提上.md) |
| **ADR-46** | [写入侧能写出读取侧拒绝的东西；以及同一个 bug 我写了两遍](ADR-46-写入侧能写出读取侧拒绝的东西；以及同一个-bug-我写了两遍.md) |
| **ADR-47** | [落盘、并发、认不出的取值 —— 三条都是「我以为已经保证了」](ADR-47-落盘、并发、认不出的取值-——-三条都是我以为已经保证了.md) |
| **ADR-49** | [刷了文件所在的那一层，没刷记住这一层的那一层](ADR-49-刷了文件所在的那一层，没刷记住这一层的那一层.md) |
| **ADR-50** | [崩溃持久性从「承诺」降为「尽力而为」](ADR-50-崩溃持久性从承诺降为尽力而为.md) |
| **ADR-54** | [一个改动装了三层保证 —— 按「谁在写」拆开](ADR-54-一个改动装了三层保证-——-按谁在写拆开.md) |
| **ADR-55** | [拆出来之后第一件事：三步协议的第二步没有比对](ADR-55-拆出来之后第一件事：三步协议的第二步没有比对.md) |
| **ADR-56** | [一个只写在注释里的契约，只兑现了一半](ADR-56-一个只写在注释里的契约，只兑现了一半.md) |
| **ADR-58** | [把体量与集成纪律从第三层提到第二层](ADR-58-把体量与集成纪律从第三层提到第二层.md) |
| **ADR-59** | [分支寿命从第三层提到第二层](ADR-59-分支寿命从第三层提到第二层.md) |
| **ADR-61** | [--all 那条路也接上 PR 创建时间这把锚](ADR-61---all-那条路也接上-PR-创建时间这把锚.md) |
| **ADR-62** | [判定模块必须有变异，从欠条变成审计的一项](ADR-62-判定模块必须有变异，从欠条变成审计的一项.md) |
| **ADR-63** | [叠分支不再继承下面那条的豁免：把 PR 的 base 传进闸门](ADR-63-叠分支不再继承下面那条的豁免：把-PR-的-base-传进闸门.md) |
| **ADR-64** | [评审发现分三档路由，第二轮之后只登记不修，阻塞项除外](ADR-64-评审发现分三档路由，第二轮之后只登记不修，阻塞项除外.md) |
| **ADR-65** | [三条分支合回一条：按「谁的版本更新」取产品代码，按「谁有那台机器」取检查链](ADR-65-三条分支合回一条：按谁的版本更新取产品代码，按谁有那台机器取检查.md) |
| **ADR-66** | [并发写回的承诺从 D4 撤回，直到所有写入方都进了同一个协议](ADR-66-并发写回的承诺从-D4-撤回，直到所有写入方都进了同一个协议.md) |
<!-- END:GENERATED -->
