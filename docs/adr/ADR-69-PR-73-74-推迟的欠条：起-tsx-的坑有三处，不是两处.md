# ADR-69 PR #73 / #74 推迟的欠条：起 tsx 的坑有三处，不是两处

> ⚠️ 欠条：`npm run mutate` 起测试子进程那个坑**有三处，不是两处** —— `scripts/check/mutate.ts`
> 已修（PR #73），还剩 `scripts/check/selfcheck.ts` 的 `spawnSync('npx', …)` 与
> `scripts/test.ts` 里那处直接指 `node_modules/.bin/tsx` 的调用。后者在 Windows 上同样起不来：
> libuv 只按**文件名段**找扩展名（`.bin` 里那个点不算），无扩展名就只试 `tsx.com` / `tsx.exe`，
> 而 npm 在 Windows 生成的是 `tsx.cmd` / `tsx.ps1` / `tsx`(sh shim) · 重启条件：PR #74 已合入 ——
> 三处抽一份共用的「怎么起 tsx」一起改（同一份证据，按 `6-INTEGRATE.md` 的切分判据是一个改动）

> ⚠️ **上面第一块这张欠条已还**：三处起 tsx 的写法抽成 `scripts/check/tsx-cmd.ts` 一份，
> 由 #78 还上 —— 只出「起哪个可执行文件、前面垫哪些参数」，不代跑（三处的等法不一样，
> 一样的是那条命令）。三条负片 `M-H16-a` / `M-H16-b` / `M-H16-c` 各守一个决定。
> **另外四块没动**，各自的重启条件照旧。

> ⚠️ 欠条：Windows 上 `scripts/check/mutate-restore.ts` 里 `killTest` 那一刀**落空**，失败还被
> `catch` 吞掉 —— `process.kill(-pid)` 的负号是 POSIX 的进程组语义，配的是入口那边的
> `detached: true`；而 Node 文档写明 `detached` 在 Windows 上给的是**一个自己的控制台窗口，
> 不是进程组**，那个组号没有对应的组可落。**落空的只是「把它停下来」那一半**：`onInterrupt` 是
> 先杀、再还原、才退出，`killTest` 失败也照常返回，源文件是还得回去的；停不下来的是子进程 ——
> 它活着，跑的是被改过的源码，而父进程已经还原完、退出了 · 重启条件：PR #73 已合入 ——
> 要碰 `mutate-restore.ts` 的杀进程策略（Windows 上得换成 `taskkill /T` 之类），是另一份证据

> ⚠️ 欠条：`scripts/check/mutations.json` 里 **M-H14-e 的 `why`** 说反了 —— 它写着变异运行
> 「也会清掉、**写脏**那份覆盖记录，普通运行留下的证据被**顶替**」，而实测只会**被清掉**：
> 该变异自己会被断言抓到、`fail` 不为零，写盘资格那一半仍要求 `fail === 0`。这段 `why` 会被
> `npm run mutate -- --brief` 直接打给**写测试的那个上下文**（`4-VERIFY.md` 的准入读物），
> 误导面比一段注释更宽 · 重启条件：PR #74 已合入

> ⚠️ 欠条：`scripts/check/mutate.ts` 里引 Node 文档那句「`.cmd` 那一节写着**不推荐，见 DEP0190**」
> **没有标版本**。本仓库钉的是 Node **v22**，而那句「not recommended」只在 **v24+** 文档里；
> DEP0190 在 v22 是 **Documentation-only（v22.15.0 起）**，不是运行时弃用（实测 `{ shell: true }`
> 不发任何警告），运行时弃用要到 v24。**结论不受影响** —— DEP0190 正文那句「不转义、只用空格拼、
> 会导致 shell 注入」在 v22 文档里确实在 · 重启条件：下次有改动碰到 `mutate.ts` 那段注释，
> 或本仓库把 Node 升到 v24

> ⚠️ 欠条：**「并发抢号变成文件名撞车，git 当场报冲突」这句话，结论对，机制名不对，而且
> 它挡住的理由和号无关。** 三处：`docs/adr/README.md:22`、`scripts/check/adr-rule.ts:9`
> （同一句），以及 `adr-rule.ts:132`「号一旦落地，别的分支再取同一个号就是**文件名撞车**，
> git 当场报冲突」。实测（`git merge-tree --write-tree`，只读，不落盘）：
>
> - **git 确实当场报冲突** —— 但标题不同时文件名也不同，两份 ADR 文件是各自干净落地的；
>   撞的是 `docs/adr/README.md` 里那张**生成的索引**。它必须随分支一起提交（索引与目录
>   不一致 `adr-sync` 就 `exit 1`），而所有新行都插在 `<!-- END:GENERATED -->` 前的
>   **同一个锚点**，两侧同位插入不同的行。本条与 #75 同取 68 时如此，本条改到 69 之后
>   再合，**仍然冲突**。
> - **这道冲突不是编号唯一性的闸。** 拿两条号不同的探针分支（70 与 71）合一次，在同一锚点
>   照样冲突；反过来，号相同的两条若被「两行都留」地解掉，主干上仍会落下两条同号记录，
>   那时才由 `npm run adr` 报红。**挡的是并发加记录，不是抢号。**
>
> · 重启条件：本 PR 已合入 —— 要么把那三句的机制名改准（撞的是索引行，不是文件名；且它
> 不区分是否撞号），要么加一道真正管号的闸，而管得住的那道**必须跨分支**（见下面最后一段）

- 日期 · 2026-09-05 · PR #73 / #74 合入后的欠条 PR
- 类型：**不属于反向输入的五类** —— 本条不提出任何变更，只登记欠条
- 冲击的需求：—— 无，五条都落在检查链与决策记录自身，不触及需求登记表
- 结论：**接受为已知缺口**，各带重启条件；本条只记事实，修法不在本条

### 为什么单开一条记录

按 `6-INTEGRATE.md`「登记怎么做：欠条 PR」，合入后作者开的第一条 PR 就是欠条 PR，
把「不修在本 PR 的发现」里的每一条各写一行。那份索引只是目录，不是登记处 ——
留在 PR 描述里的东西，接手的会话翻不到。

**这一条要装的不止 #74 那三条。** #73 合入之后按这条规矩本该先开欠条 PR，
而当时接着开的是 #74（补一条没赶上合并的更正），于是 #73 索引里的两条从未落到主干上：
一条是 `selfcheck.ts` 起 tsx —— 第一块把它和 #74 查出来的第三处并成了一条；
另一条是 Windows 上那一刀落空 —— 第二块，**是评审在本 PR 上点出来的，我自己没想起来**。

被指出的这些事在主干上都没有现成的记录可以追加，所以按 `6-INTEGRATE.md`「都没有的，按
`2-CHANGE.md`「接受为已知缺口」开一条」新开这一条只装欠条的记录。最后一块是本 PR 自己撞出来的
（编号撞车）——按同一节「本来就要为阻塞项**或降级** push 的，欠条可以搭在那次提交里」，
它搭在这里，不再单开一条欠条 PR。

### 共同点值得记一句

**第一块是判据落在实例上而不是类上。** #73 修了 `mutate.ts` 一处就收工，而同一个坑当时
已经有第二处（`selfcheck.ts`），第三处（`test.ts`）是后来查 Node 在 Windows 上怎么找可执行
文件时才发现的。ADR-45 正文骂过同一件事（「一次只修一处就是又一次『只修实例』」），
`4-VERIFY.md` 把它一般化成「判据要落在那个类上」——**而我在读着 ADR-45 干活的时候又踩了一次**。

**第二块不是那一类，别归进去（评审指出）。** 「杀不掉」和「起不来」是两个不同的坏，
第二块自己的重启条件就写着它「是另一份证据」；#73 把它明写成推迟项，是**正当的推迟**，
不是只修了一个实例。它的坏法是另一种：**写进 PR 描述就停在那儿了。** #73 合入之后本该先开
欠条 PR，接着开的却是 #74，于是它只活在一个已关闭的 PR 描述里 —— 正是 `6-INTEGRATE.md`
那一节自己警告的「留在 PR 描述里的东西，接手的会话翻不到」，而且是**评审替我想起来的**。

**第三、四块都是把没核过的说法写进记录。** 一条是变异集的 `why`（主干上早就写着，这次才被实测
推翻），一条是我自己引 Node 文档时没核版本。`docs/CONVENTIONS.md` 第 11 条说的「描述保证的
那句话，没有任何检查看着」，这两条各是一个新实例。

**第五块不同类：它是把一道闸认错了。** 编号唯一性没有任何**跨分支**的占号检查；合并时确实
会被索引的同位插入冲突挡下，但那道冲突不区分是否撞号，也挡不住「两行都留」的解法 —— 那时
仍要等合进主干之后 `npm run adr` 才红。**这道缺口和 ADR-34 那条欠条不是同一个形状**（评审指出，我原先写成同一个）：
ADR-34 缺的是「所有消费者之前的一个统一阶段」，那道闸站在同一棵树里就建得起来；而这里
`adr-sync` 看得见的只有当前这棵树加主干（`readdirSync` 加 `git show <base>:<路径>`），
别的分支上占着的号它**看不见** —— 同一棵树里加多少道闸都挡不住，要挡住得有跨分支的占号或协调。

> ⚠️ **上面第五块这张欠条已还。** 那几句的机制名改准了：撞的是 `docs/adr/README.md`
> 里那张**生成的索引**（新行都加在同一个位置），而这道冲突**不区分**两边有没有抢同一个号；
> 撞号由 `adr-rule.ts` 的 `checkAll` 在两边合到一起之后比出来。
> **结论不动** —— 一条记录一个文件仍然是对的，变的只是「它挡住的是什么」这半句。
>
> **实际改了七处，而欠条点的是三处 —— 先说清两个数的单位不一样。** 欠条那个「三处」
> 数的是**出现次数**（落在两个文件里）；这次按出现次数数是**七处**，落在六个文件里。
> 那次清点只扫了 `docs/adr/` 与 `adr-rule.ts`，漏掉 `process/2-CHANGE.md`（同一句的
> 通用层版本）、`scripts/test.ts` 里那条注释、变异集 `M-H10-a` 的 `why`（它会被
> `--brief` 打给写测试的上下文，误导面比注释更宽），以及 `adr-sync.ts` 里那句
> 「这次拆分本来要暴露的并发抢号」—— 最后这处带的是同一个**前提**而不是同一句话，
> 按下面那条判据照样扫得到，所以一并改了。
>
> 判据该落在**那句话**上 —— 拿「撞车／抢号／静默交错」扫全仓，而不是落在那几个行号上。
> **这正是 ADR-64「重启条件响了没人提醒」那条欠条的又一个实例：清点靠人记，就会漏。**
>
> **顺带查出来一件比机制名更难看的事。** 原先 `adr-rule.ts` 把「文件名撞车」写成了
> 跨分支占号的**保护**。实测（`git merge-tree`，用**两条在途分支上**真实发生过的那次
> 抢号 —— `950e37a`（后来合进了主干）与 `ace85ef`（只在
> `origin/claude/kol-formal-verification-kr5igx` 上，从未合入）各建了一个 ADR-68；
> ⚠️ 这两份**从来没有在主干上合到一起过**，主干当前树上 68 是个永久空号，
> 所以不能说成「主干上真实发生过」）：两份同号记录**各自干净落地**，
> 唯一的冲突路径是索引；换成编号不同的两条（68 与 69）再合一次，照样在同一处冲突。
> 而 `slugify` 有损三步（剔标点、连续空白归一成 `-`、截到 32 码点），两个**不同**标题
> 在同号时可以生成完全一样的文件名（截断和剔标点这两种，`scripts/test.ts` 里各摆着
> 一条测试；顶到 32 码点上限的标题现存记录里已经有）—— 那一格 git 报的是
> 「两边都新建了这个文件」，
> 人按「留一个」解掉就无声少了一条记录，而检查事后只看得见一个文件、一路全绿。
> **那一格不是保护，是丢数据。** 这句写进了 `adr-rule.ts` 的注释。
>
> ⚠️ **第四句要更正的话就在这张欠条自己的正文里，而我把它原样抄进了四处新文字。**
> 上面第五块写着「标题不同时文件名也不同」；本 PR 第一版拿它当替换文案，写进了
> `README.md:22`、`adr-rule.ts` 那段注释、`M-H10-a` 的 `why`、`scripts/test.ts`
> 那条注释 —— 主干上这四处**原先并没有**这句话，是这次改机制名时新引进去的。
> #113 第一轮评审指出它不成立：`slugify` 有损三步（剔标点、连续空白归一成 `-`、
> 截到 32 码点），不同标题照样可能同名。四处连同上面那段「更难看的事」（它只提了
> 截断这一种）都在第二轮改掉了。
>
> **结论仍然不动，而且比原先更成立**：「别指望 git 拦住抢号」不但因为它多数时候
> 拦不住，还因为真拦住的那一格是丢数据。正文按「追加不删改」留着原话。
>
> 这也是同一个毛病的又一次：**改一句错话的时候，顺手把它错的那半个前提也搬走了。**
> 判据该落在前提上 —— 换掉一句话之前，先问它凭什么成立，而不是只问它叫什么名字。
>
> · **另外四块逐块点名**（笼统写一句「状态照旧」正是这张欠条自己在骂的那件事）：
>   - **第一块**（三处起 tsx）：已还，#78 抽成 `tsx-cmd.ts`，本文件往上第 10 行就标着 —— 它没有活着的重启条件了。
>   - **第二块**（`process.kill(-pid)` 在 Windows 上不是进程组）：**仍欠着**，`mutate-restore.ts:127` 那一刀还在，重启条件不变。
>   - **第三块**（`M-H14-e` 的 `why`）：**仍欠着**，变异集里那段原话没动，重启条件不变。
>   - **第四块**（`mutate.ts` 引 Node 文档没标版本）：**这次清点才发现它早就还了，而且没人标。**
>     那段注释在 #78（`d8818e4`）随三处起 tsx 一并移进 `tsx-cmd.ts`，版本限定当时就补上了
>     （`tsx-cmd.ts` 那段现在写着「本仓库钉的是 Node v22……结论不受版本影响」）；
>     主干的 `mutate.ts` 里现在一个 `DEP0190` 都没有。**重启条件在 #78 那次就响了，
>     被顺手还掉，全仓没有任何地方标过** —— 这是 ADR-64 那条欠条的第十个实例（该条既有实例已编到第九，见 ADR-64 那三块追加），补标在此。
