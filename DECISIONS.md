# 决策记录

> 一条记录，一个不可回收的编号。**追加，不删改。**
>
> 编号 `ADR-NN`。数据需求用 `D` 前缀（`D1`、`D2`…），两套前缀不撞号。
>
> 驳回尤其要记 —— 同一个提议会反复出现，尤其当实现者是每次从零开始的 AI。
> **一条驳回记录的价值，和它被引用的次数成正比。**

---

## ADR-01 P2 拆成可执行的一半和只能靠自觉的一半

- 2026-08-25 · 建立需求登记表与审计时发现
- 类型：**需求有歧义**（不是变更 —— 需求没说清，而不是说错）
- 触发它的事实：审计报「P2 是红线但没有测试」。`process/1-REQUIREMENTS.md`
  要求每条红线至少一个测试且被变异验证，但 P2 管的是 Agent 生成的文本内容，
  不经过本仓库任何代码路径，没有可变异的实现。
- 冲击的需求：P2
- 结论：**采纳**（补充定义，不改红线本身）
- 理由：两个都不能选 ——
  降级 P2 不可接受（邮件发出去收不回来，是代价最高的一条）；
  编一个假测试更不可接受（`4-VERIFY.md`：一个从来不会失败的检查等于没有检查）。
  正确的做法是把它**拆开**：
  - **可执行的一半**：产出物必须原样保留占位符。render 不得删除、替换或
    「补全」草稿里的 `{…}` —— 抹掉占位符，运营看不到还有待填项，会把半成品发出去。
    已由 M-P2-a 覆盖。
  - **只能靠自觉的一半**：草稿里的产品事实是否真的来自产品页。
    这是 `process/README.md` 第三层，**显式留在审计报告里，不假装它被保证了**。
- 连带改动：`docs/requirements.json`（P2 的 accept）· `scripts/lib/rows.ts`（提取
  可测函数）· `scripts/test.ts` · `scripts/check/mutations.json`
- 教训：**红线「必须有测试」这条要求，本身会逼出一个有用的区分** ——
  一条红线如果完全测不了，多半是它把「代码该保证的」和「人该保证的」混在一起说了。
  拆开之后两半都变得可管理。这不是 P2 独有的形状。

---

## ADR-02 变异存活时先分清是测试假，还是变异等价

- 2026-08-25 · 首次跑变异测试时
- 类型：**事实证伪**
- 触发它的事实：M-P1-a 和 M-P1-b 同时存活。诊断后发现两者原因完全不同 ——
  - M-P1-a：测试根本没调用 `fillEmail`，只测了底层的 `extractEmail`。**测试是假的。**
  - M-P1-b：变异把 `c.followers !== undefined && c.followers >= 5000` 改成
    `(c.followers ?? 0) >= 5000`。对 `undefined` 两者都是 false，**行为完全一致** ——
    这是个等价变异，杀不掉也不该杀。
- 冲击的需求：P1
- 结论：**采纳**（M-P1-a 修测试；M-P1-b 改写变异）
- 理由：`4-VERIFY.md` 说「变异存活 = 那个测试是假的，必须修测试（不是删变异）」。
  这条在绝大多数情况下对，但**它预设了变异本身是有效的**。
  等价变异是例外：它不改变可观测行为，因此任何正确的测试都杀不掉它。
  把等价变异当成"测试假"去改测试，只会写出一条断言实现细节而非行为的坏测试。
  M-P1-b 改写成「把未知粉丝当作**符合区间**给分」—— 那才是真的 P1 违规，且立刻被抓到。
- 连带改动：`scripts/check/mutations.json` · `scripts/test.ts` ·
  `scripts/lib/score.ts`（提取 `passesFollowerGate` 使闸门可测）
- 教训：**遇到变异存活，先问「这个变异真的改变了行为吗」，再问「测试为什么没抓到」。**
  顺序反了会污染测试集。判别办法：手工推演变异前后在边界输入上的输出，
  完全一致就是等价变异。

---

## ADR-03 成本闸门不预置默认值，超限后允许追加预算续跑

- 2026-08-25 · 需求评审时由用户提出
- 类型：**发现更优解**
- 触发它的事实：初稿设计的是「达到预算上限即停止采集，用已有数据收尾」。
  用户指出应当允许追加额度后重新继续。
- 冲击的需求：P3 · F7 · D6
- 结论：**采纳**
- 理由：硬停会在两种常见情况下造成纯浪费 —— 预算估低了、或者关键词比预期多。
  两种情况下用户都愿意多花几毛钱跑完，而硬停逼他从头再来一遍（**已经花掉的钱白花**）。
  采纳的前提是采集必须可断点续跑（D6）：`task.json` 记录已完成的关键词索引与
  累计请求数，`--resume` 重入时跳过已完成项、以已有 requests 初始化 Budget，
  因此**不重复跑也不重复计费**。
  检查了这不是「降标准」：用户得到的信息没有变少或变模糊，
  相反他多拿到了一个明确的选择点。
- 连带改动：`skill/SKILL.md`（超限后的续跑一节）· `docs/business-requirements.md` ·
  `scripts/collect.ts`（退出码 3 + `--resume`）· `scripts/lib/budget.ts`（跨运行累加）
- 教训：**「达到上限就停」是个看起来安全实则昂贵的默认。**
  闸门的目的是让用户知情并做选择，不是替他做决定。
  凡是设计成「到点即停」的地方，都该问一句：停下来之后，他还能不能接着走？

---

## ADR-04 测试与实现由同一上下文写成，登记为已知缺口

- 2026-08-25 · 引入 process/ 流程时
- 类型：**接受为已知缺口**
- 触发它的事实：`process/4-VERIFY.md` 的第一条对策是「写测试时不许读实现」，
  且「让写代码和写测试的不是同一个上下文」。本仓库的代码和测试是同一个会话里
  连续写出来的，完整违反了这两条。
- 冲击的需求：全部有测试的需求（P1–P5 · D1–D7 · F2/F6/F7 · U1/U2/U4）
- 结论：**接受为已知缺口**
- 理由：现在重写一遍测试并不能消除污染 —— 同一个上下文对需求的理解已经形成，
  「假装没读过实现」不产生任何保证。**声明它，比假装解决它更有价值。**
  部分缓解来自变异测试：它是他评而非自评，14 个变异全部被抓到，
  至少证明这些测试不是纯粹的实现镜子。但变异集也是同一上下文写的，
  **同源污染没有被消除，只是被削弱了**。
- **什么条件下重启这个讨论**：下一次需求变更时，由一个**清空上下文的新会话**
  只读 `docs/SPEC.md` 的验收标准重写测试，与现有测试比对差异。
  差异点就是污染的具体位置。
- 连带改动：`scripts/test.ts` 顶部注释 · `docs/CONVENTIONS.md` 第 10 条
- 教训：**同源污染没有事后补救办法，只有事前安排。**
  下一个项目从第一天就把测试放到独立会话/分支去写，成本远低于事后重来。

---

## ADR-05 429 退避重试：文档承诺了但实现漏了

- 2026-08-25 · 脚本自检首次跑通时
- 类型：**事实证伪**
- 触发它的事实：假 fetch 在第 7 次调用注入 429，collect 直接抛错退出。
  查 `skill/references/providers/tikhub.md`，里面明确写着「429 → 退避后重试，
  把间隔从 150ms 调到 300ms」—— **文档写了，代码没实现。**
- 冲击的需求：无（这是实现缺陷，不是需求变更）
- 结论：**采纳**（补实现）
- 理由：事实赢。文档是对外承诺，实现必须跟上。
  实现时多做了一步：撞到 429 后**永久调慢后续间隔**（翻倍，上限 1s），
  而不只是本次退避 —— 撞过一次说明整体节奏太快，只退避当次会立刻再撞。
- 连带改动：`scripts/providers/tikhub.ts`
- 教训：**这个缺陷是自检抓出来的，不是测试抓出来的。**
  单测不会构造 429，因为写测试的人和写实现的人有同一个盲区。
  假 fetch 的价值不在断言正确性，在于**逼着每条错误分支真的被执行一次** ——
  凡是「只在特定条件下才走到」的代码，都该被这样喂一遍。

---

## ADR-06 首次完整跑通 Skill 暴露的四个采集缺陷

- 2026-08-26 · 按 SKILL.md 走完 Phase 01–06 时
- 类型：**事实证伪**
- 触发它的事实：真实跑一遍便携榨汁机的建联任务，四个只有真跑才会暴露的问题：
  1. **`offset` 按固定步长递增。** 写的是 `page * 20`，而 API 某页返回不足 20 条时，
     下一页就会**跳过数据**。旧项目 `tikhub-kol-sourcing` 用的是 `offset += items.length`，
     是对的
  2. **没用 API 的 `has_more`。** 靠「本页新增 0 人」推断关键词是否耗尽 —— 不准，
     且要多发一次请求才知道
  3. **profile 补全无 try/catch。** 64 人里任何一个 404，整轮采集全挂
  4. **达标判断数错了对象。** 用采集总数（64）比目标（50），但过粉丝闸门后只剩 36 ——
     既没达标，又因为提前停止导致 7 个关键词只跑了 1 个
- 冲击的需求：D6（断点续跑）· F2（四维关键词）
- 结论：**采纳**（全部修复）
- 理由：第 4 条最严重，它让**关键词策略整体失效** —— 竞品词一个词就吃掉全部配额，
  场景词、人群词、整个 IG 一条都没跑到。而四维各有各的价值正是这套策略的核心。
  修法不只是改判据，还把顺序执行改成**轮转**：先给每个关键词各抓一页，再回头抓第二页。
  这样同样的预算下，每个维度都保证有产出。
  改轮转又引入一个回归：per-keyword 的 offset 只在内存里，续跑时全部从第 0 页重来，
  **重复抓也重复付费**，违反 D6。已把 offsets 持久化进 `task.json`。
  该回归是**脚本自检抓到的**，不是测试 —— 单测不会构造「预算中途耗尽再续跑」这个序列。
- 连带改动：`scripts/lib/types.ts`（新增 `SearchPage`、`TaskState.offsets`）·
  `scripts/providers/tikhub.ts`（search 返回分页信息）· `scripts/collect.ts`（轮转 +
  达标判据 + 错误恢复）· `skill/references/providers/_interface.md`
- 教训：**「达标」这类判据最容易数错对象。** 采集数、过滤后数、去重后数、
  写进名单数是四个不同的量，用错一个就会在完全正常的日志下产出错误行为。
  凡是写 `if (x >= target)`，都要回答一句：**x 和 target 是同一种东西吗？**

---

## ADR-07 旧项目的代码值不值得参考：三处借鉴，四处反面教材

- 2026-08-26 · 用户问 `tikhub-kol-sourcing` 的实现值不值得参考
- 类型：**发现更优解**（部分）
- 触发它的事实：对照读 `scrape-powerbank.ts`
- 结论：**采纳三处，其余不取**
- **借鉴的**：
  - `offset += items.length` —— 见 ADR-06，我们这边是真 bug
  - `if (!data?.data?.has_more) break` —— 用 API 自己的信号判断分页终止
  - profile 补全单个失败不中断
- **不取的（旧代码的问题）**：
  - `Number(a.follower_count || 0)` / `String(a.signature || '')` —— 正是 P1 禁的形状
  - `if (followers < FOLLOWER_MIN) continue` 在**采集阶段**就按粉丝数丢人。
    而 `follower_count` 缺失时是 0，于是**所有没返回粉丝数的创作者被静默丢弃**。
    这是 P1 那条红线最好的反面实证
  - 空 `catch (e) {}` 吞掉错误 —— 「补全失败」与「补全了但没 bio」分不清
  - 用 `app/v3/handler_user_profile` 补 profile，然后读 `user.bioLink?.link` ——
    **该端点没有 `bioLink` 字段**，那行是死代码，`bio_link` 恒为 null。
    跨平台同人识别最可靠的信号，在旧版里从未生效过
- 教训：**一份能跑出结果的代码，不等于它做的事和你以为的一样。**
  旧项目跑了很多轮、产出过真实名单，但其中一整个字段是死的、一整类创作者被静默丢弃 ——
  两者都不会报错，也不会在产出物里显形。
  这正是本项目把「未查询 vs 查过没有」做成红线并配变异测试的原因。

---

## ADR-08 采集累加器与交付物拆成两个文件；记忆过滤按任务作用域

- 日期 · 2026-08-26 · 代码复查环节
- 类型：**事实证伪**（两处实现与 D6/P1 的假设矛盾）
- 冲击的需求：D6（断点续跑）、P1（三态）
- 结论：**采纳，两处都改**

### 触发它的事实

复查时实跑 `collect → render → collect --resume`，`creators.json` 从 4 人变成 **0 人**：

```
1) collect          → creators.json 4 人
2) render           → memory 写入 4 条
3) collect --resume → collected: 0, filtered_recommended: 4
   creators.json 剩 0 人
```

`collect.ts` 结尾把过滤后的名单写回 `creators.json`，而 `--resume` 开头又从**同一个文件**
读回。`render.ts` 的 `recordRecommendations` 把这批人按当前 product 记进记忆之后，
续跑时 `filterByMemory` 判定「本产品已推荐过」，全部滤掉，然后把空数组写回去。
**已经付费采集的数据不可恢复地消失。**

触发路径不冷门：用户看完报告说「人不够，再多找点」，Agent 就会去跑 `--resume`。
而 `skill/SKILL.md` 当时根本没写 `--resume` 的具体命令。

第二处：`identity.ts` 的 `primary.email = primary.email ?? secondary.email ?? null`
把两侧都 undefined（未查询）的情况写成 null（查过，没有）。同一个函数里紧挨着的 `sum()`
对 followers 处理得完全正确 —— 漏的只是 email 这一行。

### 连带改动

| 文件 | 改了什么 |
|---|---|
| `scripts/lib/task.ts` | 新增 `creators.raw.json` 累加器（只增不减）+ `taskId()` |
| `scripts/collect.ts` | `--resume` 读累加器；过滤只作用于交付物 |
| `scripts/lib/memory.ts` | `filterByMemory` / `recordRecommendations` 增加 task 作用域；同任务重复 render 覆盖而非追加 |
| `scripts/lib/identity.ts` | 抽出 `mergeEmail()`，三态与 `sum()` 对齐 |
| `scripts/check/lint.ts` | `FALLBACK` 加入 `null` |
| `scripts/check/audit.ts` | 删掉让入口自检判据失效的 `includes(base)` 兜底 |
| 测试 | 新增 `[P1] 跨平台合并…`、`[D6] 续跑不得被自己上一轮滤空` |
| 变异 | `M-P1-f`、`M-D6-b` |
| 自检 | 新增 `collect → render → --resume` 端到端回归 |
| 文档 | D6 验收补充；SKILL.md 补 `--resume` 命令；memory.md 补 task 字段 |

### 教训

**这两个 bug 全部通过了 lint / 类型 / 20 个变异 / 自检 / 审计。** 原因各不相同，
但指向同一件事：

1. **`null` 是三态模型的中间态，却是唯一没被 lint 盯住的兜底值。**
   `FALLBACK` 白名单列了 `0 / '' / [] / false`，偏偏漏了那个语义最接近红线的。
   检查表里最该出现的一项，往往因为「它看起来不像默认值」而被漏掉。

2. **数据丢失发生在文件的职责重叠处，不在任何一个函数内部。**
   `filterByMemory` 是对的，`saveCreators` 是对的，`loadCreators` 也是对的 ——
   错的是让同一个文件既当输入又当输出。**单元测试永远看不到这类 bug**，
   因为它不在任何一个单元里。这是 ADR-06 那条教训的另一个面：
   「每个部件都正确」和「组装起来正确」是两个命题。

3. **同一个坑第四次。** ADR 里已经记过两次「逻辑埋在入口脚本里就测不了，
   要抽到 `lib/`」（M-P2-a、M-U5-a 存活），于是有了 `rows.ts`。
   但 `collect.ts` 的 `main()` 仍是裸的流水线：合并 → 闸门 → 记忆过滤 → 落盘，
   而 bug 正好落在这段。**教训被记下来了，却没有被应用到最大的那个入口。**
   记录一条教训的成本远低于执行它 —— 下次复查先看还有哪些 `main()` 没被拆。

---

## ADR-09 入口脚本的管线逻辑抽到 lib/pipeline.ts

- 日期 · 2026-08-26 · 复查后的整改
- 类型：**发现更优解**（结构调整，不改需求、不改行为）
- 冲击的需求：无。P1/P4/D6/F5/U1 的既有验收标准不变，只是从此有了测试
- 结论：**采纳**

### 触发它的事实

ADR-08 的第三条教训写着「同一个坑第四次：教训被记下来了，却没有被应用到最大的
那个入口」。这条 ADR 就是去把它应用掉。

`collect.ts` 的 `main()` 与 `render.ts` 的顶层各有一段裸露的管线：

```
collect:  同人识别 → 合并 → 粉丝闸门 → 记忆过滤
render:   算分 → 分层 → 受众降权 → 排序
```

八个步骤全部有顺序约束，但顺序本身没有任何测试守着 —— 因为它们不在函数里，
测试拿不到它们。

### 抽出后新增的保证

`lib/pipeline.ts` 的 `finalize` / `rankCreators` / `pendingKeywords` /
`keywordStats` / `tierCounts`，配 5 个变异：

| 变异 | 违反什么 |
|---|---|
| `M-P1-g` | 先闸门后合并 → 「两平台各 3000、合起来 6000」的人被提前丢掉 |
| `M-D6-c` | `finalize` 就地修改累加器 → 退回 ADR-08 那个 bug 的形状 |
| `M-P4-b` | 记忆过滤跑在闸门之前 → `filtered_contacted` 虚报打扰规模 |
| `M-F5-a` | 降权跑在分层之前 → 降完被 `tierOf` 覆盖，地域不达标的人留在 A 级 |
| `M-U1-b` | 管线不排序 → A 级混在几百行里 |

`finalize` 顺手改成 `structuredClone` 输入：「累加器只增不减」从此是**结构性**
保证，不再依赖调用方记得先落盘再调用。

需求覆盖 19 → 21 条（F3、F5 此前没有任何测试）。

### 行为未变的证据

拿重构前的 commit 建 worktree，两边跑同一份真实数据（48 人的产出目录）：

```
✓ kol.csv        逐字节一致
✓ report.html    逐字节一致
✓ creators.json  逐字节一致
✓ memory         只差写入时间戳
· meta.json      仅 keywords[] 内的键顺序不同，值全等
```

### 教训

**判断一段代码该不该从入口搬进 `lib/`，只问一句：它的顺序错了会不会出错。**

会，它就有语义；有语义就该能被测；能被测就不该待在入口脚本里。
比「这段有多长」「看起来乱不乱」都好用 —— 后两个判据会把
`main()` 里那种「一行一步、读起来很顺」的管线判成不需要动，而那恰恰是最危险的地方。
已写入 `docs/CONVENTIONS.md` 第 10 条。

---

## ADR-10 不接 DecodeCreator；公开信号只做风险复核

- 日期 · 2026-08-26 · 外部增强供应商复查
- 类型：**事实证伪**
- 冲击的需求：P1、P5、F5；新增 D8、D9、F8、U7
- 结论：**采纳** —— 撤回将 DecodeCreator 作为正式增强源的候选结论

### 触发它的事实

用户实际尝试后确认 Influencers Club 与 Click Analytic 都要求公司/工作信息，违反本仓库
“普通个人拿到 repo 就能注册运行”的分发前提。继续搜索时，DecodeCreator 表面上满足普通
Google 登录与双平台，但进一步核验发现：

1. [官方 About](https://decodecreator.com/about) 说明服务由印度的一名独立开发者运营，
   数据来自 HikerAPI、tikwm 与 YouTube API 后再做派生分析；联系渠道是 Gmail
2. [服务条款](https://decodecreator.com/terms) 明确派生信号不保证准确，99.5% 只是目标而非保证
3. [.com 注册局 RDAP](https://rdap.verisign.com/com/v1/domain/DECODECREATOR.COM)
   显示域名创建于 2026-07-14，核验时只有约六周历史
4. 未找到可核验公司主体、独立基准、客户案例、状态历史或第三方评测

这些事实不能证明它是骗局，但足以证明仓库没有资格让它的分数改变 KOL 排名。

### 结论与连带改动

- 不新增 DecodeCreator key、适配器或任何供应商专属评分字段
- 原来的 `fake_follower_score` 仅保留为旧任务兼容字段，分层明确忽略
- 用 TikHub 主页近期作品计算透明的公开指标；只能称“受众质量风险”，不能称假粉率
- high 至少需要两个同行异常信号，只降一级人工复核，永不自动删除
- 不新增不透明 KOL Weight；现有 score 权重不变
- KOL Rate 拆成公开互动率与明确合作报价；缺报价不自动估价
- 第三方准入改成主体、个人自助注册、双平台、方法/schema、历史与小额实测全部通过

### 教训

**“有价格页、能点注册”不等于可进入决策链。** 数据供应商有两道门：第一道是能不能用，
第二道是它有没有资格影响用户花钱。前者只需验证注册和接口，后者还需要主体、方法、历史与
可重复基准。以后调研必须把两道门分开记录。

---

## ADR-11 当前活跃度只做标签，不改 KOL 分层

- 日期 · 2026-08-26 · 停更账号处理
- 类型：**产品取舍**
- 冲击的需求：新增 D10，扩展 U7
- 结论：**采纳**

### 触发它的事实

累计作品数只能证明历史内容积累。一个发过数百条内容、曾经互动和带货都很好，但已经
停更一年的账号，仍会满足原来的 `post_count > 30` 加分。发布间隔也只描述过去的节奏，
不能回答“截至本次采样已经多久没有更新”。

这不是假粉或受众质量问题，而是当前合作就绪度。直接删除会损失历史高价值账号；继续把它
放在“活跃”口径里又会误导运营。

### 结论与边界

- 用样本时间减去最后发布时间，分为 active（≤45 天）、cooling（46–90 天）、
  dormant（>90 天）
- 标签按平台独立计算，仅展示，不改变 score、tier、风险或名单成员
- `post_count > 30` 的 +10 保留，但统一改称“内容积累”，不再声称它代表当前活跃
- 最近发布时间包含置顶作品；置顶只从绩效聚合中排除，不能抹掉一次真实发布
- 一个有效发布时间即可测量；缺失或异常时间明确 unavailable，不补默认值
- 活跃状态是采样时快照。旧样本可以零请求补算；需要确认现在是否复更时必须刷新样本

### 教训

**历史价值与当前可联系性是两条轴。** 把它们压成一个“活跃度”分数，要么误杀曾经有效的
创作者，要么把已经停更的人包装成随时可以合作。分别展示，才允许运营做真实取舍。

---

## ADR-12 架构文档只锚定骨架，不由代码生成

- 日期 · 2026-08-26 · 补流程缺环时发现
- 类型：**流程补缺**（环节表里一直有这个产物，但从未定义过它长什么样）
- 冲击的需求：无。不改登记表，只改文档层与检查链
- 结论：**采纳**

### 触发它的事实

`process/README.md` 的环节表从第一版就写着「架构设计 → 产物：设计文档」，约束指向
`3-BUILD.md`，而 `3-BUILD.md` 通篇只讲 UI 和代码纪律。**这个槽位空了整个项目周期，
而且空着不会报错** —— 审计不查它，同步表没有它，没有任何东西提醒过。

账单是明的：`docs/CONVENTIONS.md` 第 10 条自己写着「这条踩过四次」，前三次是变异存活
（M-P2-a、M-U5-a），第四次是 ADR-08 的数据丢失。四次都是同一形状：**每个单元都对，
错的是它们被组合起来的方式。** 这类事实需求表不管（那是实现结构）、代码说不出（它说得出
零件，说不出零件之间）、单元测试看不到（它不在任何一个单元里）。此前只能活在
`pipeline.ts` 的文件头注释里，跨文件的那一半根本没地方放。

### 两条驳回 —— 单独记，因为它们会被反复重提

实现者每次从零开始，而「把架构文档写成代码的散文副本」是它最自然的默认动作。

**1. 驳回「架构文档由代码生成」。** 需求文档可以由 `requirements.json` 生成，架构文档不行：
**可生成的部分（目录树、函数签名、依赖图）没有价值，有价值的部分（为什么是这个顺序）
不可生成。** 所以只锚定骨架 —— 骨架由机器守住，肉靠人写。

**2. 驳回「架构文档写每个函数做什么」。** 那是代码和类型的工作。抄一遍只会产生一份
没有测试、没有变异、写错了不报错、而且永远「看起来是对的」的负债。按 `4-VERIFY.md`
的标准，这种文档比没有更糟 —— 它让人以为这件事被记录过了。

### 结论与边界

- 通用层规则在 `process/5-DESIGN.md`（`process/` 从四份变五份）；本项目的那一份是
  `docs/ARCHITECTURE.md`
- `npm run arch` 硬校验三件事：模块双向覆盖、需求编号有效、**顺序契约必须绑定真实变异**
- 第三条是唯一一条真耦合：想声称某处顺序有语义，就得先有一个故意改坏它的变异；
  想删那个变异，架构文档的检查会红。**没有它，顺序契约表就退化成散文**
- **这个检查证不了文档是真的。** 一份把目录树抄进去、每行胡写一句话的文档也能通过 ——
  它保证的是覆盖与绑定。这一条已登记进 `process/README.md` 的第三层表，不假装它被保证了
- 连带修一个自己引入的漏洞：新增 `docs/ARCHITECTURE.md` 后，一条只在锚点表里被提过、
  根本没落到代码的需求会从「未被引用」变成「✓」。`audit.ts` 现在把这种情况单独报成
  「仅被架构文档引用，未落到代码或测试」

### 教训

**一份文档承诺的产物，如果没有检查守着，它的缺席是不可见的。**

这比「文档漂移」更隐蔽：漂移至少有两个版本可以对比，而一个从来没被生产出来的产物，
在任何一份报告里都不留痕迹 —— 环节表里那一行看起来一直是满的。

所以推论是：**每次在流程文档里新增一个「产物」，同时问一句「什么东西会在它不存在时报错」。**
答不出来，那一行就只是一句愿望。

---

## ADR-13 缓存命中永远按当前口径重算，不引版本号

> ⚠️ **本条「边界」一节里「盘上的样本本身不会被重写」那一句已被 ADR-14 推翻。**
> 其余部分仍然有效。（追加不删改：原文留着，因为它错在哪本身是有用的信息。）

- 日期 · 2026-08-27 · 修 D8 窗口顺序时由 PR #4 的评审意见发现
- 类型：**事实证伪**（「旧样本可零请求补算」这句承诺，实现只覆盖了其中一种「旧」）
- 冲击的需求：D8 · D10 · F8（受众风险按指标算，指标旧则风险旧）
- 结论：**采纳**

### 触发它的事实

`enrich` 的缓存命中分支只在**活跃字段缺失**时才本地重算 —— 那个条件是当初为 D10
迁移写的（更早的样本没有 activity 字段）。于是一份在 D10 之后、D8 窗口修复之前生成的
`enrichment.json`，`hasActivity` 为真，整段被跳过：旧窗口算出来的中位数、发布间隔、
以及按它们算出来的受众风险全部留在交付物里，运营不额外付费 `--refresh` 就换不掉，
而**交付物上看不出任何区别**。

条件写对了它当时要解的那个问题，错在它把「旧」定义成了「缺那三个字段」。

### 为什么不引版本号

第一反应是给 metrics 盖一个 revision，版本不匹配才重算。驳回，两个理由：

1. **重算不花钱。** 它只读磁盘上已有的样本，零 API 请求。既然没有成本，
   「什么时候该重算」就不是一个需要答案的问题 —— 永远算，缓存里的数永远是
   当前代码对当前样本的输出。
2. **版本号把一次性的问题换成永久的问题。** 有了它，每次改口径都要记得递增；
   忘记递增不报错，而症状与今天这个 bug 一模一样。省下来的是一次纯函数调用，
   换来的是一条只能靠自觉维护的规则。

### 边界

- `locally_recomputed` 的含义随之变准：现在是「手上的数**换过**的账号数」，
  不再是「补过字段的账号数」。比较用键序无关的序列化 —— 磁盘上的键序由写下它的
  那一版代码决定，直接比较字符串会把键序差异误报成改动
- 重算不改 `observed_at`：它属于当初那次采样，不是这次跑的时间
- 判定放在 `lib/assessment.ts` 的 `recomputeCachedMetrics`，不留在入口脚本 ——
  变异测试只跑 `scripts/test.ts`，留在 `enrich.ts` 里的判定没有任何东西守得住。
  M-D10-a 守判定；入口的接线由自检的「旧口径缓存零请求就地纠正」守
- **仍然做不到的**：盘上的样本本身是旧的（提供方多返回、在截断之前写下的那些）
  不会被重写，只是算的时候按窗口截。要让样本也收窄，等于重写历史记录，
  且会丢掉当初真实抓到的东西 —— 不做

### 教训

**零成本的正确性不要加条件。**

一个条件判断的价值，是省下它守着的那份成本。成本是零的时候，条件本身就是纯负债：
它唯一的作用是提供一个出错的地方，而且出错时不报警。

---

## ADR-14 旧缓存的样本记录也收进窗口，与新抓样本同一形状

- 日期 · 2026-08-27 · PR #4 第二轮评审意见
- 类型：**事实证伪**（ADR-13 的边界建立在一个我没去核实的假设上）
- 冲击的需求：D8（溯源）· D10（零请求补算）· U7（摘要关联到交付行）
- 结论：**采纳**。ADR-13 的「盘上的样本本身不会被重写」作废

### 触发它的事实

`summaryOf` 把样本记录**自己的说法**发布进交付物：记着几条、`sample_size`、`basis`，
经 `attachAssessments` 挂到每一行。所以一份收窄之前写下的缓存（提供方多返回 16 条）
会在交付物上声称 16 条，而指标是按窗口内的 11 条算的 —— 同一行数据里两个数互相打脸，
而运营两个都看得到。

ADR-13 写那条边界时的理由是「重写历史记录会丢掉当初真实抓到的东西」。
这个理由是假的：那批窗口之外的帖子**从来没有任何计算用过**，而新抓那条路径
（`publicPostSample`）从一开始就把它们丢掉了。真正的不对称是形状：旧缓存与新样本
在盘上长得不一样，收窄之后才一致。

### 结论与边界

- 旧缓存走**与新抓样本同一条** `publicPostSample`，所以「盘上的样本记录」
  只有一种形状，与它是哪一版写下的无关
- 收窄不动 `observed_at` 与 `source`：它们属于当初那次采样
- 样本本身是 `unavailable`（私密、账号不可用）时原样留着，不被改形
- 零请求这一条不变；由自检的「超窗样本记录零请求收窄」守，判定由 M-D8-c 守

### 顺带修掉的一个虚报

同一轮评审指出：`calculatePublicMetrics` 只给 `audience_quality_risk` 放一个占位符，
真值由 `assignAudienceRisks` 跨账号赋。把它算进「数换过没有」的比较，
每一轮都会把「占位符 vs 已赋值」当成变化 —— `locally_recomputed` 于是每轮虚报，
而那正是 ADR-13 里我自己声称「变准了」的数字。比较现在排除这一个字段（M-D10-b 守）。

### 教训

**一条边界如果建立在「下游用不到它」上，先去看下游用没用。**

我当时没看 `summaryOf`。ADR-13 那一节读起来是个有分寸的取舍（不重写历史、不丢证据），
而它其实是一句没核实的猜测 —— 而且被写成了决策记录，比留在代码里更难被质疑。

---

## ADR-15 记忆读不出来时不产出名单；那一份记忆绝不被覆盖

> ℹ️ **本条「教训」第 3 点指出的流程缺口已由 ADR-16 补上**（`2-CHANGE.md` 现在有
> 「需求冲突」和「产品取舍」两类）。原文留着 —— 它是这条缺口被发现时的样子。
>
> ⚠️ **「边界」一节里「`memory_status` 缺失时按 `ok` 处理」那一条已被 ADR-18 推翻。**
> 理由写的是「那时去重是无条件跑的」—— 过滤这一步确实无条件跑，但**本条 ADR
> 修的正是它遇到坏文件会静默失效**，所以「跑过」不等于「生效」。缺失现在读作
> `unknown`。（追加不删改：原文留着，因为它错在哪本身是有用的信息。）

- 日期 · 2026-08-28 · 讨论「需求冲突该由谁裁决」时翻代码发现
- 类型：**事实证伪**（D4 的假设「退化为空记忆是安全的」与 P4 矛盾）。
  但它的形状是新的 —— 不是「需求 vs 现实」，是**「需求 vs 需求」**，
  `process/2-CHANGE.md` 的五分类里没有这一类，见文末
- 冲击的需求：**P4**（红线）· D4 · P5
- 结论：**采纳**。D4 的验收标准改写，两处止血，加一个显式逃生口

### 触发它的事实

四条各自正确的需求，串成一条没人看的路径：

1. **S3** 定下 contacted 由用户手动标记 —— 于是 `docs/business-requirements.md`
   和 `skill/references/memory.md` 都写着「**直接改 JSON**」。
   这个产品要求一个非程序员用手编辑一份 JSON
2. **D4** 规定「记忆文件损坏时退化为空记忆并告警，不中断任务」
3. **P4** 是红线，而它的**全部执行力**来自那份记忆
4. **P5** 要求交付时声明数据边界，但记忆状态从来不在交付物里

跑一遍这条路（截断 `memory/creators.json` 模拟一次写到一半的中断，
或者一个多余的逗号）：

- `filterByMemory` 一个人都不滤，**打扰过、甚至已拉黑的人全部重进名单**
- 交付物上 `filtered_contacted` 是 `0` —— 和「确实没人联系过」同一个值
- SKILL 要求交付时报的那行统计变成「0 人此前推荐过已过滤，0 人已标记联系过」，
  读起来就是一次干净的首轮采集
- 随后 `render` 调 `recordRecommendations`，它 `loadMemory()` 拿到空对象、
  `saveMemory()` 盖回去 —— **contacted 与 blocked 被永久抹掉，`first_seen` 一并重置**。
  `memory/` 不进 git，这是唯一一份副本；原本还能手工修的截断文件，
  被一份合法 JSON 盖掉之后连修的机会都没有。而按 S3，这份数据只有用户自己知道
- 警告打印的是「原文件未覆盖」。这句话在 `filterByMemory` 那条路上是真的，
  在 `render` 这条路上**是假的**，而且它打印了两次

`loadMemory` 里这两行返回的是**字节相同的值**：文件不存在（第一次跑，确实没人）、
文件读不出来（可能有一百个人，我读不出来）。两个不同的真相，一个值 ——
`process/README.md` 总纲说的「把第三档压进否」。

### 六道防线为什么一道都没拦住

| 防线 | 为什么绕过去了 |
|---|---|
| 类型 | 空 `creators` 是 `MemoryFile` 的合法值 |
| 纪律 lint | 没有兜底表达式，是一个 `return` 字面量 |
| 测试 | P4 的用例开头先写一份**合法**记忆再测。损坏路径在套件里不存在 |
| 变异 | M-P4-a/b 锚在过滤逻辑上。逻辑没错，错的是喂给它的东西 |
| 审计 | 按编号逐条数，D4 那行是「测试· 变异·」—— 一个测试都没有，而总判定是 ✓ |
| 交付物 | `meta.json` 没有任何记忆状态字段 |

**计量单位是「每条需求有没有测试」，所以交点上的测试不属于任何一条需求 ——
写了不加分，不写不扣分。** 靠提醒守不住这个，只能靠让交点在计量里有位置。

### 结论与边界

**止血（不进 trade-off，判据 2 直接给了答案）：**

- 记忆读不出来时 `recordRecommendations` 不写回，返回 `written: false` 和原因。
  不写回是**缩范围**（这一轮的推荐记录丢了，而且说出来），覆盖是**降标准**
  （文件看起来完整，其中最要紧的部分是假的）
- `saveMemory` 改成写临时文件再 `rename`。原来的直接盖写是非原子的 ——
  **这个模块既制造这个故障又吞掉它**

**裁决（红线赢，不需要讨论）：**

- `loadMemory` / `filterByMemory` 在读不出来时**抛**，`collect` 以退出码 `2` 结束，
  不产出名单。理由是 `docs/CONVENTIONS.md` 第 3 条的前半句：让错误往上抛。
  为什么不能走后半句（返回一个能表达「我没有资格回答」的东西）—— **名单没有第三态**：
  CSV 里每一行就是「这个人可以发信」，一份标注着「可能不该联系」的可联系名单
  是个自相矛盾的对象
- 中止是**便宜**的：`persist()` 跑在 `finalize()` 之前，采集结果与预算状态都已落盘。
  在这里中止一个 API 请求都不损失，`--resume` 时关键词全在 `done` 里，
  直接走回这一步。**这一条是 A 能当默认的全部理由** —— 不查它就会误以为
  中止会卡死流水线

**收场（这一半是产品取舍，由用户定，我最初擅自替他定了 b）：**

- `--ignore-memory`：必须用户显式打出来才继续。它不让重复打扰的风险消失，
  只是把它**从一个静默默认变成一次显式决定** —— 红线要挡的从来不是
  「这件事绝不许发生」，是「这件事不许在用户不知情的时候发生」
- 那一批的 `memory_status` 是 `unreadable_ignored`（**不得伪装成 `absent`**），
  报告与 `meta.json` 各带一条声明。两件事分开报：这一批可能重复打扰（去重没跑）、
  下一批可能重复推荐（这一批没记下）—— 后果不同，不能合成一句

**边界：**

- `TaskState.memory_status` 缺失时按 `ok` 处理。这只可能来自本次改动之前的
  `collect`，那时去重是无条件跑的 —— 是关于**本仓库自己历史**的判断，
  不是对数据的兜底
- `render` 里「写回在报告之前」是有语义的顺序，但变异只跑 `scripts/test.ts`，
  入口里的接线没有变异守得住（与 ADR-13 同一个处境）。声明的**判定**在
  `lib/report.ts`，由 M-P5-b / M-P5-c 守；**接线**由自检的
  「render 记忆读不出来时不覆盖原文件」那三条断言守
- **没做**：把 `docs/requirements.json` 里那个从 2026-08-25 起就没递增过的
  `version` 字段变成能被机器校验的东西。这次顺手把它改成 2，但**没有任何检查
  会发现下次忘记改** —— 和 ADR-13 驳回缓存版本号时说的是同一件事，仍然待决

### 连带改动

`scripts/lib/memory.ts` · `scripts/lib/pipeline.ts` · `scripts/lib/types.ts` ·
`scripts/lib/report.ts` · `scripts/collect.ts` · `scripts/render.ts` ·
`docs/requirements.json`（D4 改写）· `docs/SPEC.md`（重新生成）·
`docs/ARCHITECTURE.md`（锚点表、缝隙契约、退出码）· `skill/references/memory.md` ·
`skill/SKILL.md` · `scripts/test.ts`（D4 从零个测试到一个套件）·
`scripts/check/mutations.json`（M-P4-c · M-D4-a/b/c · M-P5-b/c）·
`scripts/check/selfcheck.ts`

### 教训

**需求冲突不是某条需求写错了，是几条各自正确的需求串成了一条没人负责的路径。**

D4 想要的**性质**是「记忆不可用不该让整个任务失败」。它写下的是
「退化为空记忆」—— 那是一个**实现方式**，而且在所有满足那个性质的方案里，
它是唯一一个会破红线的。`process/1-REQUIREMENTS.md` 早就写着「需求文档里不要有
实现方式」，这次是那条纪律第一次具体化：**需求里夹带的实现方式，会替下游把一个
本来有三种解法的问题，锁死成跟红线冲突的那一种。**

三条可操作的：

1. **写需求时只需要检查它与红线的交点。** 红线只有五条，这是 O(5) 不是 O(n²)，
   而且红线是全序最高的，交点的结论已经确定 —— 只需要把它写下来
2. **裁决和收场是两个问题。** 红线预先裁决了「什么不能发生」，但「那么该怎么办」
   经常还剩一个真正的产品取舍。压力解除的那一刻最容易把它顺手做掉
3. **`2-CHANGE.md` 的五分类装不下这一类。** ADR-11 当时自己标了「产品取舍」，
   也不在五类里。这两类都该补进去，并写明：**产品取舍默认需要用户，其余默认不需要**

---

## ADR-16 变更分类补上「需求冲突」与「产品取舍」，并写明哪些必须找人

- 日期 · 2026-08-28 · 落实 ADR-15 教训的第 3 条
- 类型：**流程补缺**（分类表两次装不下真实发生的变更）
- 冲击的需求：**无**。不改登记表，只改通用层 `process/`
- 结论：**采纳**

### 触发它的事实

`process/2-CHANGE.md` 的分类表原本五类，而这个仓库里已经有两条 ADR 归不进去：

- **ADR-11** 自己标了「**产品取舍**」——「停更账号是删、是降级、还是只打标签」，
  读多少代码都答不出来，它是价值判断
- **ADR-15** 我标成「事实证伪」，并在括号里注明形状不对：它不是
  「需求 vs 现实」，是「**需求 vs 需求**」

两次都不是分类填错了，是**表里没有那一格**。而分类表的整个作用是
「类型决定处理方式」——一条归不了类的变更，处理方式就只能靠临场判断，
而临场判断发生在压力最大的时刻。

### 结论与边界

`process/2-CHANGE.md`：

- 分类表补两类：**需求冲突**（红线在场则红线赢，不进 trade-off）、
  **产品取舍**（默认要人拍板）
- 开篇从「反向输入」一种来源，改成两种（另一种是冲突）
- 新增「第三步 · 谁来裁决」：四条判据里本来就编码着「谁答」，把它写出来 ——
  **缩范围、降覆盖、不可逆**三个触发要人，其余都有标准答案
- 写明**裁决与收场是两个问题**：红线裁决「什么不能发生」，不裁决「该怎么办」，
  后一半经常还剩一个真正的取舍。压力解除那一刻最容易把它顺手做掉
- 写明**落纸与打断人是两个动作**：落纸无条件且立刻，打断人按三条触发、
  攒到汇报点、带推荐去问。例外只有一个 —— 不可逆且正在发生的，立刻停
- 「为红线冲突去找人裁决，是失职」——它把刻意关闭的决定重新打开；
  且「这两条需求冲突了，你来定」是「实现方便」的高级伪装
- 同步表加一行：**改 `process/` 里任何一份，必须同步各工具入口与 `AGENTS.md.tpl`**。
  这一条是我这次自己漏了、复核时才补上的 —— 路由表指向这里的章节，漏改就是
  转发到一个不存在的地方

`process/1-REQUIREMENTS.md`：

- 新增「新增或修改一条需求时，只查它与红线的交点」。两两查是 n²，做了也流于形式；
  与红线的交点是常数次，而且结论已经确定，只需要写下来
- 「不要有实现方式」补上后果：夹带的实现方式会替下游把一个本有几种解法的问题
  锁死成一种，锁死的那种万一和红线冲突，**下游是照着需求写的，没人觉得自己错了**

`process/README.md`：环节图保证的是**纵向**不漂移，对**横向**什么也没说 ——
交点不属于任何一条需求，所以逐条来的检查（测试、变异、审计覆盖率）都看不见它。

**边界：**

- `process/` 是跨项目层。这次没往里写任何本产品的东西（复核时查过：
  平台名、业务字段、产品词一个都没有）
- **这一层没有机器检查。** `process/README.md` 第二层说得很清楚：能变成检查的
  才是保证，散文只是期望。这次加的全是散文，属于第三层 —— 会因工具、模型、人
  而执行到不同程度。**不假装它被保证了**

### 教训

**一个装不下的分类，代价不是分类不准，是处理方式没有归属。**

分类表的作用是「类型决定处理方式」。一条归不了类的变更，等于让人在当场重新
发明一套处理办法 —— 而那正是最不该临场发挥的时刻。

所以分类表还需要一条规矩：**归不了类本身就是信号**，说明表该改了，
而不是把它硬塞进最像的那一格。ADR-11 和 ADR-15 都硬塞过，塞了两次才有人回头看表。

---

## ADR-17 需求登记表的元数据要么派生要么删；交点在计量里有了位置

> ⚠️ **本条落地的内容指纹当时只覆盖顶层字段** —— 交点的裁决改成相反的意思，
> 指纹纹丝不动。已由 ADR-18 修好。本条声称的「手改需求不回写当场变红」，
> 在修好之前只对顶层字段成立。

- 日期 · 2026-08-28 · ADR-15 边界里留下的待决项
- 类型：**流程补缺**（登记表缺三样东西：可校验的元数据、指回决策的链接、交点的归属）
- 冲击的需求：**无**。不改任何一条需求的文本，只改登记表的结构与检查链
- 结论：**采纳**

### 触发它的事实

四件事凑成同一个形状 —— **写在纸上但没人查，于是慢慢变成假的**：

1. `docs/requirements.json` 第 3 行的 `"version": 1`，2026-08-25 写下之后
   经历 5 次需求改动（新增 D8/D9/D10/F8/U7，重写 U5/U6/P5/D6/D8 的验收标准）
   **一次都没有递增过，也没有任何检查发现**。这正是 ADR-13 驳回缓存版本号时
   说过的话：「版本号把一次性的问题换成永久的问题 —— 忘记递增不报错」
2. 决策记录里有「冲击的需求：编号」，那是**正向**链接。审计据它能回答
   「这条需求被引用了吗」，却回答不了「**这句话是哪次决策的结果**」。
   U5/U6 在 `34ff0ea` 里被就地改写，旧文本只活在 commit message 里
3. `process/1-REQUIREMENTS.md` 早就规定「作废时保留编号并标记作废」，
   而登记表**没有这个字段**。第一次作废发生时才补，一定是赶在最忙的时候补
4. 交点没有归属。审计的计量单位是「每条需求有没有测试」，于是
   **交点上的测试写了不加分，不写不扣分** —— D4 × P4 那个红线失效就是这么活下来的

### 结论与边界

**一、元数据：派生，或者删掉，没有第三条路**

- `version` 删除，换成 `content_hash`（需求内容的 sha256 前 12 位）
- `content_hash` 与 `updated_at` 都由 `spec-sync --write` 写，**没有人需要记得改**
- 手改需求不回写 → `npm run spec` 当场变红，并打印实际指纹
- 指纹只覆盖需求本身，不含它自己和 `updated_at`（否则自我循环）；
  比较用键序无关的序列化 —— 磁盘上的键序由写下它的那一版代码决定（ADR-13 同一个坑）

**二、`adr` 反向链接**

每条需求可以列出**塑造了这条文本**的决策记录 —— 不列所有提到它的（ADR-04 那种
横切的已知缺口不算）。双向都校验：登记表里的 ADR 编号必须存在，
决策记录「冲击的需求」里的编号也必须存在。后者是「编号不回收复用」
唯一查得了的那一半。

14 条需求补上了链接。**U5/U6 没有** —— 它们当初就是在一条 commit 里就地改的，
没有决策记录可指。这个空缺现在是显式的。

**三、`deprecated`**

字段 + 渲染（单独成节，不混进现行表）+ 审计（不计入覆盖率，编号仍然列出）。
目前**没有任何一条需求作废**，所以这条路径靠 harness 测试和 M-H3-a 撑着，
不是靠真实数据。

**四、交点在计量里有了位置**

- 登记表的 `tension`：声明在**让步的那一方**，双向声明会被当场拦下
  （两处各写一遍迟早出现两种说法）
- 裁决渲染进 `SPEC.md` —— 只躺在 json 里等于没写
- 测试里用 `tension('A', 'B')` 认领一个交点，审计逐个找
- **交点里有红线的，没有测试认领就是硬失败** —— 红线第 2 条硬要求的自然延伸

登记了三处交点：**D4 × P4**（ADR-15）、**D4 × D6**（ADR-08）、
**F5 × P1**（裁决在 `docs/CONVENTIONS.md` 第 2 条，没有 ADR）。
第三处此前完全没有测试，这次补上：缺增强层时主流程走完（F5）不等于
给缺失字段填值（P1）。

**边界：**

- 判定抽进 `scripts/check/spec-rule.ts`，`spec-sync.ts` 只剩读写、打印、退出码 ——
  与 `lint-rule.ts` / `why-rule.ts` 同一个理由，也是同一条踩过五次的坑
  （`docs/CONVENTIONS.md` 第 10 条）
- 决策记录里的编号形状**从分类前缀派生**，不写死成「一个大写字母加数字」。
  写死的话，换一个用长前缀的项目，这条检查会安静地一条都匹配不到 ——
  一个永远通得过的检查等于没有检查。这一条是被自己的测试抓出来的：
  fixture 用了两字母编号，检查没反应
- **`tension` 挡不住「该声明而没声明」。** 它只保证已声明的交点有裁决、
  有测试；一个从没被人想到的交点，登记表里不会有它。能提前挡住的只有
  与红线的交点（`process/1-REQUIREMENTS.md` 新增那一节），
  剩下的仍然只能等实现或评审时撞出来

### 连带改动

`scripts/check/spec-rule.ts`（新增）· `scripts/check/spec-sync.ts`（改成只做入口）·
`scripts/check/audit.ts` · `scripts/check/mutations.json`（M-H2-a · M-H3-a · M-H4-a · M-H5-a）·
`scripts/test.ts`（harness 块 + `tension` 助手 + F5 × P1 用例）·
`docs/requirements.json` · `docs/SPEC.md` · `docs/ARCHITECTURE.md` · `docs/SYNC.md` ·
`AGENTS.md` · `process/1-REQUIREMENTS.md`

### 教训

**「以后记得改」不是一种机制，是一种许愿。**

`version: 1` 躺了三天、五次改动、无人发现，而它长得和一个正常工作的字段一模一样。
所有手工维护的元数据都会走到这一步 —— 区别只在于多久，以及是谁在什么时候
为它付代价。

判据只有一条，`process/README.md` 第二层早就写了：
**散文里的纪律是期望，能报错的检查才是保证。** 一个字段如果既不派生、
又没有检查盯着，它的正确性就等于最后一个改它的人的记性。

---

## ADR-18 指纹要看得见嵌套字段；查不到的去重状态记为不知道

- 日期 · 2026-08-28 · PR #5 的自动评审提出两条，都成立
- 类型：**事实证伪**（两处，都是「我以为它在守着，其实它没有」）
- 冲击的需求：**D4**（补 `unknown` 一档）· P5（多一条声明）
- 结论：**采纳**，两条都改

### 一、内容指纹看不见嵌套字段

`JSON.stringify(value, keys)` 的白名单参数是**逐层生效**的。ADR-17 里拿顶层键
当白名单，于是每一个嵌套对象都被同一张表过滤：

```
{"accept":"a","cat":"D","id":"D4","pri":"P1","tension":[{}],"text":"t"}
                                              ^^^^^^^^^^^^
```

**交点整个被序列化成一个空对象。** 实测：把 `tension.ruling` 改成意思完全相反的
一句话、把 `tension.with` 改成指向另一条需求、把 `deprecated` 的理由整段换掉 ——
指纹三次都纹丝不动。

讽刺的地方在于：**这些恰恰是 ADR-17 那一轮新加的、最需要被守住的内容。**
我加了一道检查、加了测试、加了变异，然后让它对我新加的字段全盲 ——
而它看起来在工作，因为改 `accept`（顶层字段）确实会让它变红。

我的 harness 测试也没抓到，因为那条用例改的正好是顶层的 `accept`。
**一个用例只覆盖了判据成立的那一半，看起来和覆盖全了一模一样。**

修法：递归排序键的规范化，不用白名单参数。新增用例分别改嵌套的
裁决、指向、作废理由，M-H2-b 守着。

### 二、旧任务目录的去重状态被当成「没问题」

ADR-15 的边界写着：`memory_status` 缺失时按 `ok` 处理，理由是
「那时去重是无条件跑的」。

**这个理由是假的,而且假在一个我本该最清楚的地方**：过滤这一步确实无条件跑，
但 ADR-15 修的正是它**遇到读不出来的记忆会静默失效**。所以对一份早期版本
产出的任务目录，「跑过」和「生效了」之间隔着的正是那个 bug ——
到底去没去重，事后查不出来。

把它读成 `ok`，就是拿一个查不到的事实当肯定答案 —— 这个仓库总纲里
排第一位的事故形态，而我在修这个事故的同一次改动里又犯了一遍。

修法：`MemoryStatus` 加第四档 `unknown`；`render` 把缺失读作 `unknown`；
报告出一条不同措辞的声明（说「无从确认」，不说「你跳过了」），
并给出拿到确定答案的办法（重跑一次采集）。D4 的验收标准补上这一档。

`FilterResult.memory_status` 的类型排除 `unknown` —— 那一档是**记录的缺失**，
不是**观察的结果**，当场观察不可能得到「不知道」。

### 连带改动

`scripts/check/spec-rule.ts` · `scripts/lib/types.ts` · `scripts/lib/memory.ts` ·
`scripts/lib/pipeline.ts` · `scripts/lib/report.ts` · `scripts/render.ts` ·
`docs/requirements.json`（D4）· `docs/SPEC.md` · `scripts/test.ts` ·
`scripts/check/mutations.json`（M-P5-d · M-H2-b，并修好被改动打断的 M-H2-a）·
`scripts/check/selfcheck.ts` · `skill/references/memory.md` ·
`skill/references/output-format.md`

### 教训

**两条都是「我加了一道检查，然后没检查这道检查看得见什么」。**

- 指纹那条：加了检查、加了测试、加了变异，而测试用例恰好落在判据成立的那一半，
  于是三层防线一起放行
- `unknown` 那条：我在 ADR-15 里亲手写下一条边界，理由用的是被这次改动否定掉的
  旧行为。**决策记录里的一句猜测，比留在代码里更难被质疑** —— ADR-14 的教训，
  隔了三条记录又踩一次

共同点是：**新加的守卫要问一句「它看不见什么」，而不是「它抓到了什么」。**
抓到了什么由已有的用例回答，那是自证；看不见什么得主动去找，
而最省事的找法是**拿新加的字段本身去试**。

---

## ADR-19 解析成功不等于形状对；写不进去不等于交付失败

- 日期 · 2026-08-28 · PR #5 第二轮自动评审，两条都成立
- 类型：**事实证伪**（一条是旧实现一直有的洞，一条是我这次改动**新引入**的回归）
- 冲击的需求：**P4**（红线）· D4
- 结论：**采纳**，两条都改

### 一、合法 JSON、错误结构 —— 同一条红线的另一扇门

`JSON.parse` 成功，不等于拿到的是一份记忆。而这个文件是产品**明确要求运营手改**的
（S3 → `business-requirements.md` 写着「直接改 JSON」），手改最容易改出的恰恰是
**能解析但形状错**的东西。实测：

| 文件内容 | 修之前 | 修之后 |
|---|---|---|
| `{"version":1,"creators":[]}` | `status: ok`，**所有人进名单** | 拦下：creators 不是键值对象（是数组） |
| 条目缺 `contacted` | `status: ok`，**该人进名单** | 拦下：tiktok:alice 的 contacted 不是 true 或 false |
| `contacted: "true"`（字符串） | `status: ok`，**该人进名单** | 同上 |
| `recommendations: null` | `status: ok` | 拦下 |

第一行最典型：花括号改成方括号。字符串取键在数组上一个都取不到，于是
**打扰过、已拉黑的人全部重新进名单，而报告会说去重成功了** —— 与 ADR-15
修的是同一个红线失效，只是走的另一扇门。ADR-15 只堵住了「解析不了」那一扇。

修法：解析后过一道结构校验，不合格归入 `unreadable`。

- **只校验过滤真正会读的字段**：`contacted`、`blocked`（决定 P4）、
  `recommendations`（决定跨任务去重）。不做全量 schema —— 运营自己加的备注字段
  不该被判成损坏（有用例守着）
- **报错指名哪个人的哪个字段**。修它的是人，「结构不对」四个字帮不上忙

### 二、写不进去时，整个交付被一起丢掉 —— 我自己引入的回归

ADR-15 把写回从 render 末尾挪到了报告之前，**为的是让报告能声明写回的结果**。
代价没想到：`saveMemory` 在目录不可写、磁盘满、路径被占时会抛，而它现在跑在
`meta.json` 与 `report.html` 之前 —— 于是 CSV/XLSX 已经落盘，报告却没了。

挪之前，写回在最末尾，抛出来只损失记录，交付物完好。**我把一个局部失败改成了
全局失败，而且是在一次以「交付要说实话」为目的的改动里。**

修法：写回失败与读不出来走同一条路 —— 返回 `written: false` 加原因，交付照常
完成，报告上照常声明。判据是 `2-CHANGE.md` 的第 2 条：交付不出来是**降标准**
（用户什么都拿不到），少记一轮推荐是**缩范围**（东西少了但剩下的是对的）。
顺带：`saveMemory` 失败时清掉临时文件，不在盘上留半成品。

### 连带改动

`scripts/lib/memory.ts` · `docs/requirements.json`（D4 两处）· `docs/SPEC.md` ·
`docs/ARCHITECTURE.md` · `skill/references/memory.md` · `scripts/test.ts` ·
`scripts/check/mutations.json`（M-P4-d · M-D4-e，并修好被改动打断的 M-D4-c）

### 教训

**「同一个失效，还有没有别的门」是一个该单独问出口的问题。**

ADR-15 找到的是「文件读不出来 → P4 静默失效」。我修好了那扇门，写了测试、
写了变异、写了 ADR，然后**没有回头问一句：这个红线还能怎么失效？**
`JSON.parse` 成功这条路一直敞着，而它比我修的那扇门更好走 ——
坏成解析不了需要运气，坏成形状不对只需要按错一个键。

第二条的形状不一样，值得单记：**为了让交付说实话而做的改动，让交付变得做不出来。**
挪动一次调用的位置是最不像改动的改动，所以那一步没有被当作改动来审
—— 它改的不是行为，是**失败的作用域**。

---

## ADR-20 交点认领改成运行时收集；写回失败不再被说成文件坏了

- 日期 · 2026-08-28 · PR #5 第三轮自动评审，两条都成立
- 类型：**事实证伪**（一条把我上一轮亲手立的规矩变成了空头承诺，一条是 ADR-19 的余波）
- 冲击的需求：**P4**（守它的机制失效）· D4 · P5
- 结论：**采纳**，两条都改

### 一、注释掉的认领照样算数 —— 硬失败被一句注释绕过去

ADR-17 立了一条规矩：**含红线的交点没有测试认领就是硬失败**。
审计的实现是按源码正则找 `tension('A', 'B')`。于是：

```ts
  // tension('P4', 'D4')   ← 测试删了，认领留在注释里
```

审计照样报 `✓ D4 × P4`、`交点 3 处 · 有测试认领 3`、全绿通过 ——
**红线交点一个测试都没有，而我上一轮承诺的硬失败一次都不会响。**

最难堪的是，这个坑仓库里有现成的记录，就在 `audit.ts` 同一个文件里：

> 只认 `S('xxx.ts')` 这个精确形式。早先还 or 了一个 `includes(base)` 兜底，
> 结果是 selfcheck.ts 里**任何地方**提到文件名（哪怕注释里）就算「执行过」——
> 前面那个精确判据变成死代码，整条检查形同虚设。

我在写着这段注释的文件里，又建了一个同样形状的检查。

修法不是把正则写得更聪明（注释挡住了，字符串、`if (false)` 里的调用还在），
而是**换一层**：

- `tension()` 在**运行时**把认领记进一个数组，测试结束时连同覆盖到的编号
  一起写进 `.check-cache/test-claims.json`
- 审计读这份记录，不再看源码。**没执行的就是没有**
- 记录带 `scripts/test.ts` 的指纹，审计重算并比对 —— 一份过期的记录
  不能替一段已经改掉的代码作证。记录不存在或对不上，审计当场说
  「先跑 `npm test`」，而不是默默放行

顺带把 `suite()` / `covered.add()` 的覆盖统计也换成运行时记录 ——
它们是同一个形状，而 `covered` 这个集合本来就是运行时的，接过来几乎零成本。

### 二、写不进去被说成文件坏了

ADR-19 让 `written: false` 有了第二个原因（目录不可写、磁盘满），
而报告上的那句话是 ADR-15 写的，只考虑了第一个原因：

> 本次推荐未记入跨任务记忆（**记忆文件读不出来**，原文件未被覆盖）——
> 在**修好它**之前……

磁盘满的人会拿着这句话去修一份根本没坏的 JSON。

修法：`meta.json` 带上 `memory_write_error`（原因原文），报告改成
不预设原因的措辞并把原文带给用户。**两类原因要去修的地方完全不同** ——
读不出来去修 JSON，写不进去去看权限或磁盘。M-P5-e 守着。

### 连带改动

`scripts/check/claims.ts`（新增）· `scripts/check/audit.ts` · `scripts/test.ts` ·
`scripts/lib/report.ts` · `scripts/render.ts` · `docs/requirements.json`（D4）·
`docs/SPEC.md` · `docs/ARCHITECTURE.md` · `skill/references/output-format.md` ·
`scripts/check/mutations.json`（M-P5-e）· `process/4-VERIFY.md` · `.gitignore`

### 教训

**要「某段代码跑过」的证据，就别去源码里搜它。**

搜出来的东西没有执行语义：注释、字符串、走不到的分支，全都算数。
这条已经写进 `process/4-VERIFY.md` 的「判据的失效方式」那张表 ——
那张表存在的全部意义就是记下这类形状，而这一条本来就该在上面。

更值得记的是**它发生在什么时候**：我在建立一条新纪律的同一次改动里，
用一个自己刚记过教训的手法去实现它。**新立的规矩最脆弱，因为立规矩的人
正忙着论证它为什么必要，没空问它能不能被绕过。**

所以立一条硬失败的规矩时，配套要问一句：**怎么在不触发它的情况下违反它？**
答得出来，那就是这条规矩的真实强度。

---

## ADR-21 容器对了不等于里面对；生成物与源一致不等于两边装着同一批东西

- 日期 · 2026-08-28 · PR #5 第四轮自动评审，两条都成立
- 类型：**事实证伪**（两条都是「校验只做到了一层」）
- 冲击的需求：**D4** · 登记表与 `SPEC.md` 的一致性机制
- 结论：**采纳**，两条都改

### 一、推荐记录只校验了容器，没校验元素

ADR-19 加了结构校验，但 `recommendations` 只查到 `Array.isArray` 为止。
而 `filterByMemory` 会**逐条**读元素的 `task` / `product` / `date`：

| 文件内容 | 修之前 | 修之后 |
|---|---|---|
| `"recommendations": [{}]` | `status: ok`，「已推荐过」这件事悄悄消失，名单上留下一个 `undefined @ undefined` | 拦下：第 1 条推荐记录的 product 不是字符串 |
| `"recommendations": [null]` | **抛 `TypeError`** —— 绕开整条「退出码 2 + 一句人照做得了的报错」的流程 | 拦下 |

第二行尤其要紧：ADR-19 建的那条可操作报错路径，被一个 `null` 直接跳过，
用户拿到的是一句读不懂的 `Cannot read properties of null`。

修法：逐条校验元素 —— 必须是对象，`product` 与 `date` 必须是字符串，
`task` 若存在必须是字符串。仍然不做全量 schema（运营在推荐记录里自己加的
批注不该被判成损坏，有用例守着）。

### 二、分类表里没有的 `cat`，那条需求从渲染里凭空消失

`renderTables` 按分类表逐个分类挑需求。一条 `cat` 拼错、或用了没声明的前缀，
**整条被跳过**——而：

- 内容指纹**包含**它（它确实在登记表里）
- SPEC 一致性检查**全绿**

因为那条检查比的是**生成结果和生成结果**：漏掉的那条与它自己完全一致。
于是「`SPEC.md` 是 `requirements.json` 的渲染」这句承诺失效，
而两道检查都说没问题。

修法：`validateRegistry` 收下分类前缀，`cat` 不在表里当场报错。

### 连带改动

`scripts/lib/memory.ts` · `scripts/check/spec-rule.ts` · `scripts/check/spec-sync.ts` ·
`docs/requirements.json`（D4）· `docs/SPEC.md` · `scripts/test.ts` ·
`scripts/check/mutations.json`（M-D4-f · M-H4-b）· `process/4-VERIFY.md`

### 教训

两条是同一句话的两半：**校验停在了它看得见的第一层。**

- 数组是数组了，元素没看
- 分类挑得对，没问过「有没有谁一个分类都不属于」

这两条都进了 `process/4-VERIFY.md` 的「判据的失效方式」那张表，
连同上一轮的「源码里搜到 = 做过了」。**那张表这一轮长了三行,
全是同一次评审喂出来的** —— 这本身是个信号：

> 那张表是靠**被抓到**长出来的，不是靠想出来的。
> 所以「谁来抓」不是运气问题，是流程问题。

这已经是外部评审连续第四轮抓到东西，而我每一轮都跑了全套检查链、
写了测试与变异、做了自审 diff。ADR-04 登记的同源污染不是一个静态的缺口，
它在持续产出缺陷，而目前把它挡下来的**唯一**东西是一个独立上下文的评审者。
要不要把这件事从运气变成流程，是流程层的决定，已向用户提出，尚未答复。

---

## ADR-22 身份规范化两侧必须一致；错误信息不许替下一步打包票

- 日期 · 2026-08-28 · PR #5 第五轮自动评审，三条都成立
- 类型：**事实证伪**（一条是 D1 从未被真正实现，两条是我这几轮改动的余角）
- 冲击的需求：**P4**（红线）· **D1** · D4 · 登记表的派生元数据
- 结论：**采纳**，三条都改。新登记一处交点 **D1 × P4**

### 一、键的大小写让 P4 整个失效

`D1` 写着：**创作者的唯一标识是 platform 与 handle 的组合，大小写不敏感。**

实现里，**查询侧**一直在小写化（`key()` 把 handle 转小写），**存储侧**没有。
于是一份手改出 `tiktok:Alice` 的记忆：

| 键 | 修之前 | 修之后 |
|---|---|---|
| `tiktok:alice` | 已联系的人被挡住 | 同左 |
| `tiktok:Alice` | `status: ok`，**已联系的人进名单** | 被挡住（键规范化） |
| `alice` | `status: ok`，**已联系的人进名单** | 拦下：不是 platform:handle 的形式 |

D1 的验收标准写的是「同一 handle 的不同大小写形式在**去重与记忆查询**中被视为
同一人」—— 它一直是假的，而 D1 有测试、测试也一直是绿的：**那条测试测的是
去重那一半，没测记忆查询那一半。**

修法：

- `key()` 平台也小写 —— 两侧的规则逐字一致，不留潜在分歧
- 读盘时规范化所有键；写回去的自然也是规范形式（手改出来的大小写就地纠正）
- 两类键不规范化，直接判读不出来：**形状不对**（少冒号、两边任一为空，
  它永远匹配不到任何人，是个静默黑洞）、**规范化后撞键**（两条记录说法可能相反，
  该信哪条无从判断，而**答不上来时不许猜**）

**新登记交点 D1 × P4**：身份规范化是 P4 的承重结构，规范化错了红线直接失效，
而两条需求各自的测试都看不见这件事。这正是 ADR-17 建那套交点机制要接住的东西 ——
这次它接住了（`tension('D1', 'P4')`）。

### 二、错误信息替下一步打了一张空头支票

ADR-15 在 collect 的中止分支里写了一句「采集结果都在 `{dir}`，**续跑不会重复花费**」。
它成立的前提是采集已经跑完 —— 而这个分支同样会在**采集失败**（网络错误）
或**预算用尽**时到达。那时：

- `stopped` 与 `errorMsg` 记着的采集失败**被这条错误盖掉**，用户根本不知道
- 还有关键词没跑完，续跑**要继续花钱**，而信息说的是不会

「已抓到的不会重抓」被写成了「续跑免费」。修法：把采集侧的失败一并报出来，
并按 `pendingKeywords` 的实际数量说话 —— 没跑完就明说「还有 N 个关键词，
续跑会继续花钱」。

### 三、分类表不进内容指纹

`contentHash` 只吃 `requirements`。只改一个分类的中文说明：渲染出来的
`SPEC.md` 变了，而 `content_hash` 与 `updated_at` 纹丝不动 ——
ADR-17 立的「派生元数据」就此名不副实。修法：分类表一起进指纹。

### 连带改动

`scripts/lib/memory.ts` · `scripts/collect.ts` · `scripts/check/spec-rule.ts` ·
`scripts/check/spec-sync.ts` · `docs/requirements.json`（D1 交点 + 反向链接）·
`docs/SPEC.md` · `scripts/test.ts` · `scripts/check/mutations.json`
（M-P4-f · M-D4-g · M-H2-c）· `scripts/check/selfcheck.ts` ·
`skill/references/memory.md`

### 教训

**一条需求有测试，不等于它验收标准里的每一半都有测试。**

D1 的验收标准有两半：「去重中视为同一人」和「记忆查询中视为同一人」。
测试覆盖了前一半，审计据此报 `D1 测试✓`，而后一半从来没实现过 ——
**一条需求的覆盖是按编号计量的，而编号底下的连词「与」把两件事捆成了一件。**

所以验收标准里出现「与」「且」「同时」的地方，是覆盖率最容易虚报的地方：
它们在计量上是一条，在事实上是两条。

第二条的形状单独记：**错误信息里的每一句承诺，都要问一遍它在哪些路径上成立。**
「续跑不会重复花费」在写下它的那条路径上是真的，而那个分支还有另外两条进入方式。
错误信息不是散文，它是给人照着做的指令 —— 说错的代价和代码说错一样。

---

## ADR-23 承诺改了代码没改文档；分类填成另一个合法分类，红线会静静少一条

- 日期 · 2026-08-28 · PR #5 第六轮自动评审，两条都成立
- 类型：**事实证伪**（一条是上一轮改了一半，一条是上一轮的校验只做到一半）
- 冲击的需求：**P4**（守它的检查会失效）· P3（预算相关的错误承诺）
- 结论：**采纳**，两条都改

### 一、代码改准了，两份文档还在说旧话

ADR-22 把 collect 那句无条件的「续跑不会重复花费」改成按实际剩余关键词说话。
但同一句承诺还写在两个地方，**都没跟着改**：

- `docs/ARCHITECTURE.md` 的缝隙契约：「修好后 `--resume` 不产生任何新请求」
- `skill/SKILL.md`：「让用户修好文件再 `--resume`，零额外花费」

第二处最要命：**`SKILL.md` 是 Agent 读的**。代码会如实说「还有 N 个关键词
没跑完，续跑会继续花钱」，而 Agent 照着 `SKILL.md` 向用户转述「零额外花费」。
**用户拿到的是那句错的**，因为他听 Agent 说话，不看 stderr。

修了两处文案，并且给同步表加了一行：**改给用户看的报错里的一句承诺，
要改复述同一句话的每一份文档** —— 尤其是给自动化执行者的那一份。

这一条的形状不是「忘了同步」，而是：**代码里的一句话和文档里的一句话是同一个
承诺的两个副本，而只有一个副本有测试。**

### 二、分类填成另一个合法分类 —— 红线会静静少一条

ADR-21 加了「分类必须在分类表里」。但**填成另一个已声明的分类**照样通过。
把 `P4` 的 `cat` 改成 `"D"`：两个值各自合法，于是：

```
✓ P4  D  测试✓ 变异✓  引用 10 处
红线 4 条 · 有测试 4 · 有变异 4 · 显式豁免 1
✓ 审计：红线全部有测试且被变异验证
```

**审计报出「红线 4 条」，然后说全部通过。**

而下游全都按 `cat` 分流：`renderTables` 把 P4 挪进数据那一节，
`audit.ts` 的红线集合是 `cat === 'P'` 算出来的 —— 于是「每条红线必须有测试
和变异」这条硬要求，从此不管 P4。`SPEC.md` 的散文还写着「红线为什么是这五条」，
而表里只剩四条，**没有任何检查会发现这件事**。

修法：编号前缀必须与分类一致（`^{cat}\d+$`）。这条规矩也写进了
`process/1-REQUIREMENTS.md` —— 它是通用的：只要下游按分类分流，
分类填错就等于把需求换了一类。

### 连带改动

`scripts/check/spec-rule.ts` · `docs/ARCHITECTURE.md` · `skill/SKILL.md` ·
`docs/SYNC.md` · `process/2-CHANGE.md` · `process/1-REQUIREMENTS.md` ·
`scripts/test.ts` · `scripts/check/mutations.json`（M-H4-c）

### 教训

**「值是合法的」和「值是对的」之间，隔着一整类事故。**

ADR-21 那一轮我加的是「分类必须存在」——它挡住了拼错，挡不住填成另一个
真实存在的分类。同一个形状这轮出现了两次：

- 分类 `D` 是合法的，只是不该是 P4 的
- 「零额外花费」是一句合法的中文，只是不该在有剩余关键词时说

所以对每一个「必须是合法值」的校验，还要再问一句：
**一个合法但错误的值会造成什么？** 如果答案是「下游会静默换一条路走」，
那这个校验就只做了一半 —— 它得再回答「这个值和别的什么必须对得上」。

第一条另记一句：**一句承诺在代码和文档里各有一份副本时，只有代码那份有测试。**
文档那份靠人记得，而这轮证明了记不住 —— 尤其当那份文档是给另一个执行者
照着转述的，错的那份会比对的那份更早到达用户。

---

## ADR-24 红线改动必须过独立复核；验收标准拆成可独立计量的判据

- 日期 · 2026-08-28 · 用户对 ADR-21 与 ADR-22 提出的两个流程问题作出决定
- 类型：**流程补缺**（两条都是把已知缺口从「登记在案」变成「有对策」）
- 冲击的需求：**无**。不改任何一条需求要什么，只改验收标准的**形状**与它的计量方式
- 结论：**采纳**，两条都做

### 触发它的事实

PR #5 的六轮自动评审，抓到 13 条，其中 **4 条是红线级别的静默失效**：

| 轮次 | 红线怎么失效的 |
|---|---|
| 一 | 指纹对新加字段全盲（守 P4 的检查失效） |
| 二 | 合法 JSON、错误结构 —— P4 的另一扇门 |
| 三 | 交点认领可以只留在注释里，硬失败被一句注释绕过 |
| 五 | **D1 从未被实现** —— 键的大小写让 P4 整个失效 |

**每一轮我都跑了全套检查链、写了测试与变异、做了自审 diff，每一轮都报告全绿。**
ADR-04 登记的同源污染不是一个静止的缺口，它在持续产出缺陷。

第五轮那条尤其说明问题：D1 的验收标准是「在**去重与记忆查询**中被视为同一人」，
测试只覆盖了前一半，审计据此报 `D1 测试✓`，而后一半从来没实现 ——
**计量单位（每条需求）比事实（每条判据）粗，粗出来的那部分就是虚报。**

### 一、红线改动必须过一次独立复核

写进 `process/4-VERIFY.md`「它抓不到同源污染」那一节之后，作为**硬要求**
（原先只有「重型版 / 日常版」两条建议）：

> 改动触及红线、或触及守红线的那些检查时，合并之前必须由一个
> **没有参与写这段代码**的上下文复核一遍。

要点：

- **作者报告全绿不构成证据。**全绿只说明「我想到的那些都过了」，
  而同源污染的定义就是「我没想到的那一半」
- **变异测试也不构成证据** —— `4-VERIFY.md` 上一节自己写着它抓不到同源污染
- **不替代测试和变异**：测试与变异守「写下来的判据成立」，
  独立复核补「该写而没写的判据」
- **独立性来自上下文隔离，不来自复核者是谁** —— 人还是自动化的评审者都行
- 哪一半机器守得住：「没有复核记录不能合并」由协作平台的规则守；
  「复核者真的独立」没法观测，是第三层；复核意见按 `2-CHANGE.md` 评定，
  不许悄悄消化

### 二、验收标准拆成可独立计量的判据

`accept` 从一段话改成一组判据，每条有稳定编号 `{需求编号}.{字母}`：

- 35 条需求 → **80 条判据**，其中红线 10 条
- 判据编号与需求编号同规矩：**不改含义、不回收复用**（下游按它认领覆盖）
- 测试用 `criterion('D1.a')` 认领，与交点认领同一套**运行时收集**机制
  （ADR-20：源码里搜出来的东西没有执行语义）
- 审计逐条数：**红线的每一条判据没有认领就是硬失败**；
  一条需求认领了一部分、漏了另一部分，报缺口 —— 那正是 D1 的形状

**拆到多细，只有一条判据：**

> 这两半会不会被**不同的代码路径**独立满足、或者独立弄坏？

会就拆，不会就留。机械按标点切会造出一堆没有独立含义的碎片 ——
**拆分的目的是让「漏掉一半」变得看得见，不是让条目变多。**

**两条红线判据没有认领，据实登记豁免，没有伪造：**

- `P2.a`（草稿里的产品事实是否真的来自产品页）—— 由 Agent 的判断决定，
  不经过任何代码路径。第三层，靠 `outreach-draft.md` 守
- `P3.b`（collect 捕获后保存断点并退出码 3）—— 是**入口的接线**，
  不是 lib 里的判定；变异只跑 `scripts/test.ts`（与 ADR-13 同一处境）。
  由自检的「collect 预算用尽保存断点」真跑一遍守着

### 连带改动

`process/4-VERIFY.md` · `process/1-REQUIREMENTS.md` · `docs/SYNC.md` ·
`docs/requirements.json`（80 条判据）· `docs/SPEC.md` ·
`scripts/check/spec-rule.ts` · `scripts/check/claims.ts` ·
`scripts/check/audit.ts` · `scripts/test.ts` ·
`scripts/check/mutations.json`（M-H4-d + 两条判据级豁免）

### 教训

**已知缺口登记在案，不等于它被管住了。**

ADR-04 三个月前就写下了「测试与实现同上下文写成」，格式规范、理由充分、
显式留在审计报告里。而这六轮说明：**一个只被登记、没有对策的缺口，
会以稳定的速率产出缺陷**，而每一次都长得像一个新问题。

「显式留下缺口，不要消灭它」这条原则本身没错 —— 错的是把「登记」当成了终点。
登记只解决「不假装它不存在」，不解决「它每天在吃掉什么」。
所以缺口记录还该带一栏：**这个缺口目前靠什么兜着？如果答案是「没有」，
那它就不是一个缺口，是一个正在发生的损失。**

---

## ADR-25 「有个冒号」不等于键能用；「关键词跑完了」不等于续跑不花钱

- 日期 · 2026-08-28 · PR #5 第七轮自动评审，两条都成立
- 类型：**事实证伪**（两条都是上一轮的修复只覆盖了它看得见的那一种情况）
- 冲击的需求：**P4**（红线）· D4 · D6 · P3（预算相关的承诺）
- 结论：**采纳**，两条都改

### 一、键的黑洞还有三扇门开着

ADR-22 加了键的规范化与形状校验，判据是「有一个冒号，两边都不空」。
**那只挡住了我当时想到的那一种畸形。** 实测：

| 键 | 修之前 | 修之后 |
|---|---|---|
| `tikok:alice`（平台拼错） | `status: ok`，**已联系的人进名单** | 拦下 |
| `tiktok:alice:old`（多一个分隔符） | `status: ok`，**已联系的人进名单** | 拦下 |
| `youtube:alice`（不支持的平台） | `status: ok`，**已联系的人进名单** | 拦下 |

判据本身就写错了方向：我问的是「这个键长得像不像键」，
**该问的是「查询侧生成的键是什么形状」** —— 那边生成的永远是
`{支持的平台}:{handle}`，恰好一个分隔符。任何别的形状都匹配不到，
都是黑洞。

修法：分隔符恰好一个、平台必须在支持范围内。
顺带把平台的**运行时列表与类型**改成从同一处派生
（`const PLATFORMS = [...] as const` + `typeof PLATFORMS[number]`）——
手写两份迟早分叉，而分叉的后果正是「校验放行一个查询侧永远匹配不到的值」。

### 二、「关键词跑完了」不等于续跑不花钱

ADR-22 把中止时那句承诺改成按 `pendingKeywords` 说话。
**但没干完的活有两种。** 关键词全跑完之后，`--resume` 第一件事是跑
`enrichProfiles`，它对每个 `bio` 未查询或没取到外链的人调**付费**端点。
于是「采集已跑完，续跑不产生新的请求」在这种情况下**仍然是假的**。

修法：把「这个人还要不要补 profile」抽成 `needsProfile`，
**补全循环与那句话共用同一个判定**，消息按两种剩余工作量一起说。

抽出来的第二个理由比第一个更实际：两处各写一份 `c.bio === undefined || …`，
迟早有一边先改，而**先改的那边不会报错**。

### 连带改动

`scripts/lib/types.ts`（`PLATFORMS`）· `scripts/lib/memory.ts` ·
`scripts/lib/pipeline.ts`（`needsProfile`）· `scripts/collect.ts` ·
`docs/requirements.json`（D4.d 改写、新增 D6.c）· `docs/SPEC.md` ·
`scripts/test.ts` · `scripts/check/selfcheck.ts` ·
`scripts/check/mutations.json`（M-P4-g · M-P4-h · M-D6-d）

### 教训

**修一个「漏了一种情况」的缺陷时，最容易犯的错是只补上被指出的那一种。**

两条都是这个形状，而且都发生在我刚刚为同一个问题写过 ADR 之后：

- ADR-22 补了键的校验 —— 补的是「我当时想到的那种畸形」
- ADR-22 补了续跑的代价 —— 补的是「我当时想到的那种剩余工作」

区别在于问题问得对不对：

> 问「这个输入长得对不对」，答案取决于我能想到几种畸形。
> 问「**消费它的那一侧要求什么形状**」，答案是有限的、可以穷举的。

第二条同理：不去数「有哪些活没干完」，而去问「**续跑会调用哪些付费路径**」——
后者在代码里是可以数清楚的，前者取决于我记得几种。

**把判据从「像不像」换成「对面要什么」，是这两条修复真正的内容。**

---

## ADR-26 存在性判断吞掉了权限错误；审计自己的判定抽出来交给变异守

- 日期 · 2026-08-28 · PR #5 第八轮自动评审，三条都成立
- 类型：**事实证伪**（三条都落在我这几轮刚建的东西上）
- 冲击的需求：**P4**（红线）· D4 · 审计的计量机制
- 结论：**采纳**，三条都改，并把审计的判定抽进可被变异守住的模块

### 一、`existsSync` 把「读不到」说成了「不存在」

`readMemory` 用 `if (!existsSync(FILE))` 分出 `absent`。而 **`existsSync`
对权限不足、父路径不是目录、IO 错误统统返回 `false`** —— 于是：

```
读不到（非 ENOENT）→ status=absent，名单照出=true
```

名单照常产出，`memory_status` 报的是「查过，记忆里确实没人」。
**刚刚拆开的三档，被一个存在性判断压回了两档**，而这一压正好压在 P4 上。

修法：直接读，**只有 `ENOENT` 才是 `absent`**，其余都是「没查到」。

### 二、同一个方向的交点声明两次

`validateRegistry` 只查了反向重复（A 声明 B、B 又声明 A），
没查同方向重复。一条需求把同一个 `with` 写两次、两条裁决互相矛盾，
校验放行；审计渲染两行，而运行时**一次** `tension('A','B')` 认领
同时算到两行头上 —— **一个测试证明了两条相反的裁决。**

### 三、一条判据都没认领，反而干干净净

判据缺口的条件写的是 `claimed.length && claimed.length < r.accept.length` ——
**只报「认领了一部分」那种**。于是一条判据都没认领的需求拿到 `✓` 和
`判据 0/N`，一条待办都没有。更糟的是它的激励方向：

> **把仅有的那条认领删掉，缺口就消失了 —— 这个检查在奖励删证据。**

修法：任何未认领且未豁免的判据都是缺口，按需求聚合成一行报出来。
改完之后审计如实多出几十条待办，红线仍然全绿。

### 四、顺带：审计自己的判定抽进 `spec-rule.ts`

这已经是**第三条落在 `audit.ts` 里的问题**（前两条是 ADR-20 的
「注释掉的认领照样算数」和上面第三条），而 `audit.ts` 的判定
**没有任何测试或变异守得住** —— 变异只跑 `scripts/test.ts`。

按 `docs/CONVENTIONS.md` 第 10 条（这条路已经踩过三次）把每条需求的裁定
抽成 `requirementVerdict(r, evidence)`：输入全是算好的事实，不读盘、不扫源码，
因而可被测、可被变异。`audit.ts` 只剩读盘、拼接、打印、退出码。
新增 M-H6-a / M-H6-b 守它。

### 五、一个存活的变异，抓出了一条已经失效的测试

改完第一条之后，`M-D4-e`（写不进去时把整个交付一起丢掉）**存活了**。
原因：那条测试用「父路径是文件」构造写失败，而第一条修完之后
`readMemory` 会在更早的地方就判定读不出来，`saveMemory` 根本没跑到 ——
**测试还在，绿着，但它已经不测那件事了。**

重建成真正「读得出来但写不进去」的场景：在写回用的临时文件名上放一个目录。
这正是 `4-VERIFY.md` 说的变异测试的价值：它抓的是「什么都没断言的测试」，
而一条因为上游改动而悄悄失效的测试，就是这种。

### 连带改动

`scripts/lib/memory.ts` · `scripts/check/spec-rule.ts`（`requirementVerdict`）·
`scripts/check/audit.ts` · `scripts/test.ts` ·
`scripts/check/mutations.json`（M-P4-i · M-H5-b · M-H6-a · M-H6-b）

### 教训

**一个检查的激励方向，比它当下抓到了什么更要紧。**

第三条最值得记：它不是「漏报」，是**报得越少越干净**。
一条测试删掉之后审计更绿，那这个检查就在教人删测试 ——
而且删的人不会觉得自己在作弊，因为报告确实变好看了。

所以给检查写完判据之后，还要问一句：
**有没有哪种「什么都不做」或者「删掉一点东西」，会让这个检查更满意？**
有，就是这条判据写反了方向。

---

## ADR-27 同一句承诺的第三份副本 —— 按记忆去搜，搜不到不等于没有

- 日期 · 2026-08-28 · PR #5 第九轮自动评审
- 类型：**事实证伪**（ADR-23 立的同步规矩，我自己执行时漏了一份）
- 冲击的需求：P3（预算相关的承诺）
- 结论：**采纳**

### 触发它的事实

ADR-23 发现「续跑不会重复花费」这句承诺在代码和两份文档里各有一份副本，
改了代码没改文档；于是我改了那两份，并**给同步表加了一行**：
「改给用户看的报错里的一句承诺，要改复述同一句话的每一份文档」。

这一轮发现还有**第三份**：`README.md` 里写着「修好后 `--resume` 不产生新请求」。

漏掉它的原因不是忘了规矩，是**执行规矩的方法不对**：我按自己记得的措辞去
grep（「不产生新的请求」），而 README 那份写的是「不产生新请求」——
**少一个字，搜不到。**

规矩是对的，方法是错的：

> **按记忆去搜副本，搜不到不等于没有。** 同一句话在不同文档里会被不同的人、
> 在不同的时候、用不同的措辞写下来，而搜索匹配的是字面。

修法：README 那句改成有条件的说法（与代码、`ARCHITECTURE`、`SKILL.md` 一致）；
两张同步表里那一行**从「每一份文档」改成点名的清单**，并写明
「点名逐个过，不要 grep」。清单是有限的，记忆不是。

### 连带改动

`README.md` · `docs/SYNC.md` · `process/2-CHANGE.md`

### 教训

**一条同步规矩要能被执行，就得给出「要看哪几份」，而不是「所有相关的」。**

「复述同一句话的每一份文档」听起来完整，实际执行时会退化成「我搜得到的那些」。
把它换成一串点名的路径，执行方式就从**回忆 + 搜索**变成**逐个打开**——
后者会漏的唯一情况是清单本身没更新，而那是新增文档时就该做的事，
比"每次都要想起所有副本"可靠得多。

---

## ADR-28 承诺的「条件」变了要重走全表；影响渲染的东西都得进指纹，包括顺序

- 日期 · 2026-08-28 · PR #5 第十轮自动评审，两条都成立
- 类型：**事实证伪**（一条是同一句承诺的第四轮，一条是同一个指纹的第三轮）
- 冲击的需求：**D4**（验收标准本身说错了）· P3 · 派生元数据
- 结论：**采纳**，两条都改

### 一、同一句承诺，第四轮

这句「续跑要不要花钱」的承诺，四轮下来是这样走的：

| 轮次 | 做了什么 | 漏了什么 |
|---|---|---|
| ADR-22 | 改了 `collect.ts` 的代码 | 两份文档还在说旧话 |
| ADR-23 | 改了 `ARCHITECTURE` 与 `SKILL.md`，**并加了同步表规则** | README 那份措辞不同，grep 没搜到 |
| ADR-27 | 改了 README，**把规则改成点名的清单** | —— |
| **本轮** | —— | **清单里没有 `requirements.json`**；而且 ADR-25 把条件从「关键词」扩成「关键词 + profile」之后，**没有重走清单** |

所以还有四份是错的，其中最难看的一份是 **`docs/requirements.json` 的 D4.e** ——
**机器可读的真相来源**，它写着无条件的「修复后续跑不产生额外请求」。
另外三份（`ARCHITECTURE`、`SKILL.md`、`memory.md`）要么无条件，
要么只按关键词判断。

两条新认识：

1. **清单必须包含机器可读的需求登记表。** 它是最不该说错的那一份，
   而我列清单时只想着「文档」，把它漏在外面
2. **承诺的「条件」变了，和「措辞」变了一样，要重走全表。**
   ADR-23 那轮我改对了**地点**，ADR-25 那轮我改对了**条件** ——
   这是两回事，而我以为第一件做完了第二件就只用改发现问题的那一处

### 二、指纹丢掉了分类表的顺序

`canonical` 会把对象的键排序，而 `renderTables` 按**插入顺序**分节。
于是只调换两个分类的先后：

```
渲染的分节顺序变了吗？ true
指纹变了吗？         false
```

ADR-22 那轮把分类表的**值**加进了指纹，没加它的**顺序**。
修法：分类表以 `Object.entries`（数组）进指纹 —— 数组的顺序 `canonical` 不动。

### 连带改动

`scripts/check/spec-rule.ts` · `docs/requirements.json`（D4.e）· `docs/SPEC.md` ·
`docs/ARCHITECTURE.md` · `skill/SKILL.md` · `skill/references/memory.md` ·
`docs/SYNC.md` · `process/2-CHANGE.md` · `scripts/test.ts` ·
`scripts/check/mutations.json`（M-H2-d）

### 教训

**「凡是影响渲染的东西都得进指纹」比「把想到的字段加进指纹」可靠。**

指纹这件事也走了三轮：先只有需求（漏了分类表的值）、再加上值（漏了顺序）。
每一轮我都在**列举**该进指纹的东西，而列举的结果取决于我想到几样。

问题该反过来问：**渲染函数读了什么？** 那是个可以看代码数清楚的集合 ——
`renderTables(reqs, cats)` 读的就是这两个参数的**全部内容与顺序**。
和 ADR-25 那条「不去数有哪些活没干完，而去问续跑会调用哪些付费路径」是同一句话：

> **把判据从「我想到的东西」换成「对面实际用到的东西」。**
> 前者取决于记忆，后者可以穷举。

---

## ADR-29 「还没标记完成」不等于「续跑会去抓」—— 这次是多报

- 日期 · 2026-08-28 · PR #5 第十一轮自动评审
- 类型：**事实证伪**（同一句承诺的第五轮，但方向相反）
- 冲击的需求：**D6** · P3
- 结论：**采纳**

### 触发它的事实

达标提前停下时（`qualified() >= target_count`），`run()` 直接 `break`，
**剩下的关键词一个都没被碰过，也不会被写进 `state.done`**
（末尾那轮只把「跑满页数上限」的标记为完成）。

于是 `pendingKeywords(state)` 把它们算成待办，我的消息说
「还有 N 个关键词没跑完，续跑会继续发请求、继续花钱」。

而实际上：续跑先把累加器读回 `creators`，`run()` 的第一件事就是再查一次达标 ——
**同一个数、同一个门槛，立刻 `break`，一个请求都不发。**

### 方向相反，危害不同

前四轮那句承诺都是**少报**：告诉用户不花钱，实际要花。这一轮是**多报**。

多报看起来是「安全的那一侧」，但它有自己的代价：

> 用户被告知「续跑要花钱」，可能因此**不敢续跑** ——
> 而不续跑就永远拿不到那份已经付过费的名单。

一句不准的话，往哪边不准都会让人做错决定。**「保守」不是准确的替代品。**

### 结论

新增 `keywordsResumeWillRun(state, qualified)`：达标了就返回空，
否则才是 `pendingKeywords`。消息用它，进度汇报仍然用 `pendingKeywords`
（那是「还没跑完」的口径，服务的是进度不是花钱）。

D6.c 的验收判据改写成：**剩余工作量按「续跑真正会执行的路径」算** ——
关键词按「续跑会去抓的」，profile 按补全循环用的同一个判定。

### 连带改动

`scripts/lib/pipeline.ts` · `scripts/collect.ts` · `docs/requirements.json`（D6.c）·
`docs/SPEC.md` · `scripts/test.ts` · `scripts/check/mutations.json`（M-D6-e）

### 教训

**同一个集合，服务两个问题时口径可能不同 —— 别让它们共用一个函数名。**

`pendingKeywords` 回答的是「还有多少活没干完」（进度）。
我拿它去回答「续跑要花多少钱」（成本），而这两个问题在**达标提前停下**
这一种情况下答案不一样。名字里的 "pending" 掩盖了这一点：
它听起来像个通用事实，实际上是一个特定口径。

判据仍然是 ADR-25、ADR-28 那一句 —— **问「对面实际会执行什么」，
不要问「我这边还剩什么」** —— 只是这次它指向的是同一句话的另一侧：
上几轮我漏掉了对面会执行的东西，这一轮我算进了对面不会执行的东西。

---

## ADR-30 红线不许作废；`updated_at` 删掉；硬杀留下的临时文件下次清掉

- 日期 · 2026-08-28 · PR #5 第十二轮自动评审，三条都成立
- 类型：**事实证伪**（三条都落在我这几轮建的机制上）
- 冲击的需求：**P1–P5**（守它们的检查可被绕过）· D4
- 结论：**采纳**，三条都改

### 一、把红线标作废，红线就静静少一条

ADR-17 加的 `deprecated` 机制会让 `active()` 把作废的需求挡在**渲染和审计
之外**。于是给 `P4` 加一个格式完全合法的 `deprecated`：

```
✓ SPEC.md 与 requirements.json 一致（现行 34 条 · 已作废 1 条）
红线 4 条 · 有测试 4 · 有变异 4 · 显式豁免 1
验收判据 80 条 · 有测试认领 7 · 其中红线 9 条（认领 9）
✓ 审计：红线全部有测试且被变异验证
```

**审计报「红线 4 条」然后通过。** 这和 ADR-23 那条「分类填成另一个合法分类」
是同一个静默削减，只是换了一扇门 —— 而那扇门正是我上上轮亲手装的。

修法：`validateRegistry` 当场拦下红线的作废。

理由不是「作废机制有 bug」，是**作废一条红线这件事本身不该由一个字段完成**：
它的意思是「这个代价用户不再承担了」，那是产品定义变了，该走变更评定并留下
决策记录。这条也写进了 `process/1-REQUIREMENTS.md` 的红线硬要求（第 5 条），
它是「红线不参与任何取舍」的直接推论。

### 二、`updated_at`：我用来替换 `version` 的字段，得的是同一种病

ADR-17 的规矩是「元数据要么派生并校验，要么删掉」。我删了 `version`，
换上 `content_hash` **和 `updated_at`**。而 `updated_at`：

- **没人读**（全仓库只有 `spec-sync` 写它）
- **改成任何值都通过** —— 它不在指纹里，`--write` 也不碰它

```
只改 updated_at → ✓ 一致
连 --write 也不管它 → 写回之后 updated_at 还是 1999-01-01
```

**它就是 `version: 1`，换了个名字。** 而且我在 ADR-17 里亲手写下那条规矩之后
仍然把它留了下来 —— 因为它「看起来像派生的」。

按同一条规矩删掉。「最后改于何时」git 回答得更准，也不需要人维护。

### 三、硬杀在两步之间：临时文件留在盘上

`saveMemory` 写临时文件再 `rename`，挡得住「写到一半被打断」，
挡不住**在两步之间被 SIGKILL 或断电** —— 那时 `catch` 根本不会跑，
一份**完整**的临时文件留在盘上。而 D4.i 写着「不得留下截断的文件或临时文件」。

**任何 write-then-rename 都躲不掉这一格。** 能做的是下次写回时清掉它，
而且只清**已经死掉的进程**留下的 —— 活着的那些是别人正在写的
（两个任务同时跑的情形，ADR-15 里已经处理过一次）。

`D4.i` 的措辞改成如实的：目标文件不得被截断；残留的临时文件由下一次写回
清掉；活着的进程正在写的不许动。

### 连带改动

`scripts/check/spec-rule.ts`（`REDLINE_CAT` + 拒绝红线作废）·
`scripts/check/spec-sync.ts` · `scripts/check/audit.ts` · `scripts/lib/memory.ts` ·
`docs/requirements.json`（删 `updated_at`、改写 D4.i）· `docs/SPEC.md` ·
`process/1-REQUIREMENTS.md` · `docs/SYNC.md` · `scripts/test.ts` ·
`scripts/check/mutations.json`（M-H4-e · M-D4-h · M-D4-i）

### 教训

**每加一个「让某条需求不参与检查」的机制，就是给静默削减开一扇新门。**

这已经是第三扇了：

| 门 | 怎么让红线消失 | 补于 |
|---|---|---|
| 分类 | `cat` 填成另一个合法分类 | ADR-23 |
| 判据 | 红线判据不认领（曾经不算硬失败） | ADR-24 |
| 作废 | 给红线加一个合法的 `deprecated` | 本条 |

三扇门都是我自己装的，而且每一扇装的时候都在解决一个真问题。
所以规矩不该是「装门时小心」，而是：

> **任何能把一条需求移出检查范围的机制，都必须先回答：
> 它能不能把红线移出去？** 能，就要在那条路上单独加一道拦截。

这个问题该在**设计机制的时候**问，而不是等评审来问。

---

## ADR-31 代码注释也是副本；删字段和改承诺一样要走同步表

- 日期 · 2026-08-28 · PR #5 第十三轮自动评审，两条都成立
- 类型：**事实证伪**（两条都是「改完了没同步到剩下的副本」）
- 冲击的需求：P3（预算承诺）· 登记表的字段描述
- 结论：**采纳**，两条都改，并把两张清单各补一处

### 一、那句承诺的第五份副本，在脚本自己的文件头里

`scripts/collect.ts` 的文件头写着「修好后续跑零花费」。

ADR-27 把同步规则改成了点名的清单，ADR-28 又往清单里补进了
`requirements.json`。但清单列的全是**文档** —— 而**代码注释也是副本**，
并且它离实现最近，最容易被下一个人当成权威。

顺带发现同一个文件里还有一句「中止一个请求都不损失，修好文件 `--resume`
直接走到这一步」：前半句是真的（中止不浪费已花掉的请求），
后半句会被读成「续跑直接就到这一步了」。改成明确区分
「不浪费」和「续跑不花钱」这两件不同的事。

**清单补上：那个脚本自己的文件头与块注释。**

### 二、删掉一个字段，也要走同步表

ADR-30 把 `updated_at` 从 `requirements.json` 里删了，改了 `spec-sync.ts`，
**没改描述这个文件有哪些字段的两处文档**：`AGENTS.md` 和 `docs/SYNC.md`
的文档地图，两处都还写着「`content_hash` 与 `updated_at` 是派生字段」。

按那两份文档做事的人，会去找一个已经不存在的字段。

同步表原先只有「改承诺」这一行。**「增删登记表的字段」是另一类触发，
之前根本不在表上** —— 补了一行：类型与校验、读写、
以及**描述这个文件有哪些字段的文档**。

### 连带改动

`scripts/collect.ts` · `AGENTS.md` · `docs/SYNC.md`（两处：文档地图 + 两行触发规则）

### 教训

**同步表的行，是靠踩出来的。**

这张表现在有三条与「同一件事在多处有副本」相关的行，而它们分别是在
第 23、27、31 条决策记录时补上的 —— 每一条都是因为漏了一处才知道该有那一行。

这不算失败：**一张能自己长出来的表，好过一张一开始就想全的表** ——
后者不存在。但它有个前提，就是每次漏掉都要**回头补表，而不只是补那一处**。
这三次都补了表，所以下一次漏的会是新的地方，不会是同一个地方。

值得单独记的是这一条的形状：**清单只列了文档，而代码注释也是副本。**
写清单时我下意识把「文档」和「代码」当成两类东西，
而对「同一句话有几份副本」这个问题，注释和文档没有区别。

## ADR-32 记忆的键带空白 —— 同一间屋子的第四扇门

- 日期 · 2026-08-28 · PR #5 第十四轮自动评审，成立
- 类型：**事实证伪**（P1，静默破红线）
- 冲击的需求：P4（不重复打扰）· D1（身份规范化）
- 结论：**采纳**，带空白的键判为读不出来

### 事实

`tiktok:alice ` 这个键，现有的每一关都过得去：分隔符恰好一个、两边非空、
平台也在支持范围内。而查询侧生成的是 `tiktok:alice` —— 永远差那一个空格。

跑出来的结果：一个 contacted **且** blocked 的人重新进名单，
状态报「读到了」，过滤条数报 0 —— 与「确实没人联系过」完全同值。
**用户看不出任何异常。**

### 这是第四扇门

同一间屋子已经修过三次：

| | 门 | 记在 |
|---|---|---|
| 一 | 键的大小写没规范化（`tiktok:Alice`） | ADR-22 |
| 二 | 只要求「有个冒号」，平台拼错、多一个分隔符都收 | ADR-25 |
| 三 | 权限错误被 `existsSync` 压成「文件不存在」 | ADR-26 |
| 四 | 键里带空白 | 本条 |

四次都是同一个形状：**存储侧接受了一个查询侧永远生成不出来的键**，
而它静默地等价于「这个人没被联系过」。

### 这条规则是个判断，不是推导

前三扇门的判据都能从代码里推出来（查询侧就是那么生成的）。这一条不行 ——
「用户名不允许空白」是关于 `PLATFORMS` 里那两家平台的**外部事实**，
代码里推不出来。所以它绑在那个枚举上：**平台集合变了，这条得跟着重看。**

> **这一节是错的，见 ADR-37。** 仓库自己就编码了 handle 的形状 ——
> `report.ts` 渲染 `@${handle}`、`profile_url` 把它当裸路径段拼。
> 我没去看，就断言「推不出来」，然后只堵了空白，留下了第六扇门。

判断有可能错，所以选了错得起的那一边：误判会大声中止（还有
`--ignore-memory` 兜底），漏判是静默地破红线。红线不参与取舍。

### 连带改动

`scripts/lib/memory.ts` · `scripts/test.ts`（四条形状用例）·
`scripts/check/mutations.json`（M-P4-j）

### 教训

**「我已经修过这里了」不是「这里修好了」。**

三扇门都关上之后，我把「键的形状」当成了一件做完的事。而实际做完的是
三个**具体的**形状问题 —— 那不等于回答了「查询侧生成不出来的键还有哪些」。


## ADR-33 登记表只查了关系，没查形状

- 日期 · 2026-08-28 · PR #5 第十四轮自动评审，成立
- 类型：**事实证伪**
- 冲击的需求：登记表完整性（检查链自身）
- 结论：**采纳**，形状先过，再查关系

### 事实

`validateRegistry` 查的全是**关系**：编号唯一、分类声明过、交点指向真实存在的
编号、决策记录存在。每一处 `.trim()`、`.length`、`for...of` 都**假定字段在、
类型对** —— 而登记表是 `JSON.parse` 出来的，`Req` 那个接口运行时一个字段都不拦。

缺 `why` / `ruling` / `text` 会当场抛 TypeError，吵是吵了点，至少不是静默的。
**真正危险的是只被渲染、不被任何关系检查碰的字段**：

- `deprecated.since` —— 评审指出的那一个
- `pri` —— **同一类，评审没指出**

两者都会被 `renderTables` 原样写成 `undefined` 进生成的表格，`--write` 存下，
而一致性检查比的是**生成结果和生成结果** —— 于是全绿。

### 判据换成「渲染实际读哪些字段」

不是「我想得到哪些字段该在」。渲染读什么，就校验什么 —— 这是个能穷举的集合，
而「我想到的」不是。按这个判据走，`pri` 是和 `since` 一起掉出来的，
不需要再想一遍。

### 连带改动

`scripts/check/spec-rule.ts`（`shapeProblems`，形状不过就不往下走 ——
否则只会抛 TypeError 把真正的问题盖掉）· `scripts/test.ts` ·
`scripts/check/mutations.json`（M-H4-f）

### 教训

**和 ADR-19 是同一条**：`memory.ts` 早就有 `shapeProblem` 了，
因为记忆文件是手改的、显然不可信。登记表也是 `JSON.parse` 出来的，
只是它「看起来是我们自己维护的」—— 于是我没给它同样的待遇。

**数据可不可信，看它怎么进来的，不看它归谁维护。**


## ADR-34 变异记在一个不存在的编号名下，全绿

- 日期 · 2026-08-28 · 修 ADR-33 时我自己踩进去的
- 类型：**事实证伪**
- 冲击的需求：审计口径（检查链自身）
- 结论：**采纳**，记错名下当场拦下

### 事实

给 ADR-33 加变异时，我写了 `req: "H4"`。`H4` 不是任何一条需求的编号 ——
`H` 开头是检查链给自己留的名下，而它真正的写法是 `harness`，
另外十五条 `M-H*` 全都写对了。

`npm run check` 全绿。变异照样跑、照样被抓到，控制台打出 `✓ M-H4-f [H4] 被抓到`。

而 `audit.ts` 拿 `req` 去回答「这条需求有没有变异守着」—— 那条变异
**对任何一条需求都不算数**，记在了一个空名下。没有人被告知。

### 吵的那一半早就有人管了

写成**另一条真实存在**的编号是吵的：被顶替的那条会报「缺变异」。
写成**不存在**的编号是静默的 —— 而这一半没有任何检查。

### 连带改动

`scripts/check/attribution-rule.ts`（新模块）· `scripts/check/mutate.ts` ·
`docs/ARCHITECTURE.md` · `scripts/test.ts` ·
`scripts/check/mutations.json`（M-H7-a）

名下可以是一条需求，也可以是一条**验收判据** —— 豁免常常只豁免其中一条判据
（`P2.a`、`P3.b`）。第一版只认需求编号，当场被现有的三条豁免证伪。

### 教训

**这一条不是评审找出来的，是我自己在修上一条的时候踩进去的**，
而且是在一条「检查报绿不等于没问题」的记录正下方踩的。

值得记的是它怎么被发现的：不是靠检查（检查是绿的），是靠**看见输出里
`[H4]` 和旁边十五个 `[harness]` 不一样**。这一轮我一直在核对
「对面实际读的是什么」，恰好也就看了一眼那一列。

**绿色的检查链证明不了它自己的口径是对的。**

## ADR-35 一个 PR 装了四件事 —— 拆成两个，并记下为什么会这样

- 日期 · 2026-08-28 · 用户在第十四轮之后问「最开始我想让你做什么，为什么改了四千多行」
- 类型：**范围失控**（不是事实证伪 —— 每一步都对，是总和从来没被评定过）
- 冲击的需求：无。这条冲击的是**做事的方式**
- 结论：**采纳**，拆成两个 PR

### 事实

最初的请求是一个**问题**：「需求冲突、迭代和更新你怎么看？需求本身要不要做版本管理？」
授权实现的是后来的一句 `do it`，它授权的是一件有边界的事：
D4×P4 那条红线的修复 + 配套测试和变异。

最终这个 PR 是 4362 行，其中真正改行为的产品代码 433 行（9%）。

### 它是怎么长这么大的

一部分是**后来授权的**，而且是有意的乘数：

| 授权 | 带出了什么 |
|---|---|
| `do them all` | 整套登记表机器：内容指纹、决策记录反链、作废、交点 |
| 「验收标准拆成可独立计量的条目」 | 35 条需求变成 81 条判据，`criterion` 机制，审计改造 |

另一部分**没有任何人授权** —— 十四轮外部评审，每一轮我都做了四件事，
而被要求的只有第一件：

1. 修这一条
2. 把同一类都堵上
3. 写一条决策记录
4. 往通用规则里加一行

单看每一步都站得住。十四轮复利下来，决策记录占了全部篇幅的三分之一。

### 怎么拆的

- **本 PR**：D4×P4 的红线修复 —— `memory.ts` 的三档状态、原子写回、
  键的规范化与形状校验、`--ignore-memory` 逃生舱，以及它们的测试与变异
- **后续 PR**：需求登记表的机器 —— 内容指纹、判据、交点、作废、
  `spec-rule.ts` / `claims.ts` / `attribution-rule.ts`，以及审计的改造

**决策记录整份留在本 PR，不拆。** 二十条里有八条是「一条记两件事」的
（比如 ADR-30「红线不许作废」与「硬杀留下的临时文件」），
按代码归属切开会把当时真实的推理切成两半假的。代价是：本 PR 的
ADR-17、ADR-33、ADR-34 讲的是后续 PR 才会出现的文件 —— 看到那里请回到这一条。

### 教训

**我对每一条进来的意见都做了评定，唯独没有对「我自己已经做了多少」做评定。**

`2-CHANGE.md` 写着：反向输入必须走评定，**不许悄悄消化**。我在下一层
一丝不苟地执行了这条规则 —— 十四轮、每一条评审意见都验证、都记录、都回复。
而在上一层，「范围已经涨到哪了」这个问题一次都没有被提出来。

规则是「每一条输入都要评定」，我读成了「每一条输入都要评定」。
**漏掉的是：总和也是一条输入。**

一个可执行的补法：**改动超出授权时的原始范围时，把它当成一次反向输入**
—— 走同样的评定，交给当初授权的人，而不是自己吸收掉。

## ADR-36 同一个键出现两次 —— 解析已经把证据吃掉了

- 日期 · 2026-08-28 · PR #5 拆分后第一轮自动评审，成立
- 类型：**事实证伪**（P1，静默破红线）
- 冲击的需求：P4（不重复打扰）· D4（记忆不可用的分档）
- 结论：**采纳**，重复键判为读不出来

### 事实

手改的文件里把同一个人贴了两遍：

```json
"tiktok:alice": { "contacted": true,  "blocked": true,  ... },
"tiktok:alice": { "contacted": false, "blocked": false, ... }
```

`JSON.parse` 静默保留**最后一条**。跑出来 `status=ok kept=1 filtered_contacted=0` ——
一个已联系、已拉黑的人重新进名单，而状态报「读到了」。

### 为什么前四道关一道都没拦住

`normalizeKeys` 里有一条撞车检测（ADR-22 加的），专门管「两个键指同一个人」。
它拦得住 `tiktok:Alice` 与 `tiktok:alice`，**拦不住两个一模一样的键** ——
因为等它拿到 `Object.entries` 的时候，重复**已经不存在了**。

我在同一个函数里写下「两个键规范化后撞在一起 —— 该用哪一条无从判断」，
却没想到还有一种撞车是**解析器替我选了一条**，而且不告诉我。

### 修法

在**文本上**查，不在解析结果上查。`duplicateKey` 扫一遍原文，
按对象层记键，同层出现两次就报。只在已经解析成功的文本上跑，不必处理非法 JSON。

借 `JSON.parse` 还原单个键的转义 —— 手写反转义会把 `\u0062` 和 `b` 看成两个键。

### 连带改动

`scripts/lib/memory.ts` · `docs/requirements.json`（D4 的验收标准）·
`scripts/test.ts`（两条用例，其中一条**只能用手写字符串构造** ——
对象字面量和 `JSON.stringify` 都吐不出重复键）· `scripts/check/mutations.json`（M-P4-k）

### 教训

**检查要放在证据还在的地方。**

前四扇门都是「值不对」，查解析结果就够。这一扇是「证据在解析时被消灭了」——
再严的下游检查也查不到一件已经不存在的事。判断一个检查放得对不对，
要问的是「我要找的那个东西，走到这一步时还在不在」。

## ADR-37 只堵了空白，剩下的字符还开着 —— 第六扇门

- 日期 · 2026-08-28 · PR #5 拆分后第二轮自动评审，两条都成立
- 类型：**事实证伪**（一条 P1 静默破红线，一条是「类型对不等于能用」）
- 冲击的需求：P4 · D4
- 结论：**采纳**，并**更正 ADR-32 的一处断言**

### 一、handle 的形状（P1）

`tiktok:@alice`、`tiktok:alice/`、`tiktok:alice?x`、`tiktok:ali%63e` ——
四个都过得了分隔符、空白、平台三关，四个都 `status=ok kept=1 filtered_contacted=0`。

**ADR-32 里我明确拒绝过这件事**，理由写着：按用户名校验可能吃掉一份
我们自己写出来的合法记忆，而「用户名不允许空白」是外部事实、代码里推不出来。

**那句话是错的。** 这个仓库自己就编码了 handle 的形状：

| 证据 | 推出什么 |
|---|---|
| `report.ts` 渲染 `@${handle}` | handle 自己带 `@` 会显示成 `@@alice` |
| `profile_url` 拼 `.../@${handle}` 与 `.../${handle}/` | handle 是**裸的 URL 路径段** |

我没去看，就断言推不出来 —— 于是只堵了想得到的那一个字符。

取两家平台都允许的字符集（字母、数字、下划线、点；连字符留着是因为
现有数据里有 `active-kol`）。方向仍然选错得起的那边：误判大声中止，
漏判静默破红线。

### 二、类型对不等于能用（P2）

`"product": ""` 类型是字符串，检查放行。而 `filterByMemory` 拿 product
去匹配「这个人是不是已经为这个产品推过了」—— 空字符串永远匹配不上任何产品，
**这条去重记录等于不存在**，人会被重新推荐，名单上那句「上次推的是什么」
也渲染成 `" @ 2026-01-01"`。

`date` 同一类（渲染成 `"p @ "`）。`task` 不在此列：空字符串与缺省在过滤里
行为完全一致，那不是坏数据。

### 连带改动

`scripts/lib/memory.ts` · `docs/requirements.json`（D4）· `scripts/test.ts`（七条用例）·
`scripts/check/mutations.json`（M-P4-l · M-D4-k；**删掉 M-P4-j** ——
它守的空白检查被 handle 形状检查整个包含了，留着就是同一条变异写两遍。
空白的那几条用例照旧，现在由 M-P4-l 守）· ADR-32 就地更正

### 教训

**「我判断不出来」之前，先去问一遍这个仓库。**

ADR-32 那句「这是外部事实，代码里推不出来」听上去很严谨 —— 承认边界、
标明限度、还给了失败方向。它唯一的问题是**没去查**。而查的成本是两个 grep。

一个自称谦虚的结论，和一个偷懒的结论，写出来长得一模一样。
分辨它们的办法不是看措辞，是看**有没有去找过证据**。

## ADR-38 名单和它的状态分两次落盘 —— 顺序反了就是静默破红线

- 日期 · 2026-08-28 · PR #5 拆分后第三轮自动评审。**两条里只采纳一条**
- 类型：**事实证伪**（一条成立，一条前提不成立）
- 冲击的需求：P4 · P5 · D4
- 结论：**采纳一条**（落盘顺序），**驳回一条**（写回时机），理由见下

### 采纳：状态要先于名单落盘

`collect.ts` 末尾原先是 `saveCreators` 再 `saveTask`。两次独立的
`writeFileSync`，中间被打断是可能的，而两种顺序的失败后果**不对称**：

| 顺序 | 被打断之后 | 后果 |
|---|---|---|
| 名单在前（原先） | `creators.json` 已是未去重的新名单，`task.json` 还留着上一轮的 `ok` | render 从 `task.json` 读状态，**压掉警告**，把打扰过、已拉黑的人当成已去重交付 —— 静默破 P4 与 P5 |
| 状态在前（现在） | `task.json` 说「没去重」，`creators.json` 还是上一轮去重过的旧名单 | 报告多报一次警告，用户重跑一次 |

多报要用户重跑一次，少报是把人重新打扰一遍。**答不上来时报警告，不报「已去重」。**

值得记的是：**同一个文件里正确的顺序早就有** —— `persist()` 一直是
`saveTask` 在前。是末尾这段偏离了它。加功能时我照着「先写主产物、再写元数据」
的直觉写，而没有回头看这个文件自己是怎么做的。

守它的东西：**没有。** 这是入口脚本里两次落盘的先后，而变异只跑
`scripts/test.ts`，入口接线没有变异守得住 —— 与已登记的另一处入口接线同样处境。
按先例**显式登记成缺口**，不在单元测试里伪造一个认领。

### 驳回：写回记忆的时机

另一条说：`recordRecommendations` 跑在 `meta.json` / `report.html` 之前，
若后两者写失败（磁盘满），人已被记成「推荐过」，而**用户从没拿到一份完整交付**，
下一个任务却会把这些人滤掉。

**前提不成立。** `render.ts` 的落盘顺序是：

```
saveCreators(40) → CSV(44) → xlsx(50) → recordRecommendations(80) → meta(137) → report(138)
```

CSV 与 xlsx 在写回**之前**就已落盘，中间没有任何 `process.exit`、`throw` 或
`return`。也就是说走到写回时，名单**已经在用户手上**了 —— 缺的是
`meta.json` 与 `report.html`。那条记忆记的「这些人为这个产品推荐过」是**真的**。

而且写回按任务编号分段（`filter(r => !(task && r.task === task))`），
同一个任务重跑 render 会替换掉自己那一段，是幂等的。

反过来说，把写回推迟到交付物全部落盘之后，会让 `meta.json` 没法报告写回的
**真实**结果 —— 只能报一个预测。那正是 D4.j 要求的反面（「交付物必须带出失败
原因原文」），也正是 ADR-22 记过的那条：**不许替下一步打包票。**

### 教训

**「两件事要一起落盘」做不到的时候，问的不是怎么做到，是哪个先。**

原子性拿不到时还有一件事可做：**让打断之后剩下的那个状态是安全的那一个。**
顺序不是实现细节，它就是这条约束本身。

> **上面这一节只对了一半，见 ADR-41。** 我只分析了 `ok` → `unreadable_ignored`
> 这个方向，就断言「状态先写是安全的那一个」。反方向
> （`unreadable_ignored` → `ok`）恰恰相反，而**哪个顺序都不安全**。
> 「让剩下的状态是安全的那一个」这句话是对的，「所以状态先写」是错的。

## ADR-39 进程号还活着，不等于文件有主

- 日期 · 2026-08-28 · PR #5 拆分后第四轮自动评审，成立
- 类型：**事实证伪**
- 冲击的需求：D4（临时文件的清理）
- 结论：**采纳**，进程号之外再加一道文件年龄

### 事实

ADR-30 加的那道清理是：临时名带 pid，下次写回时清掉**已死进程**留下的，
不动还活着的（并发写保护）。

漏掉的是：**系统会回收进程号。** 孤儿文件活得够久，它名字里那个 pid 被分配给
一个毫不相干的长命进程之后，`alive(pid)` 就永远返回 true —— 这个孤儿再也清不掉，
一份完整的记忆副本永远躺在盘上。D4 明写着下次写回要清掉它，而它清不掉。

### 修法

进程号之外再加一道**文件年龄**：写的过程是一次 `writeFileSync` 加一次
`renameSync`，毫秒级；活过一小时的一定不是正在写的。一小时是荒谬地宽松，
为的是任何真正在写的文件都不可能被误判成孤儿。

两个条件都要满足才保留：**进程还活着 且 文件还年轻。**

顺带把 D4 的验收标准改成实话：进程号会被回收，所以「那个进程还在」
不足以判定文件有主。原先那句读起来像是 pid 就够了。

### 教训

**pid 是个会被复用的标识符，而我把它当成了身份。**

同一个形状在这个 PR 里出现过好几次：编号被回收复用会让历史记录指向错误的东西
（所以需求编号和判据编号都写死了「不回收复用」），而我在需求文档里写下这条规矩，
转头就在代码里拿一个**由系统回收复用**的数字当身份用。

「这个标识符会不会被复用」是个该在用它之前问的问题，不是该在评审里被问的问题。

## ADR-40 原子写换掉了权限位；空白该规范化还是该判损坏

- 日期 · 2026-08-28 · PR #5 拆分后第五轮自动评审，两条都成立
- 类型：**事实证伪**
- 冲击的需求：D4 · D6
- 结论：**采纳**，但两条用的是**相反**的处理方式

### 一、写回悄悄放开了权限

```
写回前: 600
写回后: 644
```

`writeFileSync` 按 umask 建临时文件（通常 0644），`renameSync` 把这个更宽松的
权限**一并装到目标上**。用户特意 `chmod 600` 过的记忆 —— 它记着谁联系过、
备注写了什么 —— 每成功写回一次就被放开一次。

这是 write-then-rename 自带的一格：rename 换掉的是整个 inode，权限跟着新文件走。
修法是把目标原有的权限位读出来、装到临时文件上，再改名。

**文件不存在时保持默认**：新文件该多严是产品决定，不在修 bug 时顺手改。

### 二、product 的首尾空白：规范化，不是判损坏

`" Foo "` 存着、`"Foo"` 查，精确相等匹配不上，「已推荐过」整条失效，
人被重新推荐一次。

**这条和 handle 的形状（ADR-37）看起来是同一类，处理却相反 —— 因为数据的来源不同：**

| | handle | product |
|---|---|---|
| 从哪来 | 提供方 API，形状受平台约束 | **用户的任务配置**，想写什么写什么 |
| 判成损坏的风险 | 几乎没有 —— 越界只可能来自手改 | **真实存在** —— 配置里带个空格，我们自己写下的记忆就被判读不出来 |
| 所以 | **拒绝** | **规范化** |

判据不是「这个字符串好不好看」，是**「这个值是谁产生的，我把它判成坏数据会不会
伤到自己」**。handle 越界只可能来自手改；product 带空格完全可能是用户配置本来
就那样，而那时候拒绝等于自伤。

纯空白（`"   "`）仍然算损坏 —— trim 之后是空的，回到 ADR-37 已经处理过的那一格。

### 教训

**同一个形状的两个问题，可以有相反的正确答案。**

上一轮刚学会「先去仓库里找证据」，这一轮差点把它用成「凡是越界一律拒绝」。
真正决定拒绝还是规范化的，不是那个值长什么样，是**它从哪来、以及判错了谁受伤**。

## ADR-41 哪个顺序都不安全 —— 断言要最后写

- 日期 · 2026-08-28 · PR #5 拆分后第六轮自动评审，成立（P1）
- 类型：**事实证伪**，而且证伪的是**我上一轮刚给出的那个修法**
- 冲击的需求：P4 · P5 · D4
- 结论：**采纳**，改成三步，并更正 ADR-38

### 事实

ADR-38 把落盘改成「状态先、名单后」，理由是两种失败后果不对称。
**那个分析只做了一个方向。**

| 状态怎么变 | 名单先写 | 状态先写 |
|---|---|---|
| `ok` → `unreadable_ignored` | 未去重的新名单 + 说已去重的旧状态 ✗ | 安全 |
| `unreadable_ignored` → `ok` | 安全 | **未去重的旧名单 + 说已去重的新状态 ✗** |

上一轮用 `--ignore-memory` 出过一份未过滤的名单，用户随后修好了记忆文件、
重跑 —— 这一次 `fin.memory_status` 变成 `ok`。按 ADR-38 的顺序，
`task.json` 先写下 `ok`；此时被打断，盘上是**上一轮那份未过滤的名单**
配一句「已去重」。报告压掉警告，打扰过、已拉黑的人照常交付。

我修的那个方向堵上了，反方向被我亲手打开了。

### 修法：不选顺序

哪个先都不安全，因为安全与否取决于状态往哪边变 —— 而那是运行时才知道的。
所以**分三步**：

1. 把断言撤成「无从确认」
2. 换名单
3. 名单确实落盘了，这才断言

任何一步被打断，盘上留下的都是一个**不做肯定断言**的状态：报告会警告，
用户重跑一次。不变式是一句话 ——
**肯定的断言永远最后写，而且只在它描述的东西已经落盘之后。**

`unknown` 因此多了第二个来源（原先只有「ADR-15 之前产出的目录」）。
不是把两种含义压进一个值：**「盘上这份名单去没去过重，无从确认」**
在两种来源下是同一句话。类型注释与 D4 都已写明。

### 顺带：这段逻辑不再是「入口接线」

ADR-38 把它登记成了显式缺口，理由是入口脚本里的接线没有变异守得住。
这次把三步挪进 `lib/task.ts` 的 `persistListAndStatus` —— **它有语义，
就该能被测**（`docs/CONVENTIONS.md` 第 10 条）。于是缺口撤掉，换成一条真的测试：
拿一个同名目录占住 `creators.json` 让它写失败，断言状态停在「无从确认」。
配 M-D4-n。

### 教训

**「不对称」这个词让我停止了枚举。**

发现两种顺序后果不对称、挑出安全的那个 —— 这个推理形状太漂亮了，
漂亮到我没有再问一句「状态还能怎么变」。而那张表只有两行是我自己填的，
另外两行一直空着。

对称性论证的危险就在这里：**它看起来已经穷举了，其实只穷举了我想到的那一维。**

## ADR-42 权限收在写完之后，那段窗口里数据是敞开的

- 日期 · 2026-08-28 · PR #5 拆分后第七轮自动评审，成立
- 类型：**事实证伪**，证伪的又是**我上一轮刚给出的修法**
- 冲击的需求：D4
- 结论：**采纳**，临时文件建的时候就给最严的权限

### 事实

ADR-40 的修法是「写完临时文件，chmod 成目标的权限，再改名」。
顺序错了：`writeFileSync` 按 umask 建文件（通常 0644），**完整的联系历史
在那一刻就已经躺在一个别人读得到的文件里了**，chmod 是事后才收的。

而且硬杀正好落在这两步之间时，那份 0644 的副本会留在盘上，
直到下次写回时被清掉。

### 修法

临时文件**建的时候**就给 0600 —— 它没有任何人需要读，比目标只严不松。
建完再 chmod 到目标该有的权限。

两步都要：建文件时的 `mode` 会被 umask 削（目标本来是 0666、umask 是 022 的话，
只靠建文件那一步会把它悄悄收严），chmod 不会。

### 一个变异当场活了

改完跑检查，**M-D4-m 存活**了。它删掉的是 chmod 那一步，而我的用例
恰好把目标也设成 0600 —— 临时文件既然按 0600 建，删不删 chmod 结果一样，
**那条测试证明不了任何事**。

把用例改成 0640（一个不等于临时文件那一档的权限），变异当场被抓到。

这正是变异测试存在的理由：它不是在测代码，是在测**测试**。
「建得严」和「事后调回目标」是两件事，而我原来的断言把它们压成了一件。

### 教训

**修一个安全问题时，问题本身会挪位置。**

ADR-40 问的是「最后落到目标上的权限对不对」，答对了。
这一轮问的是「中途有没有一瞬间是敞开的」—— 同一份数据、同一段代码，
换一个时刻去看就是另一个答案。

**「结果正确」不等于「过程中每一刻都正确」**，而涉及敏感数据时，
后者才是要问的那个。

## ADR-43 给一个值加了第二个来源，措辞还在描述第一个

- 日期 · 2026-08-28 · PR #5 拆分后第八轮自动评审，成立
- 类型：**事实证伪**（ADR-41 的余波）
- 冲击的需求：P5（交付物的声明）· D4
- 结论：**采纳**，措辞改成与来源无关

### 事实

ADR-41 给 `unknown` 加了第二个来源（名单与状态没能一起落成）。我改了
`requirements.json`、改了类型注释 —— **四份面向用户和 Agent 的措辞一份没改**：

| 副本 | 原话 |
|---|---|
| `scripts/lib/report.ts` | 「这批人由早期版本采集，当时记忆文件读不出来会被静默当成空记忆」 |
| `scripts/render.ts` 块注释 | 「缺失**只可能**来自 ADR-15 之前的 collect」 |
| `skill/references/memory.md` | 「（早期版本采集的任务目录）」 |
| `skill/references/output-format.md` | 「（早期版本采集的任务目录）」 |

于是：被打断的那种情况，用户会在报告上读到「这批人由早期版本采集」——
而它其实是**刚刚才产生的**。这不是措辞过时，是**给了用户一个编造的诊断**，
他会拿着它去查一个不存在的历史原因。

### 修法

措辞与来源无关：只说「无从确认」和「重跑一次采集即可得到确定答案」，
不解释为什么无从确认。两个来源事后本来就分不出，说不出来就别说。

这是 ADR-20 那条规矩的另一面：那次是**不许替下一步打包票**
（把「写不进去」说成「文件坏了」），这次是**不许替上一步编原因**。

### 教训

**加一个来源，和改一句话，我把它们当成了两件事。**

同步表里那条「改报错/提示里给用户的一句承诺」，列的是**改承诺**时要走的清单。
而我这次做的是**加一个来源** —— 承诺本身一个字没动，所以我根本没想到要走那张表。

漏掉的判据不是「有没有改措辞」，是**「有没有让某句已经写下的措辞变得不再为真」**。
第二个来源让「只可能来自早期采集」这句话当场变成假的，而我没改它一个字。

## ADR-44 第三次把「哪一步坏的」编给用户；以及年龄兜底的代价要写出来

- 日期 · 2026-08-28 · PR #5 拆分后第九轮自动评审，两条都成立
- 类型：**事实证伪**（一条改代码，一条改的是**需求里那句过强的话**）
- 冲击的需求：D4
- 结论：**采纳**

### 一、`MemoryUnreadable` 把 I/O 失败说成「解析失败」

```
记忆文件 .../creators.json/x.json 解析失败：ENOTDIR: not a directory
```

一个纯粹的路径问题，用户读到的是「解析失败」，然后去查一份完好的 JSON。

`collect.ts` 原样打印这句话，skill 的说明还叫 Agent 照着转述 ——
**一句编造的诊断会一路传到操作的人手里。**

`readMemory` 早在 ADR-26 就把「读不到」并进了 `unreadable`（因为 `existsSync`
分不清权限错误和文件不存在）。**状态改对了，包装它的那句话没跟着改。**

改成中性：`记忆文件 X 不可用：<detail>`。真实原因在 detail 里，它自己会说。

**这是同一条规矩第三次出现：**

| | 编的是什么 | 记在 |
|---|---|---|
| 一 | 把「写不进去」说成「文件坏了」 | ADR-20 |
| 二 | 替 `unknown` 编一个来源 | ADR-43 |
| 三 | 替失败编一个阶段 | 本条 |

三次的形状一模一样：**一个笼统的状态，配一句具体的解释，而那句解释是我
从最常见的那种情况里顺手抄来的。**

### 二、年龄兜底的代价，要写在需求里

第二条意见说：被 `SIGSTOP` 或调试器按住超过一小时的写入方，它的临时文件
会被年龄兜底当成孤儿清掉，恢复后 rename 失败。

**成立。而这次不改代码，改的是 D4 里那句话。**

D4 原先写着「不得动还活着的进程正在写的那些」—— 加了年龄兜底之后，
这句话已经不再为真。两种失败都罕见，但坏法不同：

| | 后果 |
|---|---|
| 不做年龄兜底 | pid 被回收后，一份完整的联系历史**永远**留在盘上，没人知道 —— **静默** |
| 做年龄兜底 | 被按住的写入方那一次写回失败，交付照常完成并带出原因 —— **响的** |

静默与响之间选响的。取舍本身不变，**但需求不能继续写着它没做到的事** ——
改成「不得动还活着的进程**刚写下**的那些」，并把代价写进去。

没有走「加锁」那条路：锁文件自己就会留下陈旧锁，那正是这里在解决的问题。

### 教训

**改了行为，就要回去看需求那句话还成不成立。**

第二条不是代码缺陷，是**文档在替代码吹牛**。而这种吹牛比代码缺陷更难发现 ——
检查链会验证代码符合需求，不会验证需求说的是实话。

## ADR-45 三步协议建立在一个没被保证的前提上

- 日期 · 2026-08-28 · PR #5 拆分后第十轮自动评审，成立
- 类型：**事实证伪**（ADR-41 的地基）
- 冲击的需求：D4 · D6
- 结论：**采纳**，任务目录里的落盘全部改成整体替换

### 事实

ADR-41 那套三步协议的保证是：「任何一步被打断，盘上留下的都是一个
**不做肯定断言**的状态」。

而 `saveTask` 是一句裸的 `writeFileSync`。**被杀在这一句中间，盘上留下的
不是「一个保守的状态」，是一份截断的 JSON** —— `loadTask` 直接抛，
续跑起不来，已经花钱抓到的东西找不回来，render 也跑不起来。

三步协议的每一步都假定「写下去的东西会完整地在那儿」，而那一点没有人保证。

### 一次只修一处就是又一次「只修实例」

评审点的是 `saveTask`。而同一个文件里有**四处**裸写，每一处都是那个目录里
唯一一份、坏了要重新花钱抓的东西：

| 文件 | 坏了会怎样 |
|---|---|
| `task.json` | 续跑起不来；三步协议的地基 |
| `creators.raw.json` | 采集累加器，整批采集作废 |
| `creators.json` | 交付物 |
| `enrichment.json` | 已付费的补全结果 |

抽一个 `writeAtomic` 出来，四处一起改 —— **比只改一处的代码还少**。
做法与 `memory.ts` 里那套相同，理由也相同（ADR-15）。

### 明确不做的事

**不做孤儿临时文件的清理。** 硬杀会在任务目录里留下 `*.tmp`，就让它躺着：
任务目录是一次性的、可以整个删掉。记忆文件那边要清，是因为它长期存在、
而且孤儿是一份完整的联系历史副本 —— 这里两条都不成立。
写出来是为了它是个**决定**，不是一个疏忽。

### 教训

**我给一个保证写了三步协议，却没检查那三步各自成不成立。**

ADR-41 里我论证得很细：哪个顺序不安全、为什么中间态是结构保证而不是概率保证。
整段论证的每一步都对 —— 而它站在一个我从来没看过的地基上。

**「这一步会完整地发生」是个前提，不是一个事实。** 用它推出结论之前，
得先去确认它。

## ADR-46 写入侧能写出读取侧拒绝的东西；以及同一个 bug 我写了两遍

- 日期 · 2026-08-28 · PR #5 拆分后第十一轮自动评审，两条都成立
- 类型：**事实证伪**
- 冲击的需求：D4
- 结论：**采纳**，并把原子写入合并成**唯一一份**

### 一、一次写回就能毒掉整份记忆

任务配置里 `product` 是空白时：

```
写回报告: {"written":true}
盘上存的 product = ""
下次读:   记忆文件 ... 不可用：结构不对 —— tiktok:alice 的第 1 条推荐记录的 product 不是非空字符串
```

**这是我自己造的循环。** ADR-37 加了读取侧的「product 必须非空」，写入侧没跟着加 ——
于是一次正常的写回就把一份好好的记忆变成读不出来的，此后每一次采集
都被挡在门外，直到有人手工去修。

判据是**读取侧实际会拒绝什么**，不是「这个值看起来合不合理」。
写入侧现在拒收并报 `written:false`，另外 `collect` 在**花钱之前**就检查配置 ——
空产品名走不到最后，早说比晚说好。

### 二、同一个权限位的 bug，我在两个文件里各写了一遍

上一轮（ADR-45）我给任务目录写了个新的 `writeAtomic`。两轮前（ADR-40 · ADR-42）
刚在 `memory.ts` 里修好的权限位处理，**新写的这份一个字都没带过来** ——
`saveTask` 又是 600 进、644 出。

根因不是我粗心，是**这段例程有两份副本**。所以这次不是「把第二份也修好」，
是抽成 `lib/atomic.ts` 里**唯一一份**，两边共用。

合并的直接好处当场就看见了：原先守这两份的四条变异，合并后有两条
指向了同一行 —— 说明它们本来就在重复地守同一件事，只是分散在两处看不出来。

**留在调用方的**：孤儿临时文件清不清。记忆文件要清（长期存在、孤儿是一份
完整的联系历史副本），一次性的任务目录不用。这一条按各自处境不同，
不该合进公共函数。

### 教训

**「我刚修过这个」是最不该信的一句话。**

我修过 `memory.ts` 的权限位，两轮之后写一个新函数时，那次修复没有以任何形式
出现在我脑子里 —— 因为它属于**另一个文件**。

真正的问题从来不是「记不记得」，是**同一段逻辑有几份副本**。
一份副本的时候不需要记得，两份的时候记性就成了唯一的保障，而它不可靠。

## ADR-47 落盘、并发、认不出的取值 —— 三条都是「我以为已经保证了」

- 日期 · 2026-08-28 · PR #5 拆分后第十二轮自动评审，三条都成立
- 类型：**事实证伪**
- 冲击的需求：D4 · P5
- 结论：**全部采纳**，其中并发那条**只做检测，不做串行化**

### 一、`rename` 之后不等于落到盘上了

`writeFileSync` 加 `renameSync` 保证的是**原子替换**，不是**持久化**。
断电时文件系统恢复出来的可能是一个空文件、或者干脆没有这次改名 ——
而这个函数已经返回成功了，调用方据此报告「已记入」。

补上两处刷盘：改名前刷临时文件（内容先落盘），改名后刷所在目录
（**改名本身是目录的一次改动**，它自己也要落盘）。刷不动就算了 ——
有些文件系统不支持刷目录，不该让一次成功的写回因此变成失败。

D4 原先只写「被打断不得留下截断的目标文件」，现在补上「写了不等于落盘了」。

### 二、临时名带 pid 只保住了临时文件，没保住那次读改写

这个模块的注释里明写着「两个 render 同时跑」，临时名带 pid 正是为它准备的。
**而它保护的只是临时文件不互相覆盖，不是整个读—改—写。**

两个 render 重叠时：双方读到同一份快照 → 各自加上自己那批推荐 → 先后改名。
后写的把先写的整个盖掉，**两边的报告都说「已记入」**，丢掉的那批人
下次会被重新推荐一遍。

**没有加锁。** 这一轮里第三次遇到「用锁解决」的建议，理由和前两次一样：
锁文件自己会留下陈旧锁，而判断锁陈不陈旧又要回到「持有者还在吗」加「多久了」——
同一个问题换了个文件名（ADR-39 · ADR-44 都走过这一遍）。

所以只做**检测**：改名前最后一刻再比一次盘上还是不是读到的那份，变过就拒绝写回
并报出来。**这不消灭并发，它把静默丢失换成一次明确的失败。**
「比一次」到「改名生效」之间仍有一道缝，缝压到了最小，但不是零 —— 写在注释和需求里。

### 三、认不出的取值被原样抄进交付物

`task.json` 是 `JSON.parse` 出来的，`memory_status` 写成 `null`、拼错、
或者被新版本写成一个没见过的值时，原先的三元表达式只挡 `undefined`，
其余原样抄进 `meta.json`。而报告只对 `unreadable_ignored` 与 `unknown`
两个**精确字符串**发警告 —— 一个认不出的值会**压掉警告**。

改成白名单：认得出的四个照原样，其余一律读作 `unknown`。
**白名单而不是黑名单** —— 黑名单要求我列全「坏的取值」，而坏的取值列不全。

### 教训

**三条的共同点：我以为某个机制已经保证了某件事，而它保证的是别的。**

| 我以为 | 它实际保证的 |
|---|---|
| write-then-rename 保证了「打断也不坏」 | 只保证**原子替换**，不保证**落盘** |
| 临时名带 pid 保证了「并发安全」 | 只保证**临时文件不互撞**，不保证**读改写不互撞** |
| 类型是 `MemoryStatus` 保证了取值合法 | 反序列化进来的东西，类型在运行时**一个值都不拦** |

三条都不是「忘了做」，是**把一个机制的保证范围记大了**。
而记大的那部分，正好是没有人检查的那部分。

## ADR-48 两个新函数，各自重犯了一条隔壁函数早就记过的教训

- 日期 · 2026-08-28 · PR #5 拆分后第十三轮自动评审，两条都成立
- 类型：**事实证伪**（两条打的都是**上一轮刚加的代码**）
- 冲击的需求：P4 · D4
- 结论：**采纳**

### 一、`onDisk` 把「读不到」当成了「没有」

```ts
function onDisk(): string {
  try { return readFileSync(FILE, 'utf8') } catch { return ABSENT }
}
```

一行 `catch` 吞掉全部读失败。于是记忆文件在读与比对之间变得读不到时
（运营刚建了一个权限收紧的文件），比对拿「看不到」当「没有」放行，
改名把一份**从没读到过**的记忆盖掉，里面的 contacted 与 blocked 跟着消失。

**这正是 ADR-26 在 `readMemory` 里修过的那个塌陷** —— 它就在同一个文件、
隔几十行的地方，而我写这个新函数时一个字都没想起来。

### 二、`fsync` 把文件的刷盘失败也吞了

上一轮我为「刷不动目录」写了一个吞异常的 `fsync`，**它同时被用来刷文件**。
于是磁盘满、IO 错这类**恰恰在刷盘那一刻才浮出来**的失败被吞掉，改名照做，
成功照报 —— 把这个函数存在的理由整个抵消掉。

拆成两个：`fsyncFile` 抛，`fsyncDirBestEffort` 吞。**能吞的只有目录那一个。**

### 修法都是「让判断只有一份」

第一条抽出 `isAbsence(e)`，`readMemory` 与 `onDisk` 共用 —— 因为
**上一次出事（ADR-46）正是同一个判断有两份副本、只修好了一份**。
合并之后，原先守 `readMemory` 那条 `M-P4-i` 与新加的那条指向了同一行，
又是一次「两条变异在重复守一件事」，合并成一条。

第二条不是合并而是**拆分**：原先一个函数吞两种失败，而这两种失败该不该吞
是相反的。**该合的合、该拆的拆，判据都是「它们是不是同一件事」。**

### 教训

**「隔壁函数刚记过的教训」不会自动作用到我下一个函数上。**

到这一轮为止，同一个形状出现了三次：ADR-42（权限收得太晚）、
ADR-46（新写的原子写没带上权限处理）、本条（新写的两个函数各犯一条旧账）。
三次都是**新代码重犯旧教训**，而不是旧代码没修好。

写下教训防不住这件事 —— 教训在决策记录里，而我写新函数时看的是代码。
唯一有效的是**让那个判断只存在一份**：一份的时候，新调用方自动继承它；
两份的时候，继承靠的是我记得，而我不记得。
