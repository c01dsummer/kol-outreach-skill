# 见 docs/adr/

决策记录已拆成一条一个文件，放在 [`docs/adr/`](docs/adr/)，索引在
[`docs/adr/README.md`](docs/adr/README.md)。

拆开的理由、编号规则、以及写什么，都在那份索引里。

这份文件只做转发，不装任何内容。**永远不要往这里写记录** ——
一旦写了，同一条决策就有了两个正本，而且没有任何东西会提醒你。
`npm run adr` 会在这里再次出现 `## ADR-` 分节时报错。

> 还装着整册记录的分支，跑一次 `npm run adr -- --split` 就地拆开，不必手工搬。
