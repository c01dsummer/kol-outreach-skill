# 见 AGENTS.md

本仓库的约定写在根目录的 `AGENTS.md`,那是正本 —— 因为它是跨工具约定,
Codex 等工具直接读它,而 Claude Code 读这一份。

**开工前先读 `AGENTS.md`,再按它路由。**

这份文件只做转发,不装任何规则。**永远不要往这里写内容** ——
一旦写了,两个工具看到的就是两套不一样的约定,而且没有任何东西会提醒你。

> 也可以用符号链接代替本文件(`ln -s AGENTS.md CLAUDE.md`)。
> 保留成一个普通文件是为了在 Windows、打包分发、以及某些 git 配置下也能work。
